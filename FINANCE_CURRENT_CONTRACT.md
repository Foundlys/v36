# Current Finance continuation contract

Baseline: Calendar PR #15 checkpoint `1225a069316eed37b06aa3332a8b33355c9e7356`, tree `b696deb929b4a42ad9c16cac71994a79571f917d`; CI 125 attempt 2 PASS. Attempt 1 stopped at a Sales fixture port collision after all Calendar tests passed. No old Calendar evidence is reconstructed.

The frozen Finance engine remains unchanged. Its report/dashboard forecast previously counted entries outside the selected horizon; the integration layer now uses the exact half-open window, signed safe integer cents and legal-entity currency. Explicit scenarios bind the entire current forecast/entity source hash and can alter only opening cash, whole-day horizon and existing entry dates/amounts. Zero values remain valid, malformed/null/duplicate/overflow values fail, and multiple entities are never implicitly combined. Equal-date latest plans require an explicit source rather than choosing arbitrarily.

Recorded outcome comparison uses only same-entity, same-currency posted BANK journal movements in the elapsed window. Missing or incomplete/malformed sources are unavailable, not fabricated zero results. Current journal references/hash support the displayed variance. Recommendations are conditional on recorded data; provider completeness, predictive quality and causality remain unverified. No scenario mutates sources, makes a payment, posts a journal or files a return.

Internal period closure now requires separate current review, exact source fingerprint, reason, confirmation and stable request ID. The complete bounded internal selection checks overlap, journal/account/currency integrity, exact balances, draft invoices and unreconciled imported transactions. The native close is executed inside a rollback-capable transaction with a retained receipt and durable canonical-event outbox. Current roles/capabilities apply on preview, close and retry. Failed persistence leaves the period open; an exact retry after a lost result returns the original closing. The existing engine prevents subsequent postings in the closed period. This does not certify legal/jurisdiction completeness, bank completeness, tax correctness or filing acceptance.

Native Finance and registered typed ZERO tools share these services. The protected screen offers both transports, separate period review/confirmation, dirty-input guards and exact uncertain-result retries. ZERO persists source-free references; current data is recomputed on replay. HTTP fixtures use actual local member sessions and encrypted persistence; synthetic DOM tests are not actual browser acceptance. Test files are `finance-cash-scenarios-{test,api-test,client-test}.js` and `finance-period-closing-{test,api-test,client-test}.js`.

The new full regression, source manifest and configuration-profile results are recorded in `FINANCE_CURRENT_EVIDENCE.json` after completion. Phase 24 remains active; unrelated Marketing and Procurement gaps continue. Production/main/credentials are unchanged.
