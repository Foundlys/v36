'use strict';
(function(root){
 const fallback={legend:'Deze uitvoering met ZERO controleren',prepare:'Uitvoering met ZERO voorbereiden',confirm:'Bevestig uitvoering via ZERO',retry:'Dezelfde ZERO-uitvoering controleren',reason:'Reden voor deze uitvoering',dirty:'Invoer gewijzigd; controleer deze uitvoering opnieuw.',loading:'Uitvoerinvoer controleren…',reason_required:'Vul een reden van 1 tot 500 tekens in voor deze uitvoering.',review:'Er is geen run gestart. Controleer en bevestig deze exacte invoer.',invalid:'De ontvangen uitvoerbevestiging kon niet worden gecontroleerd.',failed:'De workflowuitvoering kon niet worden gecontroleerd.',unconfirmed:'De uitkomst is nog niet bevestigd. Controleer dezelfde aanvraag opnieuw; de bevestigde invoer blijft vaststaan.',confirmed:'Native run {id}: {status}.',approval_separate:'Gebruik de afzonderlijke goedkeuringsstap.',event:'Event: {value}',inputs:'Invoer: {value}',trigger:'Workflowtrigger: {value}',action_details:'Actiedefinitie: {value}',condition:'Voorwaarde: {value}',matches:'waar',skipped:'onwaar (wordt overgeslagen)'};
 const object=value=>value!==null&&typeof value==='object'&&!Array.isArray(value),hash=value=>typeof value==='string'&&/^[a-f0-9]{64}$/.test(value),integer=value=>Number.isSafeInteger(value)&&value>=0,clone=value=>JSON.parse(JSON.stringify(value));
 Object.assign(fallback,{"recover":"Eerdere uitvoeraanvraag controleren","recovery_pending":"Een eerdere uitvoeraanvraag is nog niet bevestigd. Controleer eerst die aanvraag.","recovery_loading":"Eerdere uitvoeraanvraag controleren…","recovery_not_applied":"Deze aanvraag heeft geen run gestart en is afgesloten. Een vertraagd verzoek met dezelfde referentie kan niet meer starten.","storage_required":"Herstelgegevens kunnen niet veilig worden bewaard. Vernieuw deze weergave voordat je een uitvoering bevestigt.","confirmed_metadata":"De uitkomst is bevestigd, maar de lokale herstelverwijzing kon niet worden verwijderd. Controleer deze na verversen.","recovery_unavailable":"De eerder geregistreerde run of workflowversie is niet meer beschikbaar. De uitvoering is niet opnieuw gestart; controleer de uitvoerhistorie."});
 const canonical=value=>Array.isArray(value)?value.map(canonical):object(value)?Object.fromEntries(Object.keys(value).sort().map(key=>[key,canonical(value[key])])):value,same=(a,b)=>JSON.stringify(canonical(a))===JSON.stringify(canonical(b));
 const digest=async value=>Array.from(new Uint8Array(await root.crypto.subtle.digest('SHA-256',new TextEncoder().encode(JSON.stringify(value)))),byte=>byte.toString(16).padStart(2,'0')).join('');
 const statuses=new Set(['RUNNING','PLANNED','SUCCEEDED','ERROR','BLOCKED','AWAITING_APPROVAL','WAITING_TIME','WAITING_RETRY','DEAD_LETTER','RECOVERY_READY']),stepStatuses=new Set([...statuses,'FAILED','PLANNED_INTERNAL','SKIPPED_CONDITION']);
 const invalid=()=>Object.assign(Error('automation_run_ack_invalid'),{code:'automation_run_ack_invalid'}),shape=row=>({name:row.name,version:row.version,trigger:row.trigger,actions:row.actions,enabled:row.enabled!==false,approval_required:Boolean(row.approval_required)});
 function create({document,workflow,inputHost,getInput,canRequest,onBusy,zeroRequest,request=null,isActive=()=>true,requestContext,mode='zero',restorePending=true}){
  workflow=clone(workflow);if(requestContext)requestContext=Object.freeze({...requestContext});
  const i18n=root.FoundlyI18n,bindings=new Map(),el=tag=>document.createElement(tag),box=el('fieldset'),active=()=>box.isConnected&&inputHost.isConnected&&isActive();
  const modeKeys=new Set(['legend','prepare','confirm','retry']),nativeCopy={legend:'Handmatige uitvoering controleren',prepare:'Uitvoering voorbereiden',confirm:'Bevestig handmatige uitvoering',retry:'Dezelfde uitvoeraanvraag controleren'};
  box.setAttribute('data-workflow-run-mode',mode);
  const copy=(key,params={})=>mode==='native'&&modeKeys.has(key)?i18n?i18n.t('workflow.manual.'+key,params):nativeCopy[key]:i18n?i18n.t('workflow.run.'+key,params):(fallback[key]||key).replace(/\{(\w+)\}/g,(_,name)=>String(params[name])),shared=(key,fallback,params={})=>i18n?i18n.t(key,params):fallback.replace(/\{(\w+)\}/g,(_,name)=>String(params[name])),number=value=>i18n?i18n.number(value,{maximumSignificantDigits:21}):String(value),status=value=>shared('workflow.status.'+value.toLowerCase(),value);
  function bind(node,read){const value={read,last:read()};node.textContent=value.last;bindings.set(node,value);return node;}
  const owned=(tag,key)=>bind(el(tag),()=>copy(key)),legend=owned('legend','legend'),prepare=owned('button','prepare'),confirm=el('button'),reasonLabel=el('label'),reason=el('input'),output=el('pre'),notice=el('output');box.append(legend);reasonLabel.append(owned('span','reason'),reason);reason.maxLength=500;prepare.type=confirm.type='button';prepare.setAttribute('data-workflow-run-action','prepare');confirm.setAttribute('data-workflow-run-action','confirm');notice.setAttribute('aria-live','polite');box.append(prepare,output,reasonLabel,confirm,notice);
  let preview=null,pending=null,generation=0,busy=false;bind(confirm,()=>copy(pending?'retry':'confirm'));confirm.disabled=true;
  const metadataFields=['request_id','request_fingerprint','workflow_id','event_id','workflow_signature','request_signature'],id=value=>typeof value==='string'&&/^[A-Za-z0-9][A-Za-z0-9_.:-]{0,199}$/.test(value),validMetadata=value=>object(value)&&Object.keys(value).length===metadataFields.length&&metadataFields.every(key=>Object.hasOwn(value,key))&&['request_id','workflow_id','event_id'].every(key=>id(value[key]))&&['request_fingerprint','workflow_signature','request_signature'].every(key=>hash(value[key]));
  const recoverable=Boolean(requestContext),realmValid=object(requestContext)&&['tenant_id','dealer_id','actor_id'].every(key=>typeof requestContext[key]==='string'&&requestContext[key].length>0&&requestContext[key].length<=200),storageKey=realmValid?'foundly.workflow.runs.v1:'+JSON.stringify([requestContext.tenant_id,requestContext.dealer_id,requestContext.actor_id,workflow.id]):null,prior=[];let storageReady=true;
  const storageError=()=>Object.assign(Error('automation_run_storage_required'),{runStorage:true});
  function readPending(){if(!storageKey||typeof request!=='function')throw storageError();const raw=root.sessionStorage.getItem(storageKey),rows=raw===null?[]:JSON.parse(raw);if(!Array.isArray(rows)||rows.length>100||rows.some(row=>!validMetadata(row)||row.workflow_id!==workflow.id)||new Set(rows.map(row=>row.request_id)).size!==rows.length)throw storageError();return rows;}
  function writePending(rows){if(rows.length)root.sessionStorage.setItem(storageKey,JSON.stringify(rows));else root.sessionStorage.removeItem(storageKey);}
  function remember(meta){if(!recoverable)return;try{if(!storageReady||!validMetadata(meta))throw storageError();const rows=readPending(),existing=rows.find(row=>row.request_id===meta.request_id);if(existing){if(!same(existing,meta))throw storageError();return;}if(rows.length>=100)throw storageError();writePending([...rows,meta]);}catch{storageReady=false;throw storageError();}}
  function forget(meta){if(!recoverable)return true;try{const rows=readPending(),existing=rows.find(row=>row.request_id===meta.request_id);if(existing&&!same(existing,meta))throw storageError();writePending(rows.filter(row=>row.request_id!==meta.request_id));return true;}catch{storageReady=false;return false;}}
  document.addEventListener?.('foundly:locale',()=>{if(!active())return;for(const [node,entry]of bindings)if(node.textContent===entry.last){entry.last=entry.read();node.textContent=entry.last;}});
  const message=key=>bind(notice,()=>copy(key)),clear=()=>{bindings.delete(output);output.textContent='';};
  function feedback(error,uncertain=false){if(error.runStorage){message('storage_required');return;}const status=error.status||error.statusCode;if([401,403].includes(status)){bind(notice,()=>shared(status===403?'common.access_denied':'identity.auth_required',status===403?'Toegang geweigerd.':'Meld je opnieuw aan.'));return;}message(uncertain?'unconfirmed':error.code==='automation_run_ack_invalid'?'invalid':'failed');}
  const unresolved=()=>prior.some(row=>!row.done),blocked=()=>Boolean(pending)||unresolved()||!storageReady;
  function lock(){if(!active())return;onBusy(busy||blocked());prepare.disabled=busy||blocked();confirm.disabled=busy||unresolved()||!storageReady||!pending&&!preview;reason.disabled=busy||blocked();for(const row of prior)row.button.disabled=busy||row.done;const entry=bindings.get(confirm);entry.last=entry.read();confirm.textContent=entry.last;}
  const changed=()=>{if(!active()||pending)return;generation++;preview=null;clear();confirm.disabled=true;message('dirty');};inputHost.addEventListener('input',changed);inputHost.addEventListener('change',changed);
  async function verifyPreview(data,input){
   const realm=data?.request_context;
   if(!object(data)||!object(realm)||!['tenant_id','dealer_id','actor_id'].every(key=>typeof realm[key]==='string'&&realm[key].length>0)||requestContext&&!['tenant_id','dealer_id','actor_id'].every(key=>realm[key]===requestContext[key])||realm.tenant_id!==workflow.tenant_id||realm.dealer_id!==workflow.dealer_id||data.workflow_id!==workflow.id||data.workflow_name!==workflow.name||data.workflow_version!==workflow.version||data.workflow_signature!==workflow.signature||!hash(data.workflow_signature)||!hash(data.request_signature)||!hash(data.request_fingerprint)||!hash(data.preview_fingerprint)||!object(data.event)||!object(data.inputs)||!object(data.definition)||!same(data.definition,shape(workflow))||data.approval_required!==workflow.approval_required||data.execution_performed!==false||data.run_created!==false||data.source_result_retained!==false||!integer(data.activation_revision)||!integer(data.recovery_revision)||!Array.isArray(data.steps)||data.steps.length!==workflow.actions.length)throw invalid();
   if(data.steps.some((step,index)=>!object(step)||step.index!==index||step.type!==workflow.actions[index].type||step.title!==(workflow.actions[index].title||workflow.actions[index].message||null)||typeof step.condition_matched!=='boolean'))throw invalid();
   if(data.request_fingerprint!==await digest(input)||data.workflow_signature!==await digest(shape(data.definition))||data.request_signature!==await digest(canonical({workflow:data.workflow_signature,trigger:data.event,inputs:data.inputs})))throw invalid();
  }
  function describe(data){
   const lines=[shared('workflow.inspector.heading','{name} · versie {version}',{name:data.workflow_name,version:number(data.workflow_version)}),copy('trigger',{value:JSON.stringify(data.definition.trigger)}),copy('event',{value:JSON.stringify(data.event)}),copy('inputs',{value:JSON.stringify(data.inputs)}),shared('workflow.generator.approval','Goedkeuring vóór uitvoering: {value}',{value:shared('workflow.generator.'+(data.approval_required?'required':'not_required'),data.approval_required?'vereist':'niet vereist')})];
   for(const [index,action]of data.definition.actions.entries()){
    lines.push('',number(index+1)+'. '+shared('workflow.action.'+action.type,action.type));
    for(const key of ['title','content','message','seconds'])if(Object.hasOwn(action,key))lines.push(shared('workflow.generator.'+key,({title:'Titel',content:'Inhoud',message:'Melding',seconds:'Wachttijd'})[key])+': '+(typeof action[key]==='number'?number(action[key]):action[key]));
    const rest=Object.fromEntries(Object.entries(action).filter(([key])=>!['type','title','content','message','seconds'].includes(key)));if(Object.keys(rest).length)lines.push(copy('action_details',{value:JSON.stringify(rest)}));lines.push(copy('condition',{value:copy(data.steps[index].condition_matched?'matches':'skipped')}));
   }
   return lines.join('\n');
  }
  async function verifyResult(data,current,recovery=false){
   const run=data?.run,p=current.preview.data,realm=p.request_context;
   if(!object(data)||data.approval_is_separate!==true||!object(run)||typeof run.run_id!=='string'||!run.run_id||run.automation_id!==workflow.id||run.workflow_version!==workflow.version||run.event_id!==(p.event.event_id||p.event.id)||run.actor_id!==realm.actor_id||run.tenant_id!==realm.tenant_id||run.dealer_id!==realm.dealer_id||run.request_signature!==p.request_signature||!same(run.trigger,p.event)||!same(run.inputs,p.inputs)||!statuses.has(run.status)||run.replay_safe!==true||!Array.isArray(run.steps)||run.steps.length>workflow.actions.length||run.steps.some((step,index)=>!object(step)||step.index!==index||step.type!==workflow.actions[index].type.toLowerCase()||!stepStatuses.has(step.status))||!Array.isArray(run.outputs)||!Array.isArray(run.errors))throw invalid();
   if(run.steps.some((step,index)=>!same(step.input,workflow.actions[index])||step.idempotency_key!==`workflow:${run.run_id}:${index}`||step.status==='SKIPPED_CONDITION'&&p.steps?.[index]?.condition_matched===true||step.status==='SUCCEEDED'&&p.steps?.[index]?.condition_matched===false||step.status==='SUCCEEDED'&&step.type!=='delay'&&(!integer(step.output_index)||run.outputs[step.output_index]?.executed!==true)))throw invalid();
   if(run.status==='SUCCEEDED'&&(run.steps.length!==workflow.actions.length||run.steps.some(step=>!['SUCCEEDED','SKIPPED_CONDITION'].includes(step.status))))throw invalid();
   if(!recovery){const ack=run.request_acknowledgement;if(!object(ack)||!same(ack,{...current.metadata,request_context:realm}))throw invalid();}
   return run;
  }
  async function verifyRecovery(data,meta,current){
   const realm=requestContext||current?.preview.data.request_context;
   if(!object(data)||!metadataFields.every(key=>data[key]===meta[key])||!same(data.request_context,realm)||data.execution_performed!==false||data.outcome_reverified!==false||data.approval_is_separate!==true||!['APPLIED','NOT_APPLIED'].includes(data.state))throw invalid();
   if(data.state==='NOT_APPLIED'){if(data.run!==null)throw invalid();return null;}
   const run=data.run;if(!object(run)||!object(run.trigger)||!object(run.inputs)||run.event_id!==meta.event_id||run.request_signature!==meta.request_signature||meta.workflow_id!==workflow.id||meta.workflow_signature!==workflow.signature||await digest(shape(workflow))!==meta.workflow_signature||await digest(canonical({workflow:meta.workflow_signature,trigger:run.trigger,inputs:run.inputs}))!==meta.request_signature)throw invalid();
   return verifyResult(data,current||{preview:{data:{request_context:realm,event:run.trigger,inputs:run.inputs,request_signature:meta.request_signature}}},true);
  }
  function confirmed(run,clean=true){bind(notice,()=>copy('confirmed',{id:run.run_id,status:status(run.status)})+(run.status==='AWAITING_APPROVAL'?' '+copy('approval_separate'):'')+(!clean?' '+copy('confirmed_metadata'):''));}
  async function reconcile(meta,current=null,row=null){
   const data=await request('/api/automation/run-requests/recover',{method:'POST',body:JSON.stringify({...meta,confirm:true})});if(!active())return;
   const run=await verifyRecovery(data,meta,current);if(!active()||current&&pending!==current)return;
   const clean=forget(meta);if(row)row.done=true;if(current)pending=null;preview=null;clear();if(run)confirmed(run,clean);else message(clean?'recovery_not_applied':'confirmed_metadata');
  }
  if(recoverable){try{const restored=readPending();for(const meta of restorePending?restored:[]){const button=owned('button','recover');button.type='button';button.setAttribute('data-workflow-run-action','recover');const row={meta,button,done:false};prior.push(row);box.append(button);button.addEventListener('click',async()=>{if(!active()||busy||row.done||pending)return;busy=true;lock();message('recovery_loading');try{await reconcile(meta,null,row);}catch(error){if(active()){if(error.code==='automation_run_request_unavailable'){row.done=true;forget(meta);message('recovery_unavailable');}else feedback(error,true);}}finally{busy=false;lock();}});}if(prior.length)message('recovery_pending');}catch{storageReady=false;message('storage_required');}prepare.disabled=unresolved()||!storageReady;reason.disabled=prepare.disabled;Promise.resolve().then(lock);}
  prepare.addEventListener('click',async()=>{
   if(busy||blocked()||!active()||!canRequest())return;busy=true;preview=null;clear();lock();message('loading');const start=generation;
   try{const payload=getInput(),input=clone({event:payload.event,inputs:payload.options.inputs}),data=await zeroRequest({operation:'RUN_PREVIEW',workflow_id:workflow.id,input},root.crypto.randomUUID());if(!active()||generation!==start)return;await verifyPreview(data,input);if(!active()||generation!==start)return;preview={input,data:clone(data),signature:JSON.stringify(input),generation:start};const shown=preview.data;bind(output,()=>describe(shown));message('review');}
   catch(error){if(active()&&generation===start)feedback(error);}finally{busy=false;lock();}
  });
  confirm.addEventListener('click',async()=>{
   if(busy||!active()||confirm.disabled)return;
   if(!pending){
    if(!preview||!canRequest())return;if(!reason.value.trim()||reason.value.trim().length>500){message('reason_required');return;}
    try{const payload=getInput();if(preview.generation!==generation||JSON.stringify({event:payload.event,inputs:payload.options.inputs})!==preview.signature){changed();return;}}catch(error){feedback(error);return;}
    pending={preview,turn:root.crypto.randomUUID(),uncertain:false,submitted:false,action:clone({operation:'RUN_SUBMIT',workflow_id:workflow.id,input:{...preview.input,preview_fingerprint:preview.data.preview_fingerprint,confirm:true,reason:reason.value.trim(),request_id:root.crypto.randomUUID()}})};
   }
   const current=pending;busy=true;lock();
   try{
    if(current.submitted&&typeof request==='function'){await reconcile(current.metadata,current);return;}
    const p=current.preview.data;current.metadata??={request_id:current.action.input.request_id,request_fingerprint:await digest(current.action.input),workflow_id:workflow.id,event_id:p.event.event_id||p.event.id,workflow_signature:p.workflow_signature,request_signature:p.request_signature};if(!active()||pending!==current)return;remember(current.metadata);if(!active())return;
    current.submitted=true;const data=await zeroRequest(clone(current.action),current.turn);if(!active()||pending!==current)return;const run=await verifyResult(data,current);if(!active()||pending!==current)return;const clean=forget(current.metadata);pending=null;preview=null;clear();confirmed(run,clean);
   }catch(error){if(active()&&pending===current){current.uncertain=current.submitted;feedback(error,current.uncertain);}}finally{busy=false;lock();}
  });return box;
 }
 function createNative(options){
  return create({...options,mode:'native',zeroRequest:async action=>options.request('/api/automation/workflows/'+encodeURIComponent(action.workflow_id)+(action.operation==='RUN_PREVIEW'?'/run-preview':'/run-confirmation'),{method:'POST',body:JSON.stringify(action.input)})});
 }
 root.FoundlyWorkflowRunZero={create,createNative};
})(globalThis);
