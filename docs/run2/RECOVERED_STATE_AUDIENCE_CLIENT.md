# Same Run 2: localized audience controls and browser outcome recovery

Frozen source `1235daa47e856b131f43962578c84faa0bbf0959`, tree `5958e7f6a65784d1066813412a9d58247560dea9`: all 924 ZERO tests, 14 corpus cases and eight affected native commands PASS. Twenty-nine new permanent tests cover the actual controller and shared page, both native and ZERO HTTP, dropped committed member/filter responses, encrypted server restart and fresh client recovery. This source needs its own exact published complete CI.

The shared audience detail delegates to the new localized control. Its scoped recovery queue contains only seven exact metadata fields, including a hash of the participant reference. It never stores entered names/references, email, permission evidence, reason or the write body. Uncertain requests recover the original outcome explicitly; they do not post changed input again. Earlier grants remain distinct from a later opt-out, and absent requests are durably retired. Clean detail reopen reads current authority; denied access clears the shared workspace. A global recovery control works even when the audience is outside the first 100 visible records.

All eight locales preserve form nodes, literal data, pending input and filter selection. Unknown permission remains unknown. Read-only selection exposes recorded-permission limits without enabling writes. Member paging checks native context, revisions, counts and uniqueness by participant and history ID. Selection filters must match the displayed audience source. Native integration keeps all previous campaign/journey execution assertions while replacing unsafe audience body replay with one metadata recovery.

The manifest preserves two reproduced product defects (mismatched selection filters and repeated participants across pages), fixture corrections, and the prematurely launched dirty-tree local regression. That early run is not exact-tree evidence. Only the subsequent clean frozen source runs count. DOM fixtures do not prove visual layout or accessibility acceptance.

Native audience recovery has complete matching CI 226. The immediate continuation is journey definition, enrollment and execution recovery, followed by their localized production controls and all other genuinely open Run 2 gates. No main merge, deployment or Run 3 action.
