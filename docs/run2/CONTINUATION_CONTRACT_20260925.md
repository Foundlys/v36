# CONTINUE THE CURRENT FOUNDLY RUN 2 — DO NOT START A NEW RUN

The previous Work session executing:

**RUN 2 — ZERO COGNITIVE, AGENTIC & GLOBAL INTELLIGENCE MASTERBUILD**

was interrupted / became stuck.

This is a **CONTINUATION OF THE SAME AUTHORITATIVE RUN 2**.

It is **NOT** a new run.
It is **NOT** a restart.
It is **NOT** permission to reorient the project.
It is **NOT** permission to rebuild already completed work.

The existing Run-2 masterprompt and the Foundly OS roadmap remain the **AUTHORITATIVE EXECUTION CONTRACT**.

Your job is to recover the exact latest execution state from repository truth, preserve all completed work, continue from the first genuinely open gap, and autonomously finish Run 2 to the full acceptance standard.

# ============================================================
# 1. REPOSITORY TRUTH OVERRIDES CHAT PROSE
# ============================================================

DO NOT assume the final visible message from the previous chat is the repository truth.

Before making any implementation change, inspect and reconcile the real environment.

Canonical repository:

`Foundlys/v36`

Current Run-2 work has been proceeding on:

**PR #21**

Recover and reconcile at minimum:

- current repository;
- current worktree;
- current branch;
- local HEAD;
- `origin/main`;
- relevant remote Run-2 branch;
- PR #21 actual HEAD;
- local versus remote tree;
- staged changes;
- modified tracked files;
- relevant untracked files;
- stashes;
- unpublished local commits;
- pushed commits;
- latest GitHub Actions runs;
- failed CI evidence;
- successful CI evidence;
- targeted tests;
- full regressions;
- ZERO evaluation results;
- corpus results;
- localization evidence;
- connector/provider evidence;
- migrations/config changes;
- runtime/deployment changes if any;
- evidence/checkpoint logs generated during Run 2.

If the repository is **newer than the checkpoint below**, preserve the newer work and continue from that newer repository state.

Never roll back newer valid work merely to match this prompt.

Repository / PR / CI / test truth > previous-chat prose.

Preserve unpublished work before any reset, checkout, clean, merge, rebase, restore or other destructive Git operation.

No force pushes.
No destructive resets.
No credential mutation.
No fabricated provider evidence.
No production mutation unless explicitly permitted by the authoritative Run-2 contract.

# ============================================================
# 2. LATEST KNOWN AUTHORITATIVE CHECKPOINT
# ============================================================

Use this only as the minimum expected state to recover.

Current Run-2 branch / PR:

**PR #21**

Latest confirmed broad evidence:

- **CI #197 PASS**
- **CI #198 PASS**
- full regression PASS
- **491 ZERO tests PASS**
- **14 corpus cases PASS**

Phase-8 localization has progressed substantially.

Confirmed localization state:

- **103 section names** localized across all 8 required locales;
- **72 existing section descriptions** localized across all 8 required locales;
- **38 targeted localization tests PASS**;
- native workspace regression PASS;
- user/customer-created workspace names remain literal;
- source/provider names remain literal;
- locale switching does not trigger unnecessary source/data requests;
- selected tab/section remains preserved;
- source content and user input remain preserved;
- missing/mismatched source responses remain unknown/unverified rather than becoming false empty lists;
- stale/late responses cannot overwrite a newer section view.

Immediately before this continuation, the active action was:

1. publish the exact tested section-name + section-description localization checkpoint to the same Run-2 PR;
2. confirm the published PR tree exactly matches the tested local tree;
3. continue directly with the remaining **package / identity / settings / forms / other untranslated Phase-8 surfaces**.

DO NOT assume that publication is still outstanding.

Inspect the repository and PR first.

If that checkpoint has already been published, identify the next open localization or Run-2 acceptance gap and continue there.

# ============================================================
# 3. IMPORTANT PRIOR RUN-2 EVIDENCE — DO NOT REBUILD IT
# ============================================================

The following classes of work have already undergone extensive hardening during this same Run 2.

Do not broadly rebuild or re-audit them unless:
- a current regression fails;
- a new dependency exposes a genuine defect;
- the authoritative acceptance contract requires missing evidence.

Completed/hardened areas include substantial work across:

- CRM access-loss clearing;
- stale-response protection;
- CRM records;
- pipeline;
- customer profiles;
- CRM automations;
- source forms;
- dashboard save/retry;
- request idempotency;
- revision validation;
- storage rollback;
- permission revalidation;
- encrypted restart recovery;
- explicit-permission users without role;
- atomic profile/pipeline/stage/dashboard/evidence creation;
- event/activity durability;
- provider/source persistence;
- workspace access clearing;
- export reauthorization;
- snapshot access races;
- knowledge registry permission filtering;
- knowledge export permissions;
- unknown-versus-zero handling;
- finance currency correctness;
- mixed-currency protection;
- historical currency conflict handling;
- connector revision control;
- encrypted stored request acknowledgments;
- lost-response recovery;
- corrupted-storage rejection;
- connector probe/sync truthfulness;
- provider verification races;
- selected-provider enforcement;
- encoded connector/profile authorization bypasses;
- provider-source storage separate from Customer Truth;
- provider import recovery after storage or response loss;
- localization of many CRM/workspace/source surfaces.

Earlier failed CI runs such as #194, #195 and #196 are part of the honest evidence trail.

Do not erase or disguise failed runs.

Where the underlying defect or fixture issue has been corrected and later successful evidence exists, preserve both the failure and the successful follow-up evidence.

# ============================================================
# 4. AUTHORITATIVE RUN-2 OBJECTIVE
# ============================================================

Run 2 must transform ZERO into a production-grade cognitive, agentic and global operating intelligence.

Run 2 includes, at minimum:

- natural conversation;
- intent resolution;
- ambiguity handling;
- long context;
- personalization;
- social intelligence;
- contextual professional humor;
- multilingual reasoning;
- multi-step planning;
- causal/business reasoning;
- uncertainty handling;
- contradiction detection;
- proactive insights;
- outcome recommendations;
- layered memory;
- context assembly;
- model routing;
- fallback/degradation logic;
- specialist agents/subagents;
- permission-aware tool/capability discovery;
- deep cross-module reasoning;
- governed online research;
- knowledge/regulation refresh;
- Stack Discovery V1;
- production localization;
- hyper-realistic ZERO voice;
- Demo Universe Engine V1;
- Automotive Demo Universe V1;
- Automotive Industry Data & Knowledge foundation;
- permanent ZERO evaluation framework;
- universal search;
- attention intelligence;
- data-quality intelligence;
- AI economics/runtime governance;
- security hardening;
- performance;
- accessibility;
- responsive behavior;
- final regression;
- CI;
- release/deployment verification where permitted;
- final evidence and verdict.

# ============================================================
# 5. RUN-2 PHASES
# ============================================================

Preserve the authoritative sequence conceptually:

0. recover real execution state / baseline
1. current competitive discovery
2. ZERO evaluation baseline
3. memory / context assembly
4. model routing / agent orchestration
5. cognitive / social ZERO
6. governed research / knowledge refresh
7. Stack Discovery V1
8. full localization
9. hyper-realistic voice
10. Demo Universe Engine V1
11. Automotive Demo + knowledge
12. complex cross-module acceptance
13. UX / accessibility / responsive acceptance
14. adversarial / performance / cost acceptance
15. full regression / CI
16. release / deployment verification where permitted
17. final evidence / verdict

DO NOT restart completed phases simply because the new chat lacks conversational context.

Determine their actual evidence state from repository truth and existing artifacts.

Continue from the first materially open requirement.

# ============================================================
# 6. CURRENT PRIORITY — PHASE 8 LOCALIZATION CLOSURE
# ============================================================

At the latest known checkpoint, Phase 8 remained active.

Continue closing all remaining user-facing localization gaps.

Known likely remaining classes include:

- package settings;
- package descriptions;
- identity-related surfaces;
- configuration forms;
- form labels;
- validation messages;
- buttons;
- tabs not already covered;
- headings not already covered;
- helper text;
- statuses;
- tooltips;
- empty states;
- errors;
- confirmations;
- accessibility labels;
- shared components;
- any remaining hard-coded user-facing strings.

Required locales:

- `nl-NL`
- core English / `en-GB`
- `de-DE`
- `fr-FR`
- `es-ES`
- `da-DK`
- `nb-NO`
- `sv-SE`

Hard requirements:

- no mixed-language production experience;
- preserve customer/source/provider literal values;
- do not translate arbitrary customer-entered content;
- do not mutate IDs or machine identifiers;
- locale switching must not unnecessarily refetch data;
- locale switching must not discard forms/input;
- locale switching must not change selected object/tab/section;
- locale switching must not fabricate values;
- unknown must remain unknown;
- zero must remain genuine zero;
- missing data must not become zero, empty, success or “connected”;
- different currencies must not be silently combined;
- dates/numbers/currencies must follow appropriate locale semantics where applicable;
- stale responses must not overwrite newer state;
- revoked permissions must clear/restrict private state immediately and remain protected against late responses.

Do not close Phase 8 based merely on string-count reduction.

Close it only when the actual user-facing production surfaces satisfy the acceptance contract.

# ============================================================
# 7. HYPER-PERFECT QUALITY LAW
# ============================================================

“Working” is NOT equivalent to “complete”.

PASS requires evidence.

Evaluate every relevant capability across:

- capability depth;
- intelligence;
- correctness;
- UX;
- visual quality;
- psychological usability;
- accessibility;
- performance;
- reliability;
- security;
- privacy;
- auditability;
- interoperability;
- composability;
- maintainability;
- localization;
- recoverability;
- scalability;
- operability;
- economics;
- data quality;
- outcome evidence.

Never claim:

- LIVE;
- CONNECTED;
- READY;
- realtime;
- synchronized;
- verified;
- accepted;
- production-ready;
- provider-success;
- fully localized;
- complete;

unless the evidence genuinely supports the claim.

Unknown ≠ zero.

Configured ≠ connected.

Credentials present ≠ provider success.

Stored records ≠ proof of current successful synchronization.

Fetched provider data ≠ Customer Truth.

A test fixture ≠ provider evidence.

A partial test run ≠ full regression PASS.

A CI job killed by timeout ≠ PASS.

# ============================================================
# 8. ONE CORE / ANY COMBINATION
# ============================================================

Preserve the architecture:

FOUNDLY CORE
→ ZERO
→ UNIVERSAL MODULES / CAPABILITIES
→ INDUSTRY PACK
→ CUSTOMER COMPOSITION

No customer forks.

Any valid module combination must remain supportable.

Standalone modules and valid compositions must eventually support:

DISCOVER
→ DESIGN
→ PROVISION
→ CONNECT
→ MAP
→ CONFIGURE
→ MIGRATE / SYNC
→ AUTOMATE
→ TEST
→ VERIFY
→ DEPLOY
→ MONITOR
→ RECOVER
→ OPTIMIZE

# ============================================================
# 9. CUSTOMER TRUTH / PROVIDER TRUTH
# ============================================================

Customer-owned operational reality is authoritative for customer-owned objects unless a governed mapping explicitly says otherwise.

External/provider data may:

- enrich;
- validate;
- classify;
- supplement;

but must never silently replace Customer Truth.

Provider source storage must remain distinguishable from customer operational records.

Every important fact/state should remain classifiable where applicable as:

- PROVEN
- CUSTOMER_CONFIRMED
- INFERRED
- UNKNOWN
- BLOCKED

No fake synchronization success.

No inferred counts masquerading as persisted counts.

No provider ping masquerading as imported source records.

# ============================================================
# 10. ZERO-TRUST AUTONOMY
# ============================================================

ZERO and agents may never:

- self-escalate;
- bypass tenant boundaries;
- bypass entitlements;
- bypass module access;
- bypass capabilities;
- bypass approval requirements;
- reuse stale authorization;
- fabricate credentials;
- fabricate provider success;
- silently weaken controls;
- overwrite customer truth from external sources;
- alter production code outside governed pipelines.

Permissions and configuration must be revalidated around asynchronous boundaries where state may have changed.

Examples:

- DNS resolution;
- provider calls;
- storage waits;
- lost-response recovery;
- replay;
- restart;
- long-running tool calls;
- deferred event delivery.

# ============================================================
# 11. IDEMPOTENCY / RECOVERY / DURABILITY
# ============================================================

For every relevant mutation:

- retries must not duplicate outcomes;
- lost responses must be safely recoverable;
- idempotency identity must remain stable;
- newer data must not be overwritten by an older retry;
- deleted secrets must not be resurrected;
- failed persistence must not leave in-memory mutations treated as committed;
- events/notifications requiring durability must not be marked delivered without acknowledgment;
- restart recovery must preserve the correct state;
- corrupted persistence must fail safely;
- partial compound writes must not leave partial configuration unless explicitly transactionally designed.

Test the actual HTTP/server/storage path where relevant, not only pure-function mocks.

# ============================================================
# 12. GLOBAL LANGUAGE LAW
# ============================================================

All current user-facing production surfaces required by Run 2 must be production-localized for all 8 required locales.

There must be no knowingly unfinished mixed-language user journey at Run-2 PASS.

Keep internal identifiers stable.

Keep literal customer/provider/source content literal.

Foundly-owned UI language must localize.

ZERO output must use the selected language naturally and consistently.

# ============================================================
# 13. VOICE HARD GATE
# ============================================================

ZERO voice is first-class.

It must ultimately support:

- male and female voices;
- Executive mode;
- Conversational mode;
- Briefing mode;
- natural low-latency interaction;
- barge-in;
- context continuation;
- correct names;
- brands;
- industry terminology;
- numbers;
- currencies;
- dates;
- non-robotic cadence.

English quality target:

a refined British adult male, low / mid-low, calm, intelligent, cinematic, professional and naturally conversational, with subtle dry humor.

Foundly must retain its own legally usable voice identity.

No unauthorized cloning.

If voice acceptance is externally blocked by unavailable provider credentials/runtime/hardware, distinguish:

CODE_COMPLETE

from:

EXTERNAL_ACCEPTANCE_BLOCKER.

Never fake the evidence.

# ============================================================
# 14. STACK DISCOVERY V1 HARD GATE
# ============================================================

Do not declare Run 2 PASS unless Stack Discovery V1 satisfies the authoritative contract.

It must reason about heterogeneous customer environments and classify discovered facts truthfully.

At minimum preserve:

- provenance;
- confidence;
- freshness;
- permissions;
- contradictions;
- UNKNOWN/BLOCKED handling;
- current configuration;
- current module ownership/access;
- provider/tool identity;
- safe recovery from partial discovery.

Difficult environments must be tested, not merely happy-path mocks.

# ============================================================
# 15. DEMO UNIVERSE / AUTOMOTIVE HARD GATES
# ============================================================

Run 2 includes:

- Demo Universe Engine V1;
- Automotive Demo Universe V1;
- Automotive Industry Data & Knowledge foundation.

Demo provenance types must remain distinguishable:

- LIVE_PROVIDER
- PUBLIC_VERIFIED
- DERIVED_ESTIMATED
- SYNTHETIC_DEMO

Never present synthetic or estimated demo data as live customer/provider truth.

The Automotive Demo must demonstrate coherent cross-module intelligence rather than isolated screens.

# ============================================================
# 16. DASHBOARD VISUAL LAW
# ============================================================

The approved main Foundly dashboard is a frozen visual baseline.

Do NOT:

- redesign it;
- restyle it;
- simplify it;
- reinterpret it;
- replace its visual identity;

unless explicitly authorized by the user.

Allowed changes are limited to what is necessary for:

- correctness;
- security;
- permissions;
- compatibility;
- accessibility;
- localization;
- required integration;
- performance;

while preserving the approved design identity.

Other dashboards must still meet the hyper-perfect visual and psychological UX bar.

# ============================================================
# 17. COMPETITIVE SUPERIORITY
# ============================================================

For every major capability where comparison remains materially relevant:

DISCOVER BEST BENCHMARK
→ CAPABILITY LEDGER
→ GAP
→ IMPLEMENT
→ TEST
→ RED-TEAM
→ PROVE

Minimum target:

credible parity with strong current specialists.

Strategic target:

demonstrable superiority where technically and economically feasible.

Do NOT rerun broad competitive research already completed unless:

- the relevant evidence is materially stale;
- a new major capability was added;
- an unresolved competitive gap requires it.

# ============================================================
# 18. SECURITY / PRIVACY / ACCESS CONTROL
# ============================================================

Every affected path must preserve:

- tenant isolation;
- module isolation;
- entitlement enforcement;
- capability enforcement;
- object-level access where required;
- approval requirements;
- current authorization after role/access change;
- safe stale-response handling;
- private-state clearing after access loss;
- no unauthorized export;
- no unauthorized linked-object disclosure;
- no authorization bypass through encoding, aliases, profiles or alternate routes.

When permissions are revoked during an in-flight request, revalidate before returning, saving or replaying privileged information where required by the contract.

# ============================================================
# 19. ACCESSIBILITY / RESPONSIVE / UX
# ============================================================

Target at least WCAG 2.2 AA where applicable.

Prove relevant:

- keyboard accessibility;
- focus behavior;
- labels;
- contrast;
- reduced motion where applicable;
- responsive layouts;
- non-destructive locale switching;
- status clarity;
- error clarity;
- unknown/degraded states;
- fast task completion;
- absence of misleading success states.

Do not change the approved visual baseline merely to make testing easier.

# ============================================================
# 20. PERFORMANCE / COST / RELIABILITY
# ============================================================

Run the authoritative adversarial/performance/cost acceptance.

Test relevant:

- large datasets;
- pagination;
- counts beyond first page / first 100 records;
- concurrency;
- repeated requests;
- slow providers;
- provider failure;
- storage failure;
- response loss;
- restart;
- stale data;
- rate/cost behavior;
- fallback paths;
- timeout behavior;
- degradation;
- event durability;
- model routing economics;
- AI cost governance.

Do not weaken individual assertions or internal test timeouts merely to make CI green.

A CI job-level timeout may be raised when the complete legitimate suite requires more wall-clock time, provided test-level safeguards remain intact.

# ============================================================
# 21. TESTING STANDARD
# ============================================================

For each defect or acceptance gap:

1. reproduce it first where practical;
2. create a permanent regression test;
3. fix the smallest correct underlying cause;
4. run targeted tests;
5. run affected native/API/UI regressions;
6. run broader ZERO/corpus tests;
7. run full regression when required;
8. publish exactly the tested tree;
9. verify CI against that same tree;
10. preserve evidence.

Do not “fix” tests by weakening assertions merely because they fail.

If a fixture is wrong, correct the fixture while preserving the intended behavioral assertion.

Where appropriate, use real HTTP/storage/restart flows.

# ============================================================
# 22. GIT / CHECKPOINT DISCIPLINE
# ============================================================

Every meaningful checkpoint must be evidence-backed.

Before publishing:

- ensure working tree is understood;
- ensure test result maps to the exact source tree;
- preserve logs/hashes where the Run-2 evidence system uses them;
- avoid mixing untested later edits into a tested checkpoint.

After publishing:

- confirm PR tree equality;
- check CI on the exact commit/tree;
- record failed and successful runs honestly.

Never claim a commit is tested if the tested local tree differs.

# ============================================================
# 23. AUTONOMOUS EXECUTION
# ============================================================

Do not stop after every small fix to ask the user for permission.

Continue autonomously through the authoritative Run-2 contract.

Do not ask routine questions between phases.

If a genuine external credential/provider/human dependency blocks a gate:

- classify it honestly;
- preserve the implemented code;
- test everything that can be tested internally;
- continue with all other independent Run-2 requirements;
- return to the blocker if it becomes actionable.

Do not downgrade or waive an in-scope code defect merely to reach completion.

Do not hide material gaps.

# ============================================================
# 24. REMAINING PHASE-8 WORK
# ============================================================

After recovering repository truth, continue from the actual first open localization gap.

At the latest known checkpoint, likely remaining surfaces included:

- package configuration;
- package settings;
- identity-related labels/settings;
- forms;
- form validation;
- remaining tabs/headings;
- descriptions;
- helper text;
- notifications;
- empty/error states;
- accessibility text;
- remaining source/workspace components;
- any additional hard-coded production strings revealed by the localization inventory.

Do not treat this list as exhaustive.

Use the actual localization inventory and production routes to prove closure.

# ============================================================
# 25. AFTER PHASE 8
# ============================================================

When and only when Phase 8 is genuinely accepted, continue the same Run 2 through every remaining open phase/gate.

Do NOT stop merely because localization is finished.

Continue through:

- voice acceptance;
- Stack Discovery V1 closure;
- Demo Universe V1;
- Automotive Demo / knowledge acceptance;
- complex cross-module acceptance;
- UX/accessibility/responsive acceptance;
- adversarial/security/performance/cost acceptance;
- full regression;
- CI;
- release/deployment gates where permitted;
- post-deployment/runtime verification where permitted;
- final evidence matrix.

If repository evidence shows some of these are already fully accepted, do not redo them unnecessarily.

Verify their evidence and continue with the next genuine gap.

# ============================================================
# 26. FINAL ACCEPTANCE MATRIX
# ============================================================

Before declaring completion, produce an evidence-backed matrix covering at minimum:

- ZERO cognition;
- memory/context;
- model routing;
- agents;
- research;
- knowledge refresh;
- Stack Discovery;
- localization;
- voice;
- Demo Universe;
- Automotive;
- cross-module intelligence;
- permissions;
- tenant isolation;
- idempotency;
- restart recovery;
- stale-response protection;
- provider/source truth;
- Customer Truth protection;
- accessibility;
- responsive behavior;
- security;
- privacy;
- performance;
- reliability;
- cost governance;
- regression;
- CI;
- deployment/runtime evidence where permitted.

Every item must have a truthful state such as:

- PASS
- BLOCKED_EXTERNAL
- FAIL
- UNVERIFIED

No vague “mostly done”.

# ============================================================
# 27. FINAL RUN-2 VERDICT
# ============================================================

The final verdict must be EXACTLY ONE of:

**RUN 2 PASS**

or

**RUN 2 PARTIAL / NOT ACCEPTED**

or

**RUN 2 BLOCKED BY CODE DEFECT**

or

**RUN 2 CODE COMPLETE WITH EXTERNAL ACCEPTANCE BLOCKERS**

Do not invent a different verdict.

Do not declare RUN 2 PASS while any authoritative hard gate remains unverified, failed or materially incomplete.

Do not advance to Run 3 unless the authoritative Run-2 acceptance contract genuinely permits it.

# ============================================================
# 28. WHAT TO DO NOW
# ============================================================

Start immediately.

First:

1. recover repository / PR #21 / CI truth;
2. reconcile it with the known checkpoint above;
3. preserve anything newer;
4. determine the exact first unfinished requirement;
5. continue implementation there.

Do not perform a broad restart.

Do not rebuild Run 1.

Do not redo completed Run-2 modules without evidence of a regression.

Do not stop at the first green test.

Continue until the complete authoritative Run-2 contract has been exercised and a truthful final verdict can be produced.

The goal is not to make the status look complete.

The goal is to make **Foundly Run 2 genuinely hyper-perfect, evidence-backed, production-grade and accepted according to its own contract.**