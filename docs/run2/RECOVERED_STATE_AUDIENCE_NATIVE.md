# Same Run 2: native audience recovery and consent integrity

Frozen source `fb208f83473a797653f8d78d192959b04a44a806`, tree `40bc47e8baa671da6127916d17340ceacfe0ab04`: all 895 ZERO tests, 14 corpus cases and seven affected native commands PASS. Fifteen new tests cover native and ZERO member/filter writes, real lost responses, encrypted restart, delayed original bodies, persistence failure and identity revocation during an authenticated body wait. Published commit `0efbb5e091bf5610459fa76a7bc18206b00cf819`, tree `900ec36e30b9c5ff45584127c6e9cbfb2df5c76c`, has matching complete full CI 226 PASS (run `36167620096`, job `108179098084`). CI merge `d1498a189c3d8ccb50e69f0131d587ec57e2cd24` has the exact same tree. The complete log confirms 895 ZERO tests and 14 corpus cases.

Typed acknowledgements and durable private receipts bind each request to its actor, realm, audience, revisions and native outcome. Member recovery uses the exact retained immutable member record; filter recovery uses a retained snapshot. The original outcome and current result remain separate. An old grant cannot overwrite or be presented as a later opt-out, and an older filter confirmation cannot replace newer filters. Retiring an absent request durably blocks a delayed body. Current capabilities and parent/member ownership are rechecked; storage failure restores all transactional changes.

Portable recovery metadata contains a hash of the entered participant reference, not the reference itself. No email, permission evidence, reason or member body is needed in the future browser recovery queue. Typed source observations include exact pagination, context and current write capability. Legacy receipts do not acquire invented typed recovery evidence.

A reproduced integrity defect allowed a missing latest opt-out record to make earlier consent appear current. Native selection, listing and recovery now reject a missing confirmed member revision. Existing suppression by an inaccessible newer opt-out remains intact. The manifest preserves the original missing-feature failures, the metadata privacy correction, a temporary incomplete reference-field conversion and the missing-opt-out failure. Earlier passing unpublished trees remain explicitly separate from this final source.

Creative review controls have matching complete CI 225. The immediate continuation is the audience UI: metadata-only browser recovery, strict native response binding, eight locales, preserved input and denial/stale-view safety. Journey definition, activation and execution controls and all other genuinely open Run 2 gates remain open. No main merge, deployment, real credentials or Run 3 action.
