# Run 2 implementation checkpoint

This checkpoint extends the existing encrypted Core and current-principal capability resolver. Run 2 remains **IN PROGRESS / NOT ACCEPTED**. The acceptance matrix records missing code separately from missing runtime evidence.

## Voice and cross-module checkpoint

An explicit question naming multiple supported modules now collects authorized native specialist evidence in one ZERO turn, verifies each snapshot before and after model inference, and returns source receipts without executing business actions. Unavailable modules are denied and unavailable models produce a partial result. This does not establish difficult Automotive reasoning quality.

Voice settings define eight locales, two presentation choices and three modes. Existing lawful provider voices are used; perceived gender, naturalness and pronunciation require listening acceptance. Language, voice and mode controls were added within the existing Audio/privacy panel without changing the approved dashboard layout. Browser speech fallback remains distinct from provider voice quality.

The actual Realtime handlers bind calls to a finalized input transcript and a stable audio-item turn identifier. Model-supplied arguments cannot substitute for the user's words. Duplicate call IDs for one utterance dispatch once, an uncertain transport result cannot trigger an automatic duplicate, and a new utterance cancels an unfinished earlier one. Exact approval phrases share the same client/server rules across all eight languages; the existing native confirmation-token checks still apply. Current identity and preference checks run after the asynchronous ephemeral-credential request.

Full local `npm test` passes, including 61 ZERO unit/HTTP tests and the 14-case foundation corpus. CI 147 passed the previous research/discovery checkpoint. No live microphone, listening or browser acceptance is claimed: the supported cloud browser rejected the local preview with `ERR_BLOCKED_BY_CLIENT`. Full UI translation, realistic cognitive evaluations, demo universes and other code-controlled gaps remain open in `ACCEPTANCE_MATRIX.md`.

## Localization implementation in progress

`foundly-locales.js`, `foundly-static-copy.js` and `foundly-i18n.js` introduce explicit eight-locale presentation catalogs. A personal `ui_locale` is stored separately from ZERO's `language`. Login, enrollment, related error handling and native navigation use the catalogs. A late preference response cannot overwrite a newer explicit language selection. The catalog reports a missing key instead of substituting English for an incomplete supported locale.

Static bindings update existing text nodes and accessibility attributes. They preserve nested form controls, canonical option values and the frozen main dashboard's inline CSS. If a native renderer replaces a placeholder with business data, the old static binding cannot translate or overwrite that replacement. No blanket matching or automatic translation of customer records is performed. Seventy reviewed dynamic main-dashboard call sites use explicit catalog keys; state telemetry follows event metadata rather than matching a translated label.

Native numeric/date formatters use the interface locale. Date-only CRM/Finance values retain their calendar date regardless of browser time zone. Missing Automotive/CRM/Analysis numeric data remains unavailable rather than becoming a zero. These checks validate formatting and state contracts, not regional accounting/tax correctness or live language quality.

The seven static surfaces now contain 698 catalog-bound text and accessibility entries: main dashboard 107, workspace 120, CRM 178, Analysis 97, Finance 71, Automotive 116 and login 9. Explicit brand names, acronyms, canonical identifier examples and keyboard shortcuts are listed as invariants. No unresolved static copy remains in this limited inventory. Pipeline creation now uses six localized default names and explicit canonical OPEN/WON/LOST status and probability fields. Renaming a stage cannot change its business status; WON uses 100% and LOST 0%. Add/remove bounds and validation apply before submission. An uncertain request retains its original payload and native idempotency keys for explicit same-page retries. Closing stops subsequent writes until an explicit resubmission, including close/reopen while a call is in flight. This is not an atomic multi-record transaction or durable browser-reload recovery. Generated business-profile stage defaults now use the explicitly requested supported locale; other generated defaults and events remain separate code work. The historical seven-surface structural check is retained; the intentional CRM form/CSS extension is recorded separately. Frozen main inline CSS remains byte-identical to Run 1; browser and visual acceptance remain unverified.

Static and conservative dynamic inventories are recorded in `localization-inventory.json` and `localization-dynamic-inventory.json`. The authoring scripts are separate from the runtime; the dynamic audit uses the local Acorn parser and does not execute scanned application code. It now resolves DOM helper parameters through lexical scopes, including nested form wrappers, and rejects shadowed or reassigned helpers. The expanded audit identifies 940 remaining expression candidates across 46 loaded scripts; the main dashboard accounts for 22 of these conservative candidates. Conditionals, HTML templates, configuration labels, server errors, generated content, safe refresh of active dynamic views and remaining generated/default content need further work. Authenticated HTML now carries the current user’s validated locale before client controls are constructed. The response is not cached, and a bounded streaming transform preserves all unrelated UTF-8 bytes. Anonymous login still chooses from browser languages. All eight full-surface locale gates remain **FAIL / CODE_CONTROLLED**. Browser layout/assistive-technology acceptance and native-speaker review remain unverified.

Sign-in error and validation labels follow subsequent locale changes without another authentication request. ZERO's greeting uses the conversation language independently of the interface locale and keeps the user's name literal. Incremental HTML binding has a regression check for merging existing bindings without corrupting a tag or its adjacent controls.

The localization foundation passed full `npm test` with 69 ZERO tests and the 14-case corpus. CI 149 passed published commit `6cdefb660b7ba48138741a3390f83f67a0b64d96`, tree `17debd712763f0374a9a149a264673be87b97893`. The later seven-surface extension passes full local `npm test`, including 73 ZERO tests and the 14-case corpus; CI 150 passed its published tree `eb0c1fa1a5eedc37fc9b162cde748d47954ea641`. Exact source hashes, structural evidence and log hashes are recorded in `checkpoint-tests.json`. The internet connection was rechecked successfully; supported browser access to localhost still returns `ERR_BLOCKED_BY_CLIENT`.

Standalone CRM now packages and serves every script/style referenced by its HTML, including the shared locale catalogs and shell. Its navigation exposes CRM only after the native read check. Its existing configured actor has a private encrypted interface preference, validated independently of optional ZERO configuration. Tests cover missing-identity rejection, unsupported fields/locales, per-actor restart isolation, package dependencies and HTML language seeding. This service still has one configured actor per process; it does not introduce or claim a new multi-user identity system.

The startup/standalone extension passed 12 locale unit/HTTP tests, the existing static-response failure/recovery/cancellation regression and the expanded standalone CRM regression. CI 151 passed published head `f5718d43afe7d325264dfe7d568b2dc3bfd6f9a7`, tree `36faf22d7e97d5e43dacd27407e30f7453af43e0`. The subsequent pipeline extension passes 20 focused locale/authoring/controller tests and all native CRM regressions. CI 152 passed its published tree `2c1cd8ab04b083e2a88d37ce9d82a36c308cef41`.

Explicit dynamic message descriptors now bind catalog-owned text and accessibility attributes without matching ordinary strings or customer-shaped objects. Parameter snapshots stay private to the binding; native text replacements relinquish ownership. Labels update their text node so nested form inputs, values and handlers survive a language change. No screen reload or automatic business request is required.

The native Finance period-closing component uses these bindings for all visible local copy, known blockers, review state, receipts and guarded errors. Nested labels and dates reformat while customer names, record IDs, exact large cent totals, selected periods, reasons, confirmation and uncertain-request payloads remain intact. Unknown provider errors use a catalog error instead of raw backend text. The existing uncertain-result notice is no longer overwritten by the outer catch. Eight-locale handler tests, 85 ZERO unit/HTTP tests, the 14-case corpus and the native Finance close core/API/client regressions pass. CI 153 passed this extension at published tree `fca67e510830088580a3743b4bbe6b782ec987a0`. This does not establish complete Finance module localization, statutory correctness, provider completeness or browser acceptance.

Native CRM business-profile creation uses a validated `defaults_locale` for the six canonical stage defaults, independently of the existing business `crm_config.locale`. The browser snapshots its interface locale into this presentation choice, keeping business locale metadata unchanged. Regional business locales retain their original values; direct legacy profile requests keep their prior input shape and idempotency signature. `legacy-profile-replay-check.json` verifies this using the actual Run-1 native core and the current core against the same isolated adapter: both the original default-locale and en-US requests replay without changing records or persisting again. The browser snapshots its current interface locale, input and idempotency key once; explicit retry after a lost response reuses that same request even after a UI language change. Inputs remain locked while the result is uncertain, concurrent submissions are ignored, and success clears the form. Existing edited stage names are not renamed by replay. Eighteen focused locale tests and native core/API/standalone regressions pass; the standalone case verifies French names with unchanged OPEN/WON/LOST semantics. This uses the existing CRM persistence/idempotency contract, not a newly atomic multi-record transaction or durable page-reload recovery. CI 154 passed the preceding profile checkpoint at tree `40f76e8e867ddc14005408ce7c2616b55f1b0cf9`; the presentation/business locale separation has its own focused native and HTTP checks. Other generated profile names, event copy and module localization remain code-controlled work.

Cash scenario presentation now uses explicit native recommendation and unavailable-reason codes, while retaining the existing descriptive response fields for compatibility. Known codes select reviewed eight-locale catalog entries. Native scenario validation errors have localized actionable labels, and whole-day horizons are checked at 1–730 before a request. Unknown error codes cannot select inherited object properties. Unknown customer/provider text stays literal. Live language changes preserve scenario assumptions and current result data without fetching or recalculating; a denied reset clears stale controls and results. Sources above the 200-row editor bound expose no partial editable assumptions.

The Finance cent formatter keeps integer and fractional parts exact through BigInt and localized number parts, avoiding loss from floating-point division near the safe-integer boundary. Negative sub-unit amounts, large exact cent strings and missing-value behavior are tested. Native calculation logic and posting behavior are unchanged. Twenty-one focused locale/profile tests and the Finance/CRM native regressions pass. The earlier full ZERO check had 91 passing tests and 14/14 corpus checks; CI 155 passed the final cash/profile compatibility tree `30c2ebde90dc0ca5b7f4f11e419196116a55745d`, including the later business/presentation locale separation and editor-bound check. This checkpoint does not establish full Finance localization, native-speaker, statutory, provider or browser acceptance.

The existing main-dashboard integration cards, action labels, prompts, status counts, guarded transport errors and explicit system-check results now bind to the eight-locale catalog. Provider names, categories, technical IDs, endpoints, credentials, native schemas and provider-owned descriptions remain literal. A locale change repaints owned labels without recreating controls, making a request or replaying the existing worker action. Older refreshes cannot replace newer results. Duplicate TEST clicks are ignored while the request is pending, and cancellation at every profile/creation prompt returns without mutation. Native dedicated OAuth routes and credential payload keys remain unchanged. Missing counts are shown as unknown.

Twenty-three focused localization tests, all 98 ZERO tests, the 14-case foundation corpus and four native connector/permission/OAuth/client regressions pass. The minimal DOM fixture verifies controller state and binding ownership, not rendered layout. Main-dashboard inline CSS remains byte-identical to Run 1; no visual redesign is included. The conservative inventory still has 940 candidates, including 22 on the main dashboard, and excludes additional rich/configuration/server-generated copy. Google module content, other module overlays, generated schema defaults, browser and native-speaker acceptance remain incomplete. CI 156 failed the existing smoke source check because the action helper dynamically constructed canonical data attributes. The follow-up restores literal attribute names; the test and actual native attributes remain unchanged.

Google status/search controls now use owned catalog labels while query text, model answers and citation titles/URLs remain literal. The existing key-presence endpoint is shown as configured, not as a verified live AI connection. Failed status reads show unknown, not a fabricated disconnected state. Search cancellation/duplicate clicks dispatch no extra request, errors preserve native status semantics, and a late status failure cannot replace a newer search result or another panel.

Repeated explicit repaints exposed an ownership bug: old direct-text binding slots could prevent newly created labels from following later locale changes. Explicit `bind`/`renderText` now retire prior text-slot ownership while ordinary DOM replacement still protects customer content. Repeated integration-summary and search-result tests reproduce the previous failure and cover the fix. Twenty-seven focused localization tests and full `npm test` pass, including 102 ZERO tests and 14/14 foundation corpus checks. The unchanged smoke gate also passes the explicit canonical attributes. CI 157 passed this Google/ownership tree `cbdaa997a7d17713a4233eb24ba00b07b69c5095`. No visual layout or native provider contract was redesigned. Browser and native-speaker checks remain unverified.

The active customer spatial dashboard now has explicit eight-locale module/capability labels, navigation/search, accessibility copy, rotation/status text and canvas captions. Locale changes keep native IDs, destinations, canonical graph, world positions, camera state, focus and search input/results. They read no source and do not rerun layout. Only label textures are replaced; geometry buffers remain intact, with rollback if text-texture allocation fails. Canvas maxWidth keeps translated captions inside their existing label shapes without changing the fonts, shape dimensions or positions. Provider brands and server-owned customer activity remain literal. Existing click-time permission revalidation and revoked/failed-source clearing remain in place.

Twenty focused locale tests, all 105 ZERO tests and the 14-case corpus pass. The four existing spatial suites also pass, including 2560 real-resolver profiles, canonical scene geometry, keyboard/manual/pause continuity and actual native HTTP permission checks. Spatial CSS, canonical model and scene layout function remain identical to Run 1. DOM/CPU/texture-allocation checks do not establish WebGL pixel output, text fitting, responsive layout, accessibility quality or native-speaker acceptance. CI 158 passed the exact final spatial tree `e1f11e1be0e78cdcc5079f2a35c0efa256d307de`, including caption bounds; all full-surface locale gates remain FAIL / CODE_CONTROLLED.

ZERO status labels, owned live details, conditional wake/conversation notices and result/action/source framing now follow all eight interface locales. Canonical state codes, telemetry, audio/recognition state, native action fields, source URLs and conversation language remain unchanged. Ordinary answer text, source labels and blocked reasons are literal. Direct text-node bindings keep nested source links intact and retain customer replacement protection. A locale change during a pending native turn sends no additional request, and a lost module response shows an uncertain result without raw error text or synthetic speech.

Result and action headings no longer claim that every response or blocked action was verified/executed. Provider verification is per source and requires boolean `true`; missing sync counts are unknown, while a real zero remains zero. Twenty-four focused locale tests pass, including seven new controller/ownership cases. Full local `npm test` passed with 112 ZERO tests and 14/14 corpus cases before a final repeated-result fix. That fix clears every old result row before new progress/error text; its failing reproduction, all 112 ZERO tests, the corpus and native client/audio checks now pass. CI 159 passed the exact final ZERO presentation tree `433e49c463a95ec3cee58ef0025b059d6b5c6a91`. Main inline CSS is unchanged from Run 1. The conservative inventory contains 937 candidates (19 in index-script); backend/generated copy, other surfaces, visual fitting, native-speaker and live voice acceptance remain open. All eight full-surface gates remain FAIL / CODE_CONTROLLED.

Audio & Privacy now uses live eight-locale status labels, native percentage formatting and translated save/error notices. Unknown readings remain unknown; explicit zero and disabled states remain distinct. Transport/privacy and transcription framing also follows locale changes while actual utterances and native voice turns stay literal. Older permission reads cannot replace newer observations, and browser permission-change notifications repaint without requesting the microphone again.

A reproduced settings bug is fixed: moving a volume control or toggling the microphone no longer promotes an unsaved language, voice profile or preferred name. Explicit saving guards duplicate requests, freezes the native form/payload, preserves prior disabled controls and distinguishes an unconfirmed save from a confirmed save whose audio activation fails. The existing native routes, canonical values and microphone acquisition/release behavior remain intact. These are controller/device-fixture contracts, not real voice or browser evidence.

The full ZERO run exposed a separate test-startup defect: random port collisions could accept another process's public health response, then fail native authentication. A controlled reproduction confirmed it. The fixture now uses OS-assigned ports and observes its own child server's listening event over test-only IPC, with no replacement of production HTTP/authentication/persistence. Parallel ownership, wrong-token rejection, occupied-port failure and encrypted restart are checked. All 121 ZERO tests, 14/14 corpus cases, 32 focused locale checks and the existing native client/audio/ZERO contract regressions pass. CI 160 passed the exact audio/fixture tree `9c1cf3e7863de0f037313f1ac7660c29d4b99000`. The main inline CSS remains frozen; the conservative audit still has 936 candidates, and all full-surface locale gates remain FAIL / CODE_CONTROLLED.

Dashboard system observations, summary counts and activity timestamps now follow all eight interface locales without extra reads or visual-event replay. Missing/invalid counts remain unknown, real zero is retained, and the cached connector aggregate no longer invents zero or 100 after a missing or failed read. Literal provider names and persisted event messages remain unchanged. Date/time formatting respects the account timezone; invalid values or zones remain unknown.

A newer failed or denied read clears old source labels, counts and freshness. Older responses and replies for replaced nodes cannot restore them. Failed activity reads also clear the event cursor, so restoration does not replay historical entries as new visual activity. Twenty-four focused locale checks, all 128 ZERO tests, the 14/14 foundation corpus and existing smoke/client/audio regressions pass. CI 161 passed the exact dashboard tree `56c1465ee63e0f993db180080250e5ebdf3e67b7`. No main-dashboard CSS or layout was redesigned. The conservative inventory still contains 933 candidates (15 in index-script); other overlays, generated/backend copy and real browser/native-speaker acceptance remain open, with all full-surface gates FAIL / CODE_CONTROLLED.

A further exact-source check found that parameterised direct text fragments could duplicate leading/trailing source whitespace on each locale change. Explicit fragments now own their complete text node; static HTML retains its separate padding rule. Deliberate new text layouts also retire prior parameters, preventing an old count from silently filling a new label. The failing reproduction, 50 focused locale checks, all 129 ZERO tests and 14/14 corpus cases document the fix. Module-overlay localization remains in progress; this source-ownership fix does not establish full-surface acceptance.

## Implemented and connected

- `zero/memory.js`: eight memory layers; explicit user provenance; private ownership; current role/capability filtering; effective dates, expiry, supersession, conflicts, revision checks and deletion of version-chain content. Atomic writes reuse `scoped-mutation.js`.
- `zero/context.js`: bounded question-driven lexical context assembly with source states, provenance, conflict flags and explicit unavailable/denied states. Semantic retrieval is not claimed.
- `zero/router.js`: versioned server-controlled registry; Responses, Chat and Anthropic transports; task/privacy eligibility; bounded fallback, timeout, cancellation and concurrency; persistent daily/flow request/token budgets; optional monetary budget requiring explicit model prices; usage/latency telemetry. Provider tests are contract evidence, not real account acceptance.
- ZERO's real conversation path uses assembled memory/context and rechecks current identity and source content after inference. Generated source answers are not preserved as reusable private facts in conversation/audit replay. Deleted memory cannot return through a cached answer.
- Personal preferences and client diagnostics use per-user storage. Legacy preferences migrate only for the bootstrap operator. Eight locale identifiers and male/female, Executive/Conversational/Briefing settings are validated. Voice quality and complete UI localization are not established by these settings.
- `zero/stack.js`: read-only current composition/permission observations and explicitly confirmed private customer declarations. Conflicting source-of-truth/schema facts require clarification. Expired credentials and unobserved schemas are reported honestly. It does not claim remote provider discovery where no authorized observations exist.

- `zero/agents.js` and `zero/native-agents.js`: bounded dependency-checked plans over eight registered native read specialists, current capability checks, separate server source re-reads, per-tenant concurrency, cancellation, context/tool/deadline limits, private source-free receipts, restart interruption and idempotent replay. Write actions remain in the existing typed native approval contracts. This is not proof of cognitive multi-agent quality.
- `zero/intent.js`: conservative natural-language mutation gate. Questions, hypotheticals, quotations and negations cannot trigger legacy write detection; implicit provider ingestion is removed from analysis requests. Real HTTP tests distinguish discussion from a requested task creation.

## API

All routes use the existing authentication, tenant context, origin checks and live identity. `/api/jarvis` remains an alias of `/api/zero`.

| Route | Method | Behavior |
| --- | --- | --- |
| `/api/zero/agents` | GET | Current registered native read specialists and limits |
| `/api/zero/plans` | GET / POST | Own plan receipts / create a bounded dependency plan |
| `/api/zero/plans/:id` | GET | Read own plan state; interrupted processes never silently rerun |
| `/api/zero/plans/:id/run` | POST | Read and independently verify current native evidence |
| `/api/zero/plans/:id/cancel` | POST | Cancel an owned pending or active plan |
| `/api/zero/memories` | GET / POST | Retrieve current authorized memory / explicitly record an assertion |
| `/api/zero/memories/:id` | DELETE | Revision-bound removal, including content in superseded versions |
| `/api/zero/models` | GET | Credential-free registry state and caller-scoped usage |
| `/api/zero/stack` | GET | Current evidence, unknowns, blocked facts and conflicts |
| `/api/zero/stack/evidence` | POST | Confirm a customer declaration with source and reason; connector-management permission required |

## Model configuration

Existing `FOUNDLY_AI_MODEL`, `FOUNDLY_AI_BASE_URL`, `FOUNDLY_AI_API_KEY` and `OPENAI_API_KEY` remain supported. `FOUNDLY_ZERO_MODEL_REGISTRY` optionally supplies a JSON array of model entries. Each entry specifies `id`, `provider`, `model`, HTTPS `base_url`, server-only `credential_env`, supported `protocols`, `tasks`, `privacy`, quality/priority, input/output limits and timeout. Entries can include input/output USD per million tokens. No live model price is guessed.

Budget controls: `FOUNDLY_ZERO_DAILY_REQUESTS`, `FOUNDLY_ZERO_DAILY_TOKENS`, `FOUNDLY_ZERO_WORKFLOW_REQUESTS`, `FOUNDLY_ZERO_CONCURRENCY`, `FOUNDLY_ZERO_MAX_MODEL_ATTEMPTS`, optional `FOUNDLY_ZERO_DAILY_USD`. Credentials are not returned or logged. Unknown provider usage keeps the conservative reservation charged. A configuration rollback uses the prior reviewed deployment configuration; provider availability remains separately unverified.

Provider schema references checked on 2026-09-24: [OpenAI Responses](https://developers.openai.com/api/reference/cli/resources/responses/methods/create), [Claude Messages](https://platform.claude.com/docs/en/build-with-claude/working-with-messages). The transport adapters isolate protocol shape from tenant policy and orchestration.

## Evidence and limits

`zero-evaluation/run.js` records real HTTP scenarios using isolated encrypted storage and real identity sessions. Its no-provider cases call no AI service. `zero/*.test.js` covers failure and policy contracts; the context API fixture deliberately copies a private string to test subsequent retention/deletion/revocation. It does not grade cognition. Existing provider fixtures include deterministic public DNS for their mocked OpenAI transport; production URL validation is unchanged.

The first corpus moved from 4/14 to 14/14 passing assertions. This is a narrow foundation checkpoint, **not Run 2 PASS**. The authoritative 125-row matrix remains open where broader evidence is missing. Broader specialist cognition/execution acceptance, governed research/activation, deeper heterogeneous discovery, universal search, complete localization, real voice, demo universes, Automotive knowledge, cognitive evaluation and full acceptance continue within this run.

The first full `npm test` exposed Calendar fixture DNS, repaired without changing production URL checks. Workflow’s local HTTP fixture now has an explicit test-only HTTPS transport adapter. The next full suite passed; later agent/intent changes passed their unit and real HTTP checks plus existing ZERO/Calendar/Workflow regressions. See `checkpoint-tests.json` for exact scope and log hashes. Full Run 2 acceptance remains open.

## Research, knowledge and heterogeneous discovery

- Current web research now runs through the same model/router request, token, concurrency, fallback and timeout policy. The server permits one built-in search call per provider response, rejects research without source evidence, and records tool usage separately. Configured monetary budgets require explicit search pricing as well as model prices. Provider-reported actual use remains distinguishable from conservative unknown-use reservations; provider billing may exceed a reservation, so this is not yet a verified hard spend ceiling.
- Search results are ephemeral external references. They are no longer automatically inserted into shared customer Data records. Retrieval dates are known; publication/effective dates and jurisdiction remain unknown unless reviewed. Citations are attribution, not verification of claims.
- `/api/zero/knowledge` and `/:id/transition` implement private reference quarantine, metadata/freshness evaluation, exact-content operator review, explicit activation, version supersession, retirement and rollback. Regulation references require authority, jurisdiction, primary-source classification and effective date. These are operator attestations, not automatic legal authority. Only active, effective, current references enter ZERO context; retirement/expiry/revocation is checked again after inference and on replay.
- `/api/zero/stack/discover` reads only server-configured OpenAPI JSON targets from `FOUNDLY_ZERO_DISCOVERY_TARGETS`. Each target has `id`, `module`, `schema_url`, optional server-only `credential_env` and `ttl_seconds`. HTTPS/public-network validation, redirect refusal, current module/connector rights, 512 KiB input cap, eight-second deadline, tenant concurrency and a durable daily request ceiling bound discovery. OpenAPI 3.x and Swagger 2.0 schemas, documented operations, security schemes and webhook names retain a document hash and source URL. This proves the observed document only. Runtime behavior, identities, remote schema references and source-of-truth remain unknown. Expired credentials, missing documentation, stale metadata and outages are explicit.

Provider references: [Responses tool-call ceiling](https://developers.openai.com/api/reference/cli/resources/responses/methods/create), [Web search sources](https://developers.openai.com/api/docs/guides/tools-web-search), checked 2026-09-24.

CI run 146 on the first draft commit found a Workflow-inspection regression introduced by the natural mutation gate. The gate now preserves the explicit read-only `INSPECT_RUN` path. The original failing HTTP test, workflow language/generator checks, 51 ZERO tests and a subsequent complete `npm test` passed locally. CI 147 passed the replacement research/discovery tree; the earlier failed run is not relabeled PASS.


### Module overlay continuation

The recovered Run-2 checkpoint is recorded in `RECOVERED_STATE_20260924_1652.md`. Module overlays now use explicit eight-locale DOM bindings for headers, 149 distinct capability labels, controls and observed source states. Unknown observations remain unknown; native zero remains zero. Failed/denied reads clear previous source evidence, and late responses cannot repaint closed/replaced views. Locale changes preserve answers and native routes without new reads or actions. Google/integration button presence and handlers are tested through the actual controller DOM in the existing smoke suite. Browser/local-layout and native-speaker acceptance remain unverified; full-surface localization is still FAIL/CODE_CONTROLLED.

OAuth callback navigation flags are now explicitly untrusted evidence: source connection confirmation requires a fresh native successful observation; URL bootstrap values cannot become ingestion claims. Duplicate callback panel opening was removed. Remaining fallback graphics/legend labels and Google AI-search framing update through eight-locale bindings with unchanged scene/model/CSS. Targeted plus 141 ZERO tests and 14 foundation corpus cases pass; this later source tree awaits independent full CI.

Analysis action proposals now have explicit eight-locale control/validation/status bindings. Locale changes preserve authored report/mail text, reviewer choices, separate confirmation and exact retry payloads. A reproduced denied-refresh leak is closed: stale private detail views are cleared with the failed list, and late reads cannot restore them. Five new UI-state tests and the unchanged native/ ZERO execution contracts cover this boundary. Full-surface localization and broader Run-2 gates remain open; the conservative direct-copy inventory now lists 884 candidates.


### Continuation: Analysis models and revision history, 24 September 2026

The action-control checkpoint is published as `121594160f59b8934edc3c02c3678a77aaf95d82`, tree `7b306cbc49ebc929657c1742586fbdbf06a0e487`. Full CI 164 (`36035724562`) completed successfully.

Model fields, known measurement options, reports, source drilldown and shared definition revision/restore now use explicit live bindings for all eight locales. Authored values, negative minor-unit amounts, source names, native units/payloads and snapshots keep their meaning. Unknown values never become zero; locale changes do not request data or mutate definitions. UTC audit observations identify their time zone. Native retry keys, confirmation and unsaved-change guards remain in place.

A reproduced controller defect allowed an old query to reappear after navigating to another model and back. Selection and new-model entry now invalidate old calculations. Six scoped controller checks, all 152 ZERO tests, 14/14 foundation corpus checks and six existing native core/controller/HTTP model/history suites pass. These are bounded checks, not full localization or browser/native-speaker acceptance. The scoped direct-copy audit has zero candidates for the two changed controllers; 844 candidates remain overall. Cohort reports and the surrounding Analysis page are the next open localization gap. No production mutation, main merge or Run-3 work occurred.


### Continuation: Analysis page observations, 24 September 2026

Full CI 165 (`36037141183`) passed model/history commit `d3f8784fbe84fa6c0444ec5385f7ba943c20da6e`, tree `7ded5c8de2dfda2813a3937f3b6f0911995bea40`. The subsequent Analysis page checkpoint adds explicit bindings for fixed labels, states, counts, timestamps, canonical KPI names and stages. Source records, custom stage names, filters, ZERO questions/answers and exported bytes stay literal; no locale change performs I/O. Missing lists/counts remain unknown, and provider verification requires boolean authentication/probe/sync evidence. The page no longer assigns EUR to native minor-unit values without a supplied currency.

A reproduced denial defect retained private events and historical rows after failed current access. The corrected page clears old data and cached source snapshots, ignores late reads/replies/downloads and closed event streams, and handles optional denied/unavailable components independently. Ten new page tests, all 162 ZERO tests, 14 foundation corpus checks, native Analysis loader/core HTTP tests, smoke and masterbuild regressions pass. Main-dashboard CSS/geometry and production remain untouched. Cohort dynamic copy is the next open Analysis localization gap. Full-surface localization and broader Run-2 acceptance remain incomplete; no matrix status has been waived.


### Continuation: Cohort localization and CI 166 repair, 24 September 2026

CI 166 (`36039205487`, job `107766843393`) failed Analysis-page commit `f7e03a257004ee84fff0e6916057b82e2e83c3c2` at an obsolete source-literal assertion. The regression now exercises the actual renderer with unavailable/malformed observations and literal external text, then passes actual empty native KPI data through the renderer. Native routing, CSP, data and permission assertions remain. The full local `npm test` command subsequently passed, including all 168 ZERO tests and 14/14 foundation corpus checks. This does not turn the failed historical CI run into a pass; the repaired head needs its own CI evidence.

Cohort controls now localize explicit labels, saved-definition notices, table headings, counts, percentages, UTC dates and evidence tooltips in all eight locales. Source IDs, literal events, selected native revisions and user-authored definitions are preserved. Locale changes neither query nor save. Pending saves are guarded; uncertain-result retries keep the same payload and native idempotency key.

A separate reproduced access defect left private source-loaded acquisition/return fields visible after cohort access was revoked. Revocation and denied definition reads now clear those fields, history, titles and evidence; late responses cannot restore them. Six new cohort checks and the existing cohort computation/definition/native HTTP/ZERO suites pass. Scoped dynamic-copy candidates are zero for the changed Analysis presentation controllers, with one internal loader exception and 818 candidates overall. Full-surface localization remains incomplete. Automotive presentation is the next open surface; no implementation there has yet changed. Main remains unchanged, production was not mutated, and Run 3 has not started.


### Continuation: Automotive status truth and locale bindings, 24 September 2026

CI 167 (`36041048581`) passed the repaired cohort checkpoint `6afe2d58688fc0b6cb244a231a7ddaf290cc7ba6`, tree `040132e639d0b56143b4e64d45236aacf472a1c4`. Historical CI 166 remains failed; the replacement regression was verified in the subsequent complete CI run.

Automotive status, provider observations and overview labels now use explicit bindings for all eight locales, locale-aware counts and UTC timestamps. Actual source identity and authored queries stay literal. Missing collections/scores and conflicting provider observations remain unknown. Configured credentials no longer imply marketplace readiness; a successful observed search is distinguished from configuration and current registry evidence. No synthetic inventory or source evidence is generated.

Two before-fix controller checks reproduced false marketplace readiness and retained private dealer/inventory data after access denial. Corrected status failure handling clears those observations, result/detail panels and dependent answers. Late status, search, enrichment, detail, opportunity and ZERO replies cannot repopulate views after current access is denied; closing a pending detail prevents reopening. Eight controller checks, all 176 ZERO tests, 14/14 foundation corpus cases and the existing Automotive core/HTTP/masterbuild suites pass. Native provider transports are deterministic fixtures, not live acceptance.

The conservative direct-copy inventory lists 814 candidates, including 16 in the Automotive controller. Search/results/detail presentation remains the next open gap. Full localization, browser/layout, accessibility, voice, cognitive, demo and broader Run-2 gates remain open. Main, production and the frozen dashboard geometry are unchanged; Run 3 has not started.


### Continuation: Automotive search and opportunities, 24 September 2026

The full CI 168 test job (`107780829745`, run `36043387312`) passed Automotive status head `7bea53370d118eb9fa557db8998b9274b53e3f79`, tree `e8eb7265718837ac9f8458cd9df789c27c300ca4`.

Search criteria, vehicle cards, opportunity ranking and ZERO request framing now have live eight-locale bindings, including known native enum labels, numbers, currency and loading accessibility labels. Business identity, native payloads, authored queries, provider IDs and model answers remain literal. Unknown source states do not become verified; unavailable card scores do not become numbers. Native zero remains zero, and locale changes do not issue requests or replace cards.

Before-fix tests reproduced denied search retaining prior criteria/detail data and missing result arrays becoming zero. Both are repaired. Denials during search, enrichment, opportunities or details clear previous observations and invalidate pending callbacks. Pending searches and ZERO turns reject duplicates. Seven new controller tests, the combined 15 Automotive tests, all 183 ZERO tests, 14/14 foundation corpus cases and existing Automotive HTTP/masterbuild regressions pass. The Swedish unknown-state assertion was corrected to the catalog’s grammatical state form; no acceptance check was removed.

The conservative audit now lists 801 candidates, including three direct Automotive expressions; remaining detail template copy is not fully captured by this audit. Vehicle detail rendering is the next open gap. Full localization and the wider Run-2 exit contract remain incomplete. No production mutation, main merge or Run-3 work occurred.


### Continuation: Automotive detail evidence, 24 September 2026

CI 169 (`36044567009`) passed search head `5cdac9e44cb2a3b7526712a9246a2c0f13ca3999`, tree `71d8b550c92fb5881a0186184ae71d58a7ccc20f`.

The six vehicle-detail tabs and header now use explicit eight-locale DOM bindings for labels, known evidence enums, score components, costs, missing fields, risks, safe source links, numeric values and UTC observations. Source IDs, names, references, custom fields and rule versions remain literal. Selecting a new detail clears prior displayed data, and an older pending selection cannot replace the newer one.

Before-fix tests reproduced an unavailable numeric Buy Score still displayed as available and a string false displayed as affirmative provider verification. Corrected renderers require typed evidence, retain actual zero/negative values and distinguish absent collections from observed empty lists. Six new tests exercise all six tabs in all eight locales, literal markup, unsafe links, missing/boolean/nonfinite values, source isolation and stale detail reads.

A broader test initially failed because it extracted removed standalone formatter functions. It now executes the actual vehicle-card renderer with the same calendar/missing-value/zero/decimal acceptance, plus stricter malformed-value cases. The native HTTP regression replaces a helper-name assertion with rendering real native analysis and checking inert source text, unsafe URL omission and strict verification flags. All 189 ZERO tests, 14/14 foundation corpus cases, 21 combined Automotive controller checks and native core/HTTP/masterbuild regressions pass. The original failed run is retained in the evidence manifest.

The conservative audit lists 800 candidates overall. Two Automotive candidates are internal malformed-response exceptions classified into localized errors; backend narrative/native-speaker/layout acceptance remains separate and open. The Finance page is the next direct presentation gap. No acceptance requirement is waived, no production mutation/main merge occurred, and Run 3 has not started.


### Continuation: Finance source and current-access boundaries, 24 September 2026

CI 170 (`36045888013`) passed Automotive detail head `a7e1c88584f0ae88d5924aa6ae28a6ffdf6b331b`, tree `207e096de95f87d1e1a9518135168284fc28efaa`.

Before continuing Finance page localization, two reproduced source defects are fixed: denied refresh retaining private account/journal/editor observations, and absent aging buckets being treated as no overdue balance. Finance now clears cached/displayed source data on current access loss and rejects late ZERO answers, reads and CSV downloads. Independently authorized entity observations survive scoped report denial. Pending export/ZERO requests reject duplicates. Source-backed period controls use the existing ledger/invoice/payment capabilities; an explicit refresh checks current access even when a draft or confirmation prevents ordinary navigation. Unchanged access preserves the pending confirmation and exact retry contract.

Finance display preserves cents rather than rounding source amounts to whole euros. Strict typed observations distinguish absent lists, malformed/nonfinite values, genuine zero and boolean availability/durability. Native routes, legal-entity/date filters, CSV bytes, backend permissions and accounting mutations are unchanged. Nine page tests and all 198 ZERO tests plus 14/14 foundation corpus cases pass. Seven targeted loading/controller/HTTP/workspace regressions passed the source-guard change; final missing-journal/P&L-list presentation is additionally covered in the full ZERO run and awaits the new published CI. Structural tests calling their result accessible/responsive are not treated as browser or assistive-technology evidence.

Finance owned labels and the wider localization scope remain the next open gap. The conservative audit lists 806 candidates, including new internal exceptions; this is not a full-surface localization pass. No production mutation, main merge or Run-3 work occurred.


### Continuation: Finance page locale bindings, 24 September 2026

Finance source head `b9815a7c1f0b9e8b6261555dabe699fef22dea7d`, tree `a8eeeadba055dad432c745e1152b36b1bcf0d41b`, has CI 171 (`36047055303`) still running at this checkpoint. CI 170 remains the latest completed successful broad run.

Finance page entity options, KPIs, posted-source labels, P&L, aging, budget/forecast, compliance, journal states and request feedback now use explicit live bindings in all eight locales. Exact native cents, source currency and UTC financial observations retain their meaning. Native source text and IDs, filters, authored questions, editor identity, confirmation state and CSV bytes remain unchanged. Locale changes issue no requests. Unknowns remain distinct from observed zero and empty lists; malformed export content does not create a file. Access invalidation also clears the prior API-reachable presentation.

Four additional locale/ownership tests bring the Finance controller checks to 13; all 202 ZERO tests and 14/14 foundation corpus cases pass. Seven targeted workspace/loading/scenario/closing controller and real local HTTP suites pass. The obsolete escape-helper-name regression now checks actual literal DOM account text and exact/unknown amounts, preserving the original safety requirement. Catalog aliases and expected localized empty-state labels were verified before this checkpoint; no test is skipped.

The conservative audit lists 796 candidates overall. Finance has six internal exceptions classified into localized errors and one no-runtime fallback; other surfaces and generated/backend copy remain open. The next direct presentation gap is CRM page rendering. Full-surface locale, browser, accessibility, native-speaker, voice and wider Run-2 gates remain unaccepted. Main and production are unchanged; Run 3 has not started.


### Continuation: CRM numeric source truth, 24 September 2026

CI 171 passed Finance source head `b9815a7c1f0b9e8b6261555dabe699fef22dea7d`. Finance locale head `3aa5ab9c81205a9418a749ecb2f08850bb02034c`, tree `f471c663654068ea51981602d02b120b08c1f738`, has CI 172 (`36048311519`) still running at this checkpoint. An interrupted read-only git fetch was retried successfully; local and published histories are preserved and merged without reset or force update.

Three before-fix tests reproduced truthy/string/boolean metric coercion, invented EUR currency for a metric without currency, and native historical null ratios being compared as zero. The formatter now requires typed observations, preserves explicit currency and exact two-decimal source presentation, and formats percentage/duration units in all eight locales. Missing currency is stated explicitly instead of assigning a currency.

Native period comparison retains null when either ratio value is unavailable and rejects nonfinite differences. Legitimate count changes from observed zero remain intact. Native observed zero ratios still compare to zero, and percentage change from zero remains undefined. Five new permanent tests, all 207 ZERO tests, 14/14 foundation corpus cases and existing CRM core/API/standalone regressions pass.

This is a bounded source-truth correction before CRM dashboard localization. Remaining dashboard/chart currency and source-state presentation, other views, generated/backend copy and broader Run-2 gates remain open. The conservative inventory still lists 796 candidates. No acceptance criterion is waived, no production mutation/main merge occurred, and Run 3 has not started.


### Continuation: CRM dashboard presentation and stale-source guards, 24 September 2026

CI 172 and 173 passed their published Finance locale and CRM observation heads. Latest prior source head `d5c9acfe9dace9803fc1d839e5183a0c06e49a77` has tree `235e8b6a7899048ba94623ab3fd4c85efd6592ee`; both local and published histories remain preserved.

Two failing-before tests reproduced retained private dashboard rows after denial and late summary restoration. Dashboard/status/save generations now clear revoked observations and reject late replies; pending saves reject duplicates and detached old editing controls cannot mutate a newer layout. Native routes, layout/filter payloads, source IDs and business text are unchanged.

Dashboard metrics, tables, charts, tooltips, editing labels, comparison units and request feedback use explicit live eight-locale bindings. Locale changes preserve cards, authored inputs and layout without I/O. Missing lists/counts/amounts remain unknown, source currency is never invented, and observed zero draws no positive bar. Source charts use one measurement field, preserving zero rather than substituting lead counts; mixed currencies and malformed/negative values use explicit tables. Percentage-point differences and percentage changes remain distinct.

Thirteen new checks, all 220 ZERO tests, 14/14 foundation corpus cases and targeted native CRM regressions pass. Final comparison-unit labels were added after the native regression run and are covered by the final full ZERO run. This fixture executes the production controller with bootstrap deferred; it does not establish browser pixels, responsive layout, assistive-technology or native-speaker acceptance. The conservative inventory lists 789 candidates. No full locale gate is promoted.

Review also found the next code-controlled defect: native `saveDashboard` changes existing defaults before validating the target's ownership/revision. Atomic rejection/replay and client save retry recovery need direct tests and repair before proceeding to the other CRM views. Existing native success regressions do not accept that gap. No production mutation, main merge, credential change or Run-3 work occurred.


### Continuation: CRM dashboard atomic save and exact retry, 24 September 2026

CI 174 passed source head `60fea937deca0ea1aea1da1f77341fa68f505561`, tree `325bcc7f083e83a6cacd04f8b77c2e0622419f5c`. The first new fixture incorrectly used empty required widgets and was repaired to native valid input before claiming defect evidence. Six failing-before tests then reproduced rejected writes changing defaults, replay demoting a newer default, persistence failure leaving in-memory changes, split persistence, cached private receipt exposure after ownership change and malformed default flags. Original logs remain recorded.

Native dashboard save now validates before demotion, rechecks current write/ownership permissions for replay, and commits the layout, default selection, audit and receipt together through the existing synchronous transaction helper. Failure restores the touched buckets; notification emission follows commit. Existing generic create/update receipt signatures replay unchanged, preserving earlier Run-1/Run-2 receipts. Replays make no new audit, default-selection change or persistence call. Real HTTP tests confirm stale-revision rejection and unchanged defaults/audit after replay and encrypted restart.

The client freezes the original body, revision and idempotency key until a save is confirmed or definitively rejected. Pending retries survive all eight interface locales without I/O or payload edits. Controls remain locked during uncertainty, explicit refresh checks current access, denied access clears the frozen private request, and a confirmed write is distinguished from a later status-read failure. A malformed success response remains unconfirmed and uses the original request on retry.

Thirteen additional tests bring the complete ZERO run to 233 passing tests and 14/14 foundation corpus cases. Existing native CRM core/API/standalone regressions pass. The conservative locale inventory lists 790 candidates, including the new internally classified malformed-response exception. Other CRM views and the wider Run-2 requirements remain open; this bounded fix does not establish all CRM mutation atomicity, cross-module event delivery or full localization. No main/production change or Run-3 work occurred.


### Continuation: CRM source-view lifecycle, 24 September 2026

CI 175 passed native save/retry head `7966067b189912ba54bef1b5cd88e40ed5908750`, tree `60b5d4ca31461463f2b5a50be7ae700bb22dea3d`. Ten before-fix tests reproduced retained private record/pipeline/customer/automation data after denial, late restoration for those views, reopening a closed pending customer dialog and late ZERO answer/navigation after denial.

Central CRM denial now invalidates private source caches, mounted source editors and dialogs. Per-view generations preserve current entity/pipeline selections and prevent late replies from restoring older observations. Missing response lists stay unavailable instead of implying observed zero. ZERO guards duplicate submissions and stale navigation, retains failed/newer authored questions and uses localized progress/classified error feedback while leaving model answers literal. Record forms retain their original entity across surrounding navigation, and automation saving captures its form before the browser clears event.currentTarget.

Seventeen new tests, all 250 ZERO tests, 14/14 foundation corpus cases and native CRM core/API/standalone regressions pass. The conservative copy inventory lists 788 candidates. Controller bootstrap is deferred in the fixture; no browser pixels, native-speaker, accessibility or live-provider acceptance is claimed.

Remaining CRM pipeline/record/customer/schema/automation rendering still needs locale conversion. Review also identifies the next mutation paths requiring direct evidence: generic cached mutation replay authorizes inside the skipped operation, and generic archive does not validate expectedRevision. These are code-controlled work, alongside exact client retry for remaining actions. The dashboard-specific save correction does not accept every CRM mutation. Wider Run-2 gates remain open, and no main/production change or Run-3 work occurred.


### Continuation: CRM mutation permission, revision and persistence integrity, 24 September 2026

CI 176 passed source lifecycle head `886b8dcb2443ae626c1f0b7706a00a16527253f6`, tree `1505d691de5579348f6be989c72ddfd10fa10056`. Nine failing-before tests reproduce stale cached write authority/ownership, archived cached source disclosure, missing archive revision checks, persistence rollback/commit-count failures, revision-unbound new archive receipts, capacity insertion before rejection and an introduced dashboard regression dropping explicit permissions when roles were empty. A separate real HTTP test proves malformed If-Match headers were ignored and allowed a write.

Generic create/update/archive now checks current authority before replay and commits records, audit and receipts together with the existing synchronous adapter helper. Failed persistence restores touched buckets before returning; notifications follow a successful commit. Capacity rejects before insertion. Archive checks expectedRevision, binds new receipts to it and preserves historical delete signatures. Main and standalone HTTP adapters share strict revision-header parsing; malformed/multiple/unbalanced/unsafe values do not become unconditional writes. The dashboard regression is corrected by passing the original principal to the read check.

Real HTTP evidence configures the actual tenant package, creates a member session, checks stale and malformed revisions, changes the member’s role, obtains a fresh session and verifies replay denial before and after encrypted restart. The initial missing-composition fixture error is preserved as setup failure, not product-defect evidence.

Ten new tests bring the complete ZERO run to 260 passing tests and 14/14 foundation corpus cases. Native CRM core/API/standalone regressions pass, including standalone malformed-header checks. No full security or locale gate is promoted. Compound provisioning, stage-change automation effects and durable cross-module notification delivery remain outside this bounded record-transaction proof. The next direct work is exact client retry for remaining record/archive/stage/automation actions, followed by their remaining locale rendering. Main and production are unchanged; Run 3 has not started.


### Continuation: CRM record and automation form retry, 24 September 2026

CI 177 passed mutation-integrity head `e3083aad31418b3622f20439f3eb5a38065b9b4e`, tree `31e31437968040cc43c01953e2d8d67647465959`. Four failing-before tests reproduce record/automation requests changing key and payload after a lost committed response, and overlapping submits creating multiple native writes.

The actual form controllers now freeze the original path/body/revision/key and disable fields while a write is unconfirmed. Retrying uses that exact request across all eight locales without rereading fields. Record close/reopen attempts preserve the original pending editor; confirmed writes clear it. Definitive validation failures permit correction under a new key. Denial clears pending private requests, while late replies cannot restore source state or reset a later draft. Owned persistent feedback and generated automation follow-up framing localize explicitly; business names and canonical payload values remain literal.

Seven new tests use production controllers and real native core writes in a DOM fixture. All 267 ZERO tests, 14/14 foundation corpus cases and native CRM regressions pass. The fixture defers browser bootstrap and is not rendered-browser/accessibility/native-speaker acceptance. The conservative copy inventory lists 787 candidates. Archive/stage-move exact retry, compound stage automation recovery and remaining CRM rendering are still open. No acceptance gate is waived or promoted, main and production remain unchanged, and Run 3 has not started.


### Continuation: CRM compound stage and automation persistence, 24 September 2026

CI 178 passed form-retry head `6e12d21f0991a24192a713014d2f772378c668b5`, tree `537ffdaaaaf351ec60918332eafec986b997219b`, including the complete npm regression. Nine failing-before checks then reproduced stage writes before permission denial, unstable replay, partial persistence/effects, lost empty match sets, event payload substitution, silent match truncation and dropped explicit grants. Two additional failures covered authorization order and planned actions incorrectly reported completed. The native replay fixture omitted the original before payload; it now replays the exact original event, retaining its deduplication assertion and original failure log.

Stage changes and their internal task, execution, audit and receipt effects now stage within one tenant and commit through the existing synchronous encrypted adapter transaction. Failed persistence restores all touched buckets; notifications follow commit. Current automation/read/write authority is checked before the deal changes. Stable stage request signatures derive a stable event ID when only an idempotency key is supplied. Main and standalone API replay, role revocation and encrypted restart are covered.

A new auxiliary `crm:automation_event_receipts` bucket records the observed match set, including zero matches, and binds event IDs to their original payload. Replay first checks current receipt/definition/task access. Over-limit matches reject before effects; conflicting event IDs roll back the deal update. Historical receipts remain compatible without inventing completion evidence: stage receipts lacking an atomic automation outcome return `LEGACY_OUTCOME_UNVERIFIED`, and historical event executions lacking payload binding retain `LEGACY_UNVERIFIED`. Client and workflow results expose the review requirement.

Fifteen new tests, all 282 ZERO tests, 14/14 foundation corpus cases and native CRM regressions pass, including standalone replay across encrypted restart. The conservative locale inventory remains 787 candidates. Internal assign/field/stage automation actions still need implementation and now truthfully report PARTIAL; external writes remain explicitly gated. Durable cross-module event delivery, compound provisioning, client archive/stage retry, remaining CRM localization and wider Run-2 gates remain open. No gate is waived, no main/production change occurs, and Run 3 has not started.


### Continuation: CRM archive and stage action retry, 24 September 2026

The compound-stage checkpoint is published as `1726af627dc106a2c5525d2311695b9f475d2aaa`, tree `aad99a8ef998e089a485b1492fc1feae662d0404`. Both local and published histories are preserved and merged. CI 179 is running; CI 178 is the latest confirmed broad pass.

Four failing-before tests reproduce changed archive/stage retry keys and overlapping duplicate requests. Both actual controllers now freeze the original action, target, revision, request key and event ID until confirmation. Persistent feedback names the original source and provides an explicit retry after a refreshed list removes the archived row or navigation selects another entity. Other targets cannot replace the pending action. Missing or malformed success evidence remains unconfirmed; definitive validation failures permit correction. A confirmed write remains distinct from a later source-refresh failure. Revoked access clears private requests/notices and disables detached retries and late replies.

Twelve additional tests execute production controllers against real native CRM writes; all 294 ZERO tests, 14/14 foundation corpus cases and native CRM regressions pass. The minimal DOM mounts legacy innerHTML drag targets from the native board; it does not establish rendered-browser, assistive-technology, native-speaker or browser-reload recovery acceptance. Owned action feedback supports eight locales without network requests or changing payloads. The conservative inventory lists 786 candidates. Remaining CRM source views, compound provisioning and broader Run-2 requirements remain open. No main/production change or Run-3 work occurred.


### Continuation: CRM source presentation and truthful pipeline observations, 24 September 2026

The recovered published head `9e6c927` passes CI 180. Twelve new permanent checks cover record, pipeline, customer, automation, editor and schema presentation; native mixed-currency/missing-value/bounded-source totals; and double formatting of counts. Labels, dates, quantities and owned enum vocabulary bind to all eight locales without rereading sources, replacing action controls, changing authored fields or interpreting literal business text as markup. Missing collections remain unavailable, observed empty collections remain empty, and probabilities no longer default to zero. Pipeline money uses explicit currency, never invented EUR; native results expose value groups and coverage rather than mixing currencies or presenting truncated totals as complete.

A shared locale interpolation defect also coerced already formatted count descriptors back into numbers and could show NaN or altered quantities. Only numeric count parameters are now formatted; explicitly formatted or unknown values remain literal. Plural quantities still require numbers. All 306 ZERO tests, 14/14 corpus cases, 58 targeted checks and native CRM regression pass. Source/log hashes are in checkpoint-tests.json. The initial editor test required mounting its input elements for DOM attribute queries; that fixture fix is documented separately from product defects.

The conservative direct-expression inventory is 781 candidates and is not a completeness metric. CRM provisioning, generated/backend copy, internal automation effects and durable notifications remain open. Controller/native tests do not prove rendered browser, accessibility, native-speaker or full locale acceptance. No gate is waived; main and production are unchanged; Run 3 has not started.


### Continuation: CRM configuration recovery and current-access integrity, 24 September 2026

Existing profile composition previously persisted tenant/pipeline/stages/default/dashboard/audit/receipts separately, left early state after later failure, lost explicit grants in nested calls and released cached private results without current source checks. It now uses the existing same-tenant atomic transaction and rechecks current authority. Historical successful receipts replay compatibly as LEGACY_OUTCOME_UNVERIFIED; no historical atomic proof is invented. Native failures, one-commit durability, capacity rollback and exact HTTP replay through encrypted restart are covered. Fresh sessions after role revocation are denied before and after restart.

Actual profile and pipeline controllers now validate received acknowledgement evidence, keep exact uncertain requests, clear source forms on denied access, invalidate late callbacks and stop subsequent stage writes, even if access is restored before the old response arrives. A confirmed configuration remains confirmed when later source refresh fails. The existing manual pipeline builder remains an explicitly resumable sequence, not an atomic batch. A concrete customer360 defect also returned a linked company without its record-access filter; a failing-before test now proves the restricted company is excluded while an authorized reader still receives it.

Sixteen new tests, all 322 ZERO tests, 14/14 foundation corpus cases and native CRM regressions pass. The 781-candidate conservative inventory and all full locale gates remain open. Internal automation assign/field/stage effects, durable cross-module notifications, generated/backend localization, browser-reload retry recovery and wider Run-2 gates remain open. Browser, assistive-technology, native-speaker and live provider evidence is not inferred from fixtures. Main and production are unchanged, and Run 3 has not started.


### Continuation: durable CRM event acknowledgement and recovery, 24 September 2026

CI 182 passed the exact configuration head `eecd3a38931f9d3452f01d769b7a7e082fa46deb`, tree `d06e292b664137f140daf4aebd547fb6b004d7e7`, including full npm test. A failing-before recovery check then reproduced a persisted CRM mutation losing its downstream event across restart.

Production main and standalone adapters now commit native CRM changes together with an encrypted tenant-scoped `crm:event_outbox` snapshot. Each event has a stable identity, original source revision/value/currency and explicit unknown values. A matching sink acknowledgement is required before a persisted delivered receipt. Lost acknowledgement or failed receipt persistence retains the pending event; canonical ingestion and the standalone journal deduplicate retries. Existing legacy emit-only adapters keep their original contract. Historical missing events are not invented or retroactively certified.

Automatic delivery uses backoff, at most five attempts and batches of fifty. Authorized manage/read operators can retry a bounded batch. Current composition capabilities and native authority still apply; operational payloads stay outside generic Data views and retry responses contain counts only. Capacity rejects before changing business data. The new auxiliary bucket uses existing encrypted storage and requires no destructive migration. Standalone packaging includes the new dependency. An ingestion receipt establishes canonical acceptance, not delivery by every subscriber.

Ten new tests, all 332 ZERO tests, 14/14 corpus cases, native CRM core/API/standalone and access regressions pass. Actual main HTTP and standalone encrypted restarts preserve event identity and source facts. One initial HTTP test omitted an explicit page limit; that test-pagination correction is not product-defect evidence. Adapter outage simulations are distinguished from real HTTP evidence.

Internal automation assign/field/stage effects remain the next recorded code gap. Generated/backend localization, browser-reload durable retry recovery and broader cognitive, adversarial, performance, voice and demo acceptance remain open. The locale inventory is unchanged at 781 conservative candidates. No wider gate is waived, main and production remain unchanged, and Run 3 has not started.


### Continuation: verified native CRM automation effects, 24 September 2026

CI 183 passed exact durable-event head `37ef9abaa9ab92211c8c94fe52b10e835a72e64a`, tree `fdb7684d3eb11e7c83371fa996564afed364c223`, including full npm test. Eight failing-before native checks then demonstrated absent assign/field/stage execution, missing write/target checks, stale stage acknowledgement and unknown numeric input becoming zero. A ninth check reproduced task ownership taken from an untrusted event payload.

Explicit `AUTOMATIC_INTERNAL` action definitions now bind the triggering native CRM record, recheck current write and source access, and execute assignment, allowed business-field changes and same-pipeline stage transitions inside the existing compound transaction. Assignees are verified active CRM user records; their existence does not imply an external identity-provider account. Effects, tasks, audits, events and immutable receipts commit together. Follow-up tasks use the native write path and current source ownership. Replays recheck source references and composition capabilities. Stage effects do not recursively trigger another automation cascade. The stage endpoint returns the final native revision and requested stage; the actual drag/retry controller handles a verified follow-on stage without repeating effects.

Unknown monetary/probability input stays null; malformed and out-of-range values reject instead of becoming zero. Historical untyped action definitions remain planned, and prior execution receipts never rerun under the new implementation. External sends retain their explicit authorization/verified-transport gate.

Eleven new tests, all 343 ZERO tests, 14/14 corpus cases and native CRM regressions pass. Real HTTP demonstrates target-capability revocation, no surviving partial tasks, exact receipts and encrypted restart. The collision test now uses another real authorized source, retaining its original conflict/rollback assertion. The conservative locale inventory remains 781 candidates.

Source-backed action authoring remains the next open gap: the existing form still submits only type/title, so this native checkpoint is not complete UI acceptance. Generated/backend localization, browser-reload recovery and the wider Run-2 contract remain unaccepted. No main/production mutation or Run 3.


### Continuation: source-backed CRM action authoring, 24 September 2026

CI 184 passed native-effect head `fee57ffcbc3083bc8f0366704b09fc2106a13011`, tree `ebbec6350c4b5e6cb95f749b8b26159273ac33c1`, including full npm test. The actual automation form now authors complete typed internal actions. Native source choices select active CRM users, pipelines and matching stages; allowed field controls distinguish actual numbers, text, dates and an explicit cleared value. Source pages are bounded and malformed pagination is unavailable rather than silently complete. No opaque ID entry is required for assignees or stages.

All owned labels bind explicitly to eight locales, preserving literal source names, selected identities and authored values without additional requests. The existing exact-request save contract now includes full action configuration. Save/retry controls lock the actual submit and auxiliary source controls together. Current denial retires source choices and pending writes; late responses cannot restore private observations. Historical untyped definitions are not silently upgraded.

Ten new permanent tests, all 353 ZERO tests, 14/14 foundation corpus cases and native CRM regressions pass. Production form tests save through the real native core, lose/retry a committed response and then execute the configured assignment. Main and standalone HTML dependencies are served and packaged. The conservative direct-copy audit remains 781 candidates across 47 scripts.

CRM custom-object screens and shared-workspace/generated/backend localization remain open. Automation event entrypoints remain the existing stage/evaluation/signed-webhook paths; every-trigger background scheduling is not established by this checkpoint. Browser-reload recovery, real browser/accessibility/native-speaker acceptance and the wider Run-2 gates remain open. No main/production change or Run 3.


### Continuation: CRM custom-object language and recovery, 24 September 2026

CI 185 passed action-authoring head `95e6e8189c5156ee924679997fd0d8a38db004d1`, tree `8388563a20409e7a9f3193431cbcb918d40ddb9d`, including full npm test. Seven genuine failing-before checks then reproduced skipped access refresh with dirty input, mutable uncertain requests, fabricated EUR, late export after denial, private editor retention after source disappearance, non-replayable access changes and explicit grants lost during linked-source history reads. Earlier adapter/schema/DOM fixture setup failures are recorded separately and do not count as product defects.

Custom-object controls, schema/record/version framing, history, access/archive confirmations, related headings and classified feedback now bind explicitly to all eight locales. Authored labels, field codes, source names, field values, confirmations and native payload identity remain literal. Typed negative money uses exact native hundredths with its explicit currency; a new money field has no invented currency. Locale changes retain mounted controls and make no source requests.

Explicit refresh rechecks current editor sources even with dirty input or pending work. Revoked permissions and removed catalog access retire private editors and frozen requests; old replies and exports cannot restore them. Unconfirmed writes retain their original method/path/body and require valid native acknowledgement. Access changes now record a native same-principal request hash within the existing transaction, allowing exact replay without another revision. Historical records and versions remain intact. Current linked-source permission checks preserve explicit grants.

Fourteen new tests, all 367 ZERO tests, 14/14 corpus cases, native CRM and custom-object core/API/client regressions pass. Real HTTP proves access replay across encrypted restart and denial after fresh-session role revocation. The existing UI fixture now represents label spans and structured denial; no assertion is waived. The conservative copy audit falls from 781 to 736 candidates, with zero direct candidates in this controller; it is not a completeness claim.

Shared-workspace and specialist/generated/backend localization remain open. Browser-reload recovery, every-trigger automation dispatch and wider cognitive, adversarial, performance, demo, voice, browser/accessibility and native-speaker acceptance remain unaccepted. Main and production remain unchanged; Run 3 has not started.


### Continuation: shared workspace source observations, 24 September 2026

CI 186 passed custom-object head `db0e68701f648afb57bdf35868f9e0946f86dd55`, tree `973e2a27fbee6dde3a3cdccc0260ac8e7a8c6f79`, including full npm test. Ten failing-before tests then reproduced fabricated currency and null money, invented counts/provenance, unrelated registry fallback for explicit empty coverage, retained private state after denial/failure, late registry/ZERO restoration, cached export without current permission and detached controls changing a newer dashboard.

The actual shared controller now formats observed amounts with explicit currency and exact cents, distinguishes missing values and missing collections from actual zero/empty observations, and labels UTC observations. Eight-locale bindings cover bounded dashboard/source/connector-card framing and classified feedback without rereading sources, replacing controls or changing literal business text, filters and drafts. Current denied access retires private state, dialogs and pending read epochs; late replies cannot restore them. Export rereads the native authorized snapshot before release and escapes formula-shaped strings while preserving numeric amounts and source field IDs.

Twenty new tests, all 387 ZERO tests, 14/14 corpus cases and native workspace session/concurrency/persistence/business regressions pass. Actual member HTTP proves a permitted export and subsequent denial after capability revocation, including encrypted restart. The business-workspaces test's historical accessibility labels are source checks, not rendered-browser acceptance. The conservative audit is 707 candidates, including 365 in the shared controller. A temporary helper-detection undercount of 422 was rejected; direct text fallback restores lexical detection. No translation completeness is inferred.

Shared workspace uncertain dashboard write recovery remains the next concrete integrity gap. Section/definition copy, domain and specialist forms, connector detail, generated/backend copy and native snapshot completeness/currency metadata remain open. Browser-reload retries, every-trigger automation dispatch and broader Run-2 cognition, adversarial, performance, demo, voice, browser/accessibility and native-speaker acceptance remain unaccepted. No native storage schema or production change, no main merge and no Run 3.


### Continuation: shared dashboard request recovery and CI correction, 24 September 2026

Published source-observation head `cb5bf3c1d1cd659163bf9cf69582e1d3d70fc726`, tree `03b0d42132f4a6aebcea6058a1dc0d669a726583`, reached CI 187. That run failed when the existing Procurement economics/outcomes fixture selected occupied TCP port 46496. The failure remains recorded. The fixture now uses PORT=0 and the existing IPC listening observer on each restart. All original semantic assertions remain, and the targeted test and full local regression pass.

Seven genuine failing-before cases exposed missing native dashboard replay, malformed request identity acceptance, mutable uncertain client writes, missing acknowledgement checks, concurrent editing and module revocation during authenticated body input. One earlier 403 came from an incorrect fixture preset scope and is not counted as product evidence. Native receipt metadata now binds current actor, view, original payload and precondition within the same existing layout/audit transaction. Exact retries return the original confirmed result; a later current revision is reported separately. Current route/capability/scope checks run after the body wait before either mutation or receipt release. Private receipt hashes are excluded from public dashboard objects.

The actual shared controller freezes method/path/body/revision/key, locks controls and duplicate submissions, and requires matching native acknowledgement before confirmation. Explicit pending refresh rechecks access without replacing the original draft; denial retires it. Definitive validation failure permits correction with a new request identity. Locale switches retain the same pending request. Real member HTTP verifies a committed response lost to the controller, encrypted restart and exact recovery without a second revision, later-write preservation, hash collision refusal, failed-persist rollback and body-wait revocation for both new writes and replay.

Eleven new tests, all 398 ZERO tests, 14/14 corpus cases and full local npm test pass. The final body-wait guard also has separate native/controller and native workspace concurrency/persistence checks. Exact published-tree CI remains pending. Receipt retention remains the existing 50 revisions per view; unkeyed historical or evicted writes do not gain invented recovery evidence. No new bucket or destructive migration is introduced.

Native workspace snapshot currency/completeness and current access through asynchronous reads remain open, alongside section/domain/connector-detail and specialist/generated/backend localization. The conservative copy inventory remains 707 candidates. Browser-reload retry recovery, every-trigger automation dispatch and the wider Run-2 cognition, adversarial, performance, demo, voice, browser/accessibility and native-speaker gates remain unaccepted. No main/production change or Run 3.


### Continuation: native workspace snapshot authority

CI 188 passed dashboard recovery head `ac17373d08cad2ec866b73c70ecf46dbc0992394`, tree `8811816a95986c98df99d233cf5e10a5da7fb777`, including full npm test. CI 187 remains recorded as failed; the port-allocation defect was repaired without removing assertions.

Two native HTTP failures then reproduced private workspace data returned after module revocation or source ownership reassignment during provider observation. Snapshots now await transport before rechecking current composition/core permission and synchronously projecting current native records, ownership and registry values. Partial-composition behavior is retained. A home snapshot shares one transport observation; no unmeasured performance claim is made.

Two formatter regressions are also covered: explicit native identity/role/profile values remain literal text, while negative, fractional or unsafe counts remain unknown. Actual zero and exact signed monetary values remain valid. Four new tests, all 402 ZERO tests, 14/14 corpus cases and native masterbuild/business/composition regressions pass. The provider barrier uses the real native fetch boundary but an isolated unverified readiness response; it is not live provider evidence. A guessed nonexistent granularity test command is recorded separately as a command-selection error and replaced by the actual composition API/matrix tests.

Registry knowledge counts, source completeness and native currency metadata remain the next concrete source-integrity gaps to reproduce. The conservative locale inventory remains 707 candidates, including 365 in the shared controller. Full localization and broader Run-2 cognition, adversarial, performance, demo, voice, browser/accessibility and native-speaker gates remain unaccepted. No storage schema change, main/production mutation or Run 3.


### Continuation: current-authorized knowledge observations

Snapshot head `d57644908f68c6a72f016f426cd3491c38c5e0fb`, tree `fde24f2f0bfa0e7029ad3333a00783de7142c000`, is preserved locally and published on the same draft PR. CI 189 is running; CI 188 remains the latest confirmed broad pass.

Eight failing-before cases reproduce raw private knowledge counts, missing confidence becoming zero, invalid confidence coercion, lost explicit user grants in search/export and totals silently limited to a 100-row preview. The native register returned three records to a reader authorized for two. Counts now use native current-authorized retained records and explicitly identify that scope. Search and export apply the same record grants; export requires native source-read authority. Missing or invalid registry counts remain unknown, and genuine zero does not fall through to another count.

The Knowledge workspace now aggregates all authorized retained/current records before bounding its preview. Native HTTP with 105 real records verifies lifecycle counts, 103 current records, 100 preview rows, complete aggregates and incomplete preview coverage. Percentage denominators and observed confidence samples are explicit. Missing new confidence stays null, real zero stays zero and malformed/out-of-range input rejects before mutation. Historical numeric zeros remain intact because their original intent is not recoverable.

All 410 ZERO tests, 14/14 corpus cases, native platform and masterbuild regressions pass; native registry rights survive encrypted restart. No storage schema change or external acceptance claim is made. Native Finance currency aggregation/posting and workspace metadata are next to reproduce. Knowledge write atomicity/retry, localization and wider Run-2 gates remain open. No main/production mutation or Run 3.


### Continuation: native Finance currency observations and recovered regression

CI 189 and 190 passed exact snapshot and knowledge heads, including full npm test. The knowledge checkpoint is `f5b3ab334da71359b3aa265b8571badeca9e7dd5`, tree `b1f7bc3e834ddbcbf898e22de7fae88ab405ba71`. The new Finance implementation was first preserved locally as `59c2899` when another execution interruption occurred.

Eighteen new permanent tests reproduce invented/lost currencies, mixed sums, clamped signed revenue, unsafe/overflowed amounts, incompatible posting/reconciliation, false zero under historical conflicts and currency changes silently replaying an earlier result. An additional failing-before native ZERO assertion detects a rounded cent at the safe integer boundary. Reports now separate explicit currencies and disclose unknown scalar totals or historical conflicts without rewriting stored rows. Native account/chart setup inherits the declared entity currency; invoice/payment/reversal journals preserve it. Cross-currency posting and incompatible bank reconciliation reject before mutation, without inventing a conversion rate. Bank identities bind their recorded source fields, and compatible legacy receipts remain usable. Counterparty amounts retain currency groups.

Workspace metadata, native ZERO and actual Finance rendering retain currency, signed amounts and exact hundredths across eight locales. Native member HTTP and encrypted restart verify the source values through all three paths. Retained-source completeness is distinct from the external ledger; live provider and rendered-browser acceptance are not claimed.

The first broad local run failed a historical assertion expecting zero without any recorded currency. The corrected native test proves both unavailable currencyless money and actual zero for a configured empty EUR ledger. A second npm process disappeared during composition tests; its log is preserved without an invented exit status. All 184 remaining composition/spatial/ZERO commands have a final successful outcome. The initial ZERO phase passed 427/428; its extracted formatter test lacked ambient state. A pure explicit-currency formatter and explicit-currency assertions resolved this without changing the exact-cent or unknown-value requirements. The related native assertion was also corrected and reverified. Failed attempts and bounded source amendments remain in the durable command evidence. Together with the verified prefix they cover every existing local regression command; all 428 ZERO tests and 14/14 corpus cases pass. Exact published-tree CI is still required.

No storage migration, destructive backfill, credential change, main merge or production write occurred. Finance compound write recovery, report-range semantics/schema terminology, CRM/Analysis metric metadata, shared section/domain/connector-detail forms and localization remain open. The conservative inventory stays at 707 candidates. Full cognition, adversarial, performance, demo, voice, browser/accessibility and native-speaker gates remain unaccepted; Run 3 has not started.


### Continuation: connector detail interaction, localization and durable credential acknowledgements

CI 191 passed the exact published Finance head `e75c2b11e14b7adef8a59846dcb325cf4cb8b440`, tree `052dfba077137842f5bde8b425f18e5c603d379a`, including full npm test. The subsequent connector checkpoint adds 18 permanent tests: failing-before form ownership/races/false-success checks, native revision/receipt and corrupt-state checks, localization/frozen retry/SMTP checks, a secret-masking regression, positive/keyboard/denial checks and a genuinely dropped HTTP response after native persistence. All 446 ZERO tests and 14 corpus cases pass, as do targeted current-principal, mail-authentication and OAuth regressions. Complete npm regression for this new tree awaits exact published-head CI.

The save action now submits its own credential form, locks duplicate writes and retries the same frozen request/key after an unconfirmed response. Typed acknowledgements bind connector, request and revision. Known revision conflicts preserve input and require reopening current details. Native guarded writes store their receipt in the same encrypted atomic file as the configuration, preserve newer writes on retry and survive process restart. Deletion retains an encrypted receipt tombstone to prevent old retries resurrecting secrets. Corrupt encrypted state refuses both reads and replacement writes; undeclared credential names are not exposed in read metadata. Legacy revisionless records remain readable and no production data was migrated. Legacy unguarded callers still need separate recovery work.

Owned connector-detail copy and formatting update across all eight locales without refetching or replacing drafts. Source names, field labels and provider errors remain literal. Tabs have keyboard navigation and reciprocal panel labels, exercised only in the minimal DOM. SMTP authentication is distinguished from connected inboxes and delivered messages. Unknown or malformed ingestion counts cannot be represented as completed zero-record syncs. The conservative inventory drops from 707 to 674 candidates; this is not a localization completion metric.

The generic runtime sync route still only fetches data. A separate older integratedSync path exists and needs reconciliation, current-principal, atomicity and count-bound evidence; it has not been silently called complete or replaced. Registry coercions, OAuth rollback concurrency, credential recovery after closing/reloading, receipt retention, shared section/domain forms and localization remain open. No real credentials, deployment, main merge or Run 3 work occurred. Acceptance statuses remain 44 PASS, 46 code-controlled FAIL, 7 code-controlled UNVERIFIED and 28 environment-unverified; wider Run 2 gates remain unaccepted.


### Continuation: connector registry observation integrity

The detail/configuration tree was published as `f6331e8cb72e5f6ca545ad03c1b523d7e9ee9f93` (`6a1d0a37cc5d36fa9d2f23599bda1bcfab19796f`). CI 192 is still running at this checkpoint; it has not been labelled PASS.

Nine new registry tests include eight failing-before observations and a positive confirmed-state control. Success now requires actual booleans. Stored tokens alone do not prove authentication, and retained records do not prove successful sync/bootstrap. Missing counts remain unknown unless complete current authorized retained-record coverage is explicit; the synchronous native snapshot supplies that coverage. Invalid latency/count values no longer coerce into zero, and real zero remains zero. Unknown configuration/authentication remains unknown in Source projections; default credential fields are secret. Owned retained-record labels and missing probe/sync timestamps follow the current locale.

All 455 ZERO tests and 14 corpus cases pass; native masterbuild regression also passes with its explicitly isolated provider responses. The first broader run passed 454/455 because the existing overlay test assumed every lifecycle enum had a dedicated overlay label. UNKNOWN already rendered correctly through common.unknown in production; only that test expectation was corrected. The failed run remains recorded, and frozen dashboard production code was unchanged. No storage migration or production action occurred.

Generic sync ingestion, older integratedSync reconciliation, current-principal rechecks after provider waits, atomic import/receipt/count bounds and first bootstrap interaction remain explicit code-controlled gaps. This is another bounded Phase 8 correction, not a waiver or Run-2 completion.


### Continuation: bounded native connector source imports

CI 192 and 193 passed full npm test at their exact published detail and registry heads. The registry head is `dced1b127dd8e5251d39321b52394342835b48fc`, tree `597fd2398410c217ae299ce0f9345b1f3d76c88a`. Ten new permanent tests cover the next generic source-import correction; all 465 ZERO tests and 14 corpus cases pass. Targeted native masterbuild/current-principal regressions and later DNS/read-authority tests also passed. Full new-tree npm regression awaits publication/CI.

The generic runtime route and existing generic integratedSync branch share the same read-only GET transport and transactional source importer. It validates explicit JSON record pages, byte/item/number bounds and identity coherence before effects. Typed source envelopes cannot overwrite customer objects or accept provider-supplied tenant/provenance fields. Native source text retains its full permitted length after redaction, while unknown provider observation dates stay unknown instead of inheriting an ingestion timestamp. Counts describe the fetched page; no external source completeness is asserted.

Current rights and configuration are checked after DNS before releasing the provider request and again after the provider response. Existing encrypted persistence commits source envelopes, request receipts and audit together. Tests exercise a failed disk rename, an actually dropped HTTP reply, encrypted restart, parallel retries sharing one provider read and both generic routes replaying the same receipt. Current connector management and owner-module access guard retained raw-source bodies. Matching durable import proof now supports the registry sync observation without inferring success from unrelated records. The UI supports first bootstrap after a confirmed probe and validates matching request/proof/counts before reporting source storage.

Early test-fixture follow-ups selected the canonical-only workspace list and then an incorrect route; those errors remain in the evidence rather than being called product storage failures. Correct-path checks use the existing `/api/module/data/data` API and separately reproduced/fixed genuine text truncation, stale revisions, legacy replay, fabricated dates and source disclosure. No real provider or customer credentials/data were used.

The new encrypted receipt bucket and source-envelope record type require no destructive backfill. Provider-specific bootstrap paths, generic probe races, unsupported provider mappings, governed customer-object promotion, first-class workspace source browsing, broader localization, invalid environment limits and the wider Run-2 gates remain open. This does not establish enterprise migration, Connector Factory, live provider, browser, voice or Run-2 acceptance. No production action, main merge or Run 3 work occurred.


### Continuation: current native probe authority and transactional source activity

The source import was published as `161e2ae190a237fba5d4bf0773ccad476fa3f6e9`, tree `c007e2c0fc6e91af8f74d8529c766d196572bfca`. CI 194 failed because the smoke test fed a singular ping object to a record-page importer. That rejection remains required. Using an explicit local HTTP record page then reproduced a genuinely missing sync activity event; no failure or assertion was waived.

Native generic probes now bind their captured profile/configuration revision to current authorization after DNS and after the provider response. Revoked principals or disabled owners cannot receive an old success; configuration changes produce a conflict. The actual controller checks the returned configuration revision and suppresses a probe superseded by a concurrent credential save. Generic source imports again produce native activity, in the same encrypted atomic commit as rows, receipts and audit. A failed commit leaves no activity, and replay/restart creates no duplicate.

Nine new permanent tests initially fail these native and controller boundaries; all 33 targeted tests and the corrected smoke test pass. The complete local npm test process is running on immutable source files; no full new-tree ZERO/corpus result is claimed yet. Exact published-head CI for this correction is pending. Controlled provider/DNS/disk/socket fixtures and a synthetic local HTTP provider do not establish external provider or native browser acceptance. Inventory remains 674 candidates across 47 scripts; matrix verdicts remain unchanged.

Legacy/provider-specific and bulk probe authority, profile identity and OAuth concurrency, receipt retention, broader shared workspace section/domain localization, semantic/cognitive/industry/demo, performance and the documented external acceptance gates remain open. No production, credentials, main branch or Run 3 action occurred.


### Continuation: legacy probe authority, decoded owners and preserved regression failures

The native-probe/activity checkpoint was published as `845baa28ea4ed3cd67510e5130cbb95d6189046a`, tree `46f23c7bccac825a2032f50c7ff4eab6a31d8b3d`. The complete local npm test process and CI 195 both failed in `communication-provider-state-test.js`: the VM fixture searched for the old entire function signature and therefore never loaded status. Extraction now checks the actual function independently of its parameter list, and its no-network SMTP assertions use the current transport boundary. None of the behavioral assertions were removed. The failed process and its successful earlier prefix remain evidence; there is no claim that it completed all regression tests.

The older generic integration-test route now contacts only the selected source and binds its observation to current authority, runtime configuration/profile and legacy credentials across DNS/provider waits. Typed conflicts and owner denials reach callers correctly. Two additional failures exposed encoded-ID and custom-profile gaps in owner resolution; the common check now resolves the actual ID and owner profile. Specialized providers and bulk status operations remain open for their own current-authority review.

Eleven new permanent tests initially fail these boundaries. All 44 targeted tests, all 485 ZERO tests and 14 corpus checks now pass. Native smoke, masterbuild, provider access, current-principal and SMTP status regressions pass. New exact published-head full CI is pending. Synthetic provider/DNS fixtures do not prove live-provider acceptance. No new migration, actual credentials, production/runtime change, deployment, main merge or Run 3 work occurred. The matrix remains IN_PROGRESS_NOT_ACCEPTED.


### Continuation: shared evidence-section localization and observation integrity

The generic evidence-section renderer now uses explicit eight-locale owned copy and registered field labels. Known counts, UTC dates, exact currency and confidence retain unknown values; arbitrary source strings, unknown field names and nested objects remain literal and inert. Switching locale changes neither source values nor DOM identity and issues no requests. The 18-field limit is explicit and null fields are no longer silently dropped.

Six new controller tests fail before implementation. Matching workspace/section/status and valid record arrays are required; missing or contradictory observations do not masquerade as empty results. Independent observation tokens and current workspace/section/access checks prevent old results or errors from replacing a newer view. All 60 focused locale/workspace/connector/knowledge tests and native business workspace regression pass. No full 491-test ZERO suite or native browser/accessibility/native-speaker acceptance is claimed for this change; exact full CI remains pending. The prior connector checkpoint retains 485/485 ZERO and 14/14 corpus passes.

Inventory is 668 candidates across 47 scripts, with the frozen main dashboard still at eight candidates. The inventory is conservative. Context headings/tabs, package/identity settings, shared forms and specialist/backend copy remain actual open Phase 8 work. No storage/API migration, credentials, production action, main merge, deployment or Run 3 work occurred. Matrix verdicts remain unchanged.


### CI execution budget after the measured job cap

CI 196 began at 07:09:39 UTC and was cancelled at 07:19:39 UTC by the existing ten-minute job boundary. Its log reports 485 ZERO tests passed, zero failed, and 14 corpus passes, but the npm step/job has no successful conclusion and is not accepted as PASS. The full job budget is now twenty minutes. The npm test command, all assertions and individual test/provider timeouts are unchanged. This is execution capacity for the complete required suite; it does not satisfy or waive the separate performance/cost acceptance gate. Exact-head CI must finish successfully.


### Continuation: owned workspace section labels and descriptions

CI 197 passed the evidence-section head `848294fe2a15c19076d77495a470835a9b5c3d4b` and CI 198 passed the CI-budget head `cd69d6898e64880073f4852fefd7732d82633a78`. Both completed the full npm test process, 491 ZERO tests and 14 corpus cases. CI 196 remains a cancellation, not a pass.

All 103 section names used by the shared workspaces now have explicit labels in eight locales. All 72 existing owned descriptions and the generic description also have explicit translations. Actual-controller tests enumerate the registered definitions and exercise repeated locale changes. Tabs, titles, descriptions and eyebrows update without changing canonical identifiers, selected-tab attributes, DOM identity, source names or request counts. Unknown section names remain literal, including prototype-shaped strings and markup-shaped text.

Three permanent tests fail before the corresponding implementation. The final focused run passes 38 tests and native business workspace regression passes. Full CI for this later source tree remains pending; no full 494-test run is inferred from the focused result. The conservative inventory is 667 candidates across 47 scripts, with the main dashboard still at eight. The catalog is excluded from that inventory, so its small reduction is not a measure of translation completeness.

Package/identity settings, shared domain forms and specialist/generated/backend copy remain open. Native browser, layout/accessibility, independent native-speaker and actual voice acceptance remain unverified. Wider cognition, semantic, demo, industry, adversarial and performance/cost gates remain unaccepted. No migration, real credential change, production mutation, main merge, deployment or Run 3 work occurred. Acceptance statuses remain unchanged.


### Continuation: package composer review, recovery and localization

CI 199 passed the section-copy head `9a685c467a3671b9a8b3bb2b2476b49171bac061`: full npm test, 494 ZERO tests and 14 corpus cases. The next composer checkpoint adds eleven permanent tests. The initial six tests fail before implementation; the final targeted run passes 49 tests. Native capability resolver, authenticated composition API and workspace regressions also pass. A later diagnostic attempt was interrupted while formatting a large DOM assertion and remains recorded as a failed process, not individual completed test evidence. Equality checks now emit bounded diagnostics.

The package composer now localizes owned controls, known module/capability/industry/package labels and review messages in eight locales without replacing draft controls. The review shows entitlement-only and capability-only differences, alongside module activation and industry changes. Only a matching native preview for the exact current input enables Apply. The controller rejects malformed or contradictory catalog/current/preview/write observations and requires explicit management authority. Per-view tokens and access generation suppress stale reads and retired callbacks. Duplicate submissions are blocked. An uncertain write retains the reviewed body for retry, while a newer native configuration remains protected. Confirmed writes are not relabelled uncertain when only the navigation refresh fails.

This is actual-controller/native-resolver evidence in a minimal DOM, plus native server regression. The lost-response client case uses a controlled transport and in-memory resolver, not real dropped HTTP or cross-reload recovery proof. Cross-navigation/reload request recovery and durable attribution remain open. No full 505-test run is claimed before current-head CI completes. Inventory is 656 conservative candidates across 47 scripts; the frozen main dashboard remains at eight. Identity controls, domain forms, specialist/generated/backend copy and broader Run-2 gates remain open. No migration, real credentials, production operation, main merge, deployment or Run 3 action occurred. Matrix statuses remain unchanged.


### Continuation: native identity recovery and localized access controls

CI 200 PASS confirms full npm regression, 505 ZERO tests and 14 corpus cases on published `70a92f942b6a1b947e55689edb53afb485ca7d5d`, tree `1c97f3229f43f0bfbec29b4345ca8bd5b560157b`. The newer recovery attachment is preserved in the same Run 2 history; prior visible checkpoint prose does not supersede this evidence.

Identity management adds 20 permanent tests. Before implementation, three native receipt cases and eight controller cases fail. A further test exposes missing terminal recovery for requests that never committed. All 20 targeted cases now pass. Six native tests exercise real HTTP routes and isolated production-mode encrypted persistence: a destroyed invitation response recovers after restart without duplicate creation; invitation, reissue, update and terminal recovery roll back atomically under final-file rename failure; authority and projections remain private; concurrent retries share one result; a held-open HTTP request body cannot commit after recovery durably closes its request key. Three core guard tests cover isolation, expiry, capacity and abandoned keys. Eleven controller tests cover exact input/key retries, typed acknowledgement and live authority, scope/revision/link tampering, current view retirement, reload recovery, revoked private state, logout and eight-locale ownership.

The UI uses 55 explicit owned copy rows across eight locales without replacing drafts or canonical grants. It retains only opaque request metadata in session storage, scoped to the current tenant/dealer/actor. Confirmed native receipts remain in encrypted identity storage; no invitation secret is persisted by the browser. Recovery returns an existing still-valid result or durably closes a not-yet-committed key. A late original body then fails without affecting a member. Unknown/malformed responses never become success, and superseded/expired invitation tokens are not redisplayed. New effects require usable recovery storage. Legacy keyless API clients remain compatible and are not claimed to have the new recovery guarantees.

The additive encrypted receipt scope uses the existing core state format. No destructive migration, real credential change, main merge, production action, deployment or Run 3 action occurred. Receipt retention/operator lifecycle remains open; reaching the explicit capacity fails without silently evicting existing recovery. Real browser/storage, accessibility and native-speaker acceptance remain unverified. Full current-tree ZERO and CI are pending at this checkpoint. The conservative localization inventory is 634 candidates across 47 scripts, with the frozen main dashboard unchanged at eight. Shared domain forms and specialist/generated/backend copy remain the next Phase 8 gap. Broader hard exit gates and matrix statuses remain unchanged.

The frozen identity implementation subsequently passes all 525 ZERO tests and 14 corpus cases, plus all three existing native identity suites. Current exact-head full npm CI remains pending publication. This result does not promote full-surface or whole-run acceptance.


### Continuation: generic domain receipt integrity and shared forms

CI 201 PASS confirms complete npm regression, 525 ZERO tests and 14 corpus cases on `400b9f154140e56f9c1326b017ba751393568cc0`, tree `dc7438217ad1abbdf206ba6786fe7fd179128422`. All identity implementation and earlier local/published histories remain preserved.

The next domain checkpoint adds 15 tests. Two native and six controller cases fail before repair. Generic native updates now recover the exact still-current result of the same request before checking the original expected revision; later changes cannot be falsely attributed to an old create/update receipt. Current owner, module and edit-session checks still apply. Additive receipt fields bind the expected and resulting revisions. Older receipts are retained and explicitly unverifiable where the historical revision was not recorded. Capacity is bounded without eviction. Real HTTP tests destroy an actual committed update response and recover after encrypted restart. Core tests also cover capacity, current ownership/revocation and legacy evidence retention.

The shared editor now rejects malformed schema/list/acknowledgement observations, suppresses duplicates, keeps a frozen request across uncertainty and in-page view reconstruction, and prevents retired views from repainting or sending into another module. Confirmation is shown independently of refresh. Common controls and registered fields use 99 explicit owned copy rows in all eight locales. Actual controller/native-core tests preserve names, canonical status/supplier IDs, Procurement line prices, current industry values and recurrence/explicit time offsets through locale changes. The calendar test initially assumed normalized UTC storage; it now correctly asserts retained explicit offsets and the native absolute recurrence across the daylight-saving boundary, including duration.

Full browser reload recovery of generic editor requests remains code-controlled and open. Specialist/generated/backend copy and wider hard exit gates remain open; no matrix status was promoted. Minimal DOM is not browser/layout/accessibility/native-speaker proof. The conservative inventory is 620 candidates across 47 scripts; frozen main dashboard source remains unchanged at eight. Current full ZERO and exact-head CI are pending at this checkpoint. No destructive migration, real credential change, production action, main merge, deployment or Run 3 occurred.

The frozen domain implementation subsequently passes all 540 ZERO tests and 14 corpus cases, plus the existing business-domain, civil-time, industry-field API and workspace regression suites. Current exact-head full npm CI remains pending publication. No broader acceptance status changes.


### Continuation: generic domain recovery after controller reload

The previous domain checkpoint is published as `ff75df29f329e141d9d1351c3237ca94a6bce3d2`, tree `523e978fee4597b0d5121d3733ca0bf009c4bd8d`; CI 202 is in progress at this checkpoint. CI 201 remains the latest completed full npm PASS observed here. All prior commits and receipts are preserved.

Six new permanent tests extend generic domain recovery. Two native and three initial controller tests fail before implementation. The final focused run passes 21 cases, followed by two native cases extended to exercise the real Analysis route. An intermediate native failure revealed a mismatch between new zero and historical null expected-revision metadata; exact replay now respects the form actually recorded. This failure remains in the evidence ledger.

The authenticated schema now supplies the tenant/dealer/actor request context. The UI retains only the key, target and result revision in session storage, partitioned by that context plus module/entity. A fresh controller can reconcile an earlier request without storing its title, description or full payload. The native route returns a still-current result or atomically closes an absent key. A real incomplete HTTP body arriving afterward is rejected without a record mutation. Terminal persistence rolls back under an injected final-file rename failure and survives encrypted restart after retry. Actor/entity and current visibility checks prevent foreign or superseded results from being falsely confirmed. Loss of access to an already applied result does not expose private content or permanently block independent new records. Forged scope and retired view responses retain the recovery reference. Unavailable or malformed session storage blocks new effects.

Current controller reload proof is a fresh VM with explicit session storage and the native in-memory domain engine, backed by separate real HTTP/encrypted persistence tests. It is not actual browser/sessionStorage, layout, accessibility or native-speaker acceptance. Current full ZERO and exact-head CI are pending. Composition recovery, specialist/generated/backend copy and broader hard exit gates remain open. Matrix statuses are unchanged. No destructive migration, real credentials, provider configuration, production action, main merge, deployment or Run 3 occurred.

CI 202 subsequently passes full npm regression, all 540 ZERO tests and 14 corpus cases for ff75df2. The newer frozen terminal-recovery implementation passes 546 ZERO tests, 14 corpus cases, and existing business-domain, industry-field API and Core-access API suites locally. Current exact-head full npm CI remains pending publication.


## Continued Phase 8: retained workflow inspection

CI 203 passed published domain recovery commit `1fcc02e`, including full npm regression, 546 ZERO tests and 14 corpus cases. The later workflow inspector has twelve permanent production-component/native-core tests: all eight locales, literal source data, selected-step and signature binding, rapid switching, late replies, current denial, navigation back, missing/malformed evidence, safe diagnostics, unknown outcomes and small nonzero operands. Locale changes preserve the current selector and make no read or write calls. Readout cannot execute, retry, approve or mutate actions. Recorded action codes remain in the native journal, separate from safe screen feedback.

The native inspection, actual HTTP/current access/encrypted restart, existing component, run-query and ZERO run-control regressions pass. Two intermediate test-fixture mismatches and the small-number failure are retained in checkpoint evidence. The old native label helper now follows the owned span; no action/permission/persistence assertion was removed. Full current-source ZERO and exact-head CI are pending at this source checkpoint.

The conservative inventory is 616 candidates across 47 scripts, with the frozen dashboard unchanged at eight. This closes only the evidenced readout behavior; workflow lists, authoring/generation and other specialist/backend/generated copy remain open. Minimal DOM tests do not establish rendered browser, assistive-technology or native-speaker acceptance. No native mutation API or storage migration, credential change, deployment, main merge or Run 3 work occurred. Run 2 remains IN_PROGRESS_NOT_ACCEPTED with unchanged matrix verdicts.

The frozen workflow-inspection source `1b7ce34` (tree `a30096a50cc8b0e1bc21b88762743e41004fe26b`) subsequently passed all 558 ZERO tests and all 14 corpus cases. Exact-head CI remains pending publication; no full regression outcome is inferred for this later tree before CI finishes.


## Continued Phase 8: workflow generation and reviewed private save

The production generator now binds forty-eight owned catalog rows across all eight locales, preserving customer/model text, form inputs, native identifiers and branch semantics. Typed evidence ties the proposal to the requested draft and compiled definition; native save acknowledgement must match the reviewed compiled content and exact draft revision. A lost response retains one frozen action and turn, locks reviewed inputs and prevents a second request identity. Exact replay cannot overwrite a newer native draft. Retired views suppress new calls, old results and saved callbacks. Provider failures use safe owned feedback; model clarification questions remain literal text.

Six corrected-fixture cases fail before implementation; all eleven expanded permanent tests pass. The initial fixture retry-contract mistake and native label-helper adaptation are preserved honestly. Native generator, language parsing/API, ZERO/API and draft/API regressions pass, including actual authenticated HTTP/current principal/encrypted restart evidence with isolated provider transport. No generation or locale switch implicitly saves, publishes, activates or executes a workflow. The previous inspector checkpoint is published as `ea34f59`, tree `6350099051f60f3b38516f16952c367662c3d9b1`; its CI 204 remains running at this checkpoint. Full current-source ZERO and exact-head CI are pending.

The conservative inventory is 604 candidates across 47 scripts, with the frozen dashboard unchanged at eight. Cross-reload attribution/terminal reconciliation for uncertain new drafts, editor/autosave/publication, other workflow controls and specialist/generated/backend copy remain open. Minimal DOM/deterministic provider tests do not establish browser, native-speaker or live model quality. No native store schema or mutation API, credential, main/production/deployment or Run 3 change occurred. Matrix verdicts remain unchanged.

CI 204 subsequently completed successfully on the exact inspector head `ea34f59`, including full npm regression, 558 ZERO tests and 14 corpus cases. The later generator source `4aa2e98` (tree `618152bccf6c939d9bcfdbe417d02bd2a8ed96ce`) passed all 569 ZERO tests and all 14 corpus cases locally. Its own exact-head CI remains pending publication.


## Continued Phase 8: generated draft recovery after reload

CI 205 passed exact published generator head `c636e1b`, including full npm regression, 569 ZERO tests and 14 corpus cases. New native draft receipts now atomically retain initial creation/preview attribution and terminal absent-request closure in the existing encrypted store. Recovery checks current owner, realm, permission, revision and fingerprint. A missing APPLIED record cannot be recreated by an older retry. Receipt capacity preserves prior evidence; keyless exact native replay remains compatible. Legacy records without preview attribution stay explicitly unverifiable. Generic Data excludes the private journal.

The production generator persists only scoped draft ID/revision/preview-hash metadata. Fresh views recover through the native route without storing or reissuing private draft content. Malformed/unavailable browser storage blocks new writes; mismatched or retired replies cannot erase recovery evidence or call the saved handler. Closed requests, saved drafts and failed local metadata removal have distinct truthful feedback across eight locales.

Fifteen new permanent tests plus the eleven retained generator controls pass. Actual HTTP destroys a committed response, verifies encrypted restart, holds an original body until after terminal closure, checks EIO rollback and current member access after a body wait. Existing dynamic principals already protected that boundary and were not rebuilt. The legacy protocol regression, invalid fixture entitlement and false terminal metadata wording are preserved with their corrections. The existing generator/draft/ZERO/language/workspace regressions pass. Full current-source ZERO and exact-head CI remain pending.

Fresh VM/session-storage fixtures do not establish browser persistence, accessibility or native-speaker acceptance. The conservative inventory remains 604 candidates across 47 scripts, with eight on the frozen dashboard. Workflow editor/autosave/publication and the broader recorded hard gates remain open. Run 2 remains IN_PROGRESS_NOT_ACCEPTED; no matrix row is promoted and no main/production/deployment or Run 3 work occurred.

The frozen generated-draft recovery source `c9ec748` (tree `f297ec908288d2d05f370dfebe8b8268fe65c51c`) subsequently passed all 584 ZERO tests and all 14 corpus cases locally. Its own full regression remains pending exact-head CI after publication.

## Continued Phase 8: private editor autosave and owning-view boundary

CI 206 has now passed exact published generated-draft recovery head `48ec188`, including full npm regression, 584 ZERO tests and 14 corpus cases. The next open editor checkpoint preserves that work and adds sixteen permanent tests. Six initial session failures and four initial owning-editor failures are reproduced before their corresponding fixes. A further regression catches and corrects overblocking of save-as-new after a genuine native conflict.

The native draft response adds an exact request fingerprint. The session checks that fingerprint, normalized record hash, current owner/realm, ID, schema and exact private revision before advancing. After an uncertain response, a newer edit first replays the frozen original body and then uses the confirmed revision. Current denial preserves that original request. Newer native revisions remain protected by the existing conflict rule. No native partial-draft sanitizer is duplicated in the browser.

The editor now binds saves to its current draft session and owning view. Retired controls and queued timers cannot dispatch; a late draft acknowledgement cannot release publication or a saved callback. A form changed during the prerequisite save requires renewed review. A pending or uncertain draft cannot be forked through save-as-new; a genuine conflict may explicitly be copied without overwriting the other revision. Two actual HTTP tests destroy a successful committed PUT reply, resume after encrypted server restart, and check current member authority after a body wait. Existing dynamic principals already protected that latter boundary and were not rebuilt. Native draft, editor, ZERO, generator and workspace regressions pass. Full current-source ZERO and exact-head CI are pending.

The browser module runs in a fresh VM with native WebCrypto and a minimal DOM, not a rendered browser. Editor localization, typed ZERO review/save, publication acknowledgement and uncertain-result/cross-reload recovery remain open alongside the broader hard exit gates. Matrix verdicts remain unchanged; no main/production/deployment, real credential, destructive migration or Run 3 work occurred.

The frozen editor-session source `e6816e8` (tree `4469d859af8204758da6c65621cbb8094a174a6a`) subsequently passed all 600 ZERO tests and all 14 corpus cases. The same-source full npm regression remains pending exact-head CI after publication.

## Continued Phase 8: editor review, explicit confirmation and literal document content

CI 207 passed exact published editor-session head `47b281b`, including full npm regression, 600 ZERO tests and 14 corpus cases. The next editor checkpoint adds eighteen permanent tests; thirty-two focused controls pass with the retained session tests. Five initial confirmation failures, four initial localization failures and further reason/signature/revision/multiline failures are preserved in the evidence ledger. The existing native editor/authoring/draft/ZERO/generator/language/execution/conditions regressions pass.

Sixty-four owned rows now cover editor controls and safe feedback across all eight locales. The ZERO review includes complete literal document content, other action fields, conditions, trigger, automatic behavior, approval and retry policy. Locale changes retain input nodes, values, selectors, review state and native request identity. The existing warning that an automatic published version may start without a separate activation choice remains explicit in every locale. Arbitrary native/transport diagnostics do not become screen copy.

ZERO saves and immutable workflow publication retain exact pending requests and block alternate writes during uncertainty. Acknowledgement binds native content, current realm/revision and signature before success or a callback. Retired results cannot alter the old form. A known publication remains known if its refresh callback fails. Reason input after preview does not mutate the draft or invalidate review; an ordinary draft save does invalidate the earlier reviewed revision. A retained successful consumer can continue editing after its successful callback.

The stronger acknowledgement exposed native document content flattening. Native definition/run-step sanitation and Automation record content now preserve line breaks and tabs with the existing control-character and length bounds. Historical definitions and records remain unchanged. A real editor-to-HTTP test destroys a successful publication response, restarts the encrypted server, retries the identical request without another draft save, then separately executes the native workflow. The resulting document retains its multiline literal body through another encrypted restart. The first run-route fixture assertion expected 201; it now requires the existing 202 contract, with all native outcome assertions preserved.

This is retained VM state across a server restart, not browser-reload recovery. Durable editor-update/publication recovery and shared automation controls remain open. The conservative locale inventory is 569 candidates across 47 scripts; the frozen dashboard remains eight. Full current-source ZERO and exact-head CI are pending. Matrix verdicts and the wider hard gates remain unchanged; no real browser/native-speaker claim, main/production mutation, credentials, deployment or Run 3.

The frozen editor-confirmation source `7e51f69` (tree `1131290c05505804dfb48db70c12045e8990501c`) subsequently passed all 618 ZERO tests and all 14 corpus cases. Full npm regression for this source remains pending exact-head CI after publication.


## Continued Phase 8: ongoing private draft recovery after reload

CI 208 passed full npm regression on published `01f579e`, including 618 ZERO tests and 14 corpus cases. The next checkpoint retains optional keyed write receipts atomically with private drafts and audit. Current owner, realm, expected revision and exact request fingerprint bind recovery. A still-current draft can be recovered without replaying its body; a closed absent key cannot later apply its delayed request. Missing, superseded and legacy evidence remain explicit. The bounded metadata-only journal is excluded from generic Data, and existing keyless/generator recovery remains compatible.

The editor and ZERO draft save persist four-field metadata before dispatch, partitioned by tenant/dealer/actor. A fresh view validates native recovery before offering an owned draft. Seven additional owned messages cover all eight locales. Unavailable/malformed storage blocks new writes; retired/forged/current-denial responses retain references. Metadata cleanup failure does not undo a known native result. Invalid partial condition input now returns a definite 422, allowing correction rather than trapping an invalid uncertain retry.

Fifty focused tests pass, including nineteen new permanent tests. Actual editor-to-HTTP evidence destroys a committed response, restarts encrypted persistence and recovers in a fresh VM without private body replay. Further HTTP evidence covers EIO rollback, a delayed incomplete PUT after terminal closure and current member authority after a body wait. An existing timing-dependent authoring fixture is strengthened to complete delay and exact approval before requiring terminal replay; its intermediate approval expectation failure remains in the ledger. Full current-source ZERO and exact-head CI remain required.

Fresh VM/session-storage fixtures do not prove actual browser persistence, accessibility or native-speaker acceptance. Publication recovery after browser reload is next; shared automation controls and broader hard gates remain open. Matrix unchanged; no main/production/deployment, credentials, destructive migration or Run 3.

The frozen editor write-recovery source `af72944fafc40f6480859cb2e41467d66b260c87` (tree `a47429dc6f187124ce5f2984439c62a058c0c681`) passed all 637 ZERO tests and all 14 corpus cases. Current-source full npm regression remains pending exact-head CI after publication.


## Continued Phase 8: immutable publication recovery after reload

CI 209 passed full npm regression on exact published `467b3b0`, including 637 ZERO tests and 14 corpus cases. The next checkpoint adds twenty-one permanent tests; fifty-five focused tests pass. Eight initial native and seven initial fresh-editor failures are reproduced. A further failing-before case catches recovery refreshing away newly typed private input; recovery now preserves that input and confirms the old outcome without refreshing the editor.

Optional actor-scoped publication keys bind exact input and immutable definition hashes. Native receipt, version, audit and owned event outbox share one transaction. Exact existing versions can gain a new request confirmation without duplicate definition/events. Metadata-only recovery returns the retained immutable version or closes an absent request; a delayed original POST cannot publish afterward. Legacy keyless publication remains compatible, without retrospective request attribution. Missing/corrupt retained results are explicit, receipts are bounded without eviction, and the private journal is excluded from generic Data. Management, module and workflow capability checks remain current after body waits.

The production editor stores only scoped request ID and two hashes, before sending publication. A fresh VM can recover without retaining or replaying the private definition. Eight additional owned rows cover all eight locales. Typed checks bind realm, request and immutable content; retired/forged/denied replies retain evidence. Failed local cleanup cannot undo a known outcome. Unavailable historical results do not block independent drafts; definitive conflicts can be corrected as a new immutable version.

Actual HTTP destroys a successful publication response, restarts encrypted persistence and recovers through a fresh editor. Further HTTP tests prove EIO rollback, terminal closure before completion of an original POST body, actor isolation and current capability/member revocation after authenticated body waits. Recovery does not run or activate workflows; existing automatic publication behavior is unchanged and remains stated in the editor. Existing authoring, activation, execution, access/event, platform and workspace regressions pass. Full frozen-source ZERO and exact-head CI remain pending.

Minimal DOM/fresh VM does not establish actual browser persistence, accessibility, native-speaker or provider acceptance. Locale inventory remains 570 candidates across 47 scripts, with eight on the frozen dashboard. Shared automation controls and wider hard gates remain open, matrix unchanged. No main/production/deployment, real credentials, destructive migration or Run 3.

The frozen publication recovery source `d439fd0b0a5838c507132ba8da543568b4f0c8fb` (tree `0927d9484d4dd80913a2c5fce2cc5804c3b0b8ec`) passed all 658 ZERO tests and all 14 corpus cases. Current-source full npm regression remains pending exact-head CI after publication.


## Continued Phase 8: shared automation lists and owning-view requests

CI 210 passed full npm regression on exact publication-recovery head `5a98d9a`, with 658 ZERO and 14 corpus cases. The next checkpoint adds fourteen permanent tests of the actual shared controller and workflow components. Twenty-nine focused tests and existing manual-run, native recovery UI, ZERO run-client and workspace regressions pass. Nine initial cases fail before implementation; further failures preserve the live-label input-removal and retired ZERO-control defects and their fixes.

Twenty-two owned catalog rows cover list loading, safe invalid/empty feedback, filters, pagination, counts, version/activation, trigger/action/step labels, UTC wake time and retained retry explanations in all eight locales. Names, literal action text, run IDs, input nodes, search text and selected filters remain unchanged. Raw step diagnostics stay in native evidence rather than being rendered as screen copy. Unknown activation/time remains unknown; an empty page with a positive total is not described as an empty catalogue.

An optional current-view predicate now protects the shared request before global response/error handling. A stale access denial cannot retire a newer authorized view. Automation binds section, workspace, query and access generation; old or detached replies cannot fetch further data, repaint current content or adopt conversation state. Retired list and operation controls cannot dispatch native requests. Pending resume cannot duplicate its request. A late ZERO preview cannot restore old controls. Current access denial retains the existing global workspace retirement and safe localized notice.

These are explicit HTTP snapshot/minimal DOM state tests, not real browser or native-provider execution acceptance. Full current-source ZERO and exact-head CI remain pending. The locale inventory is 558 candidates across 47 scripts; the frozen dashboard remains eight. Shared action copy, typed confirmations, uncertain execution/recovery and broader gates stay open. Matrix unchanged; no native API/storage migration, credentials, main/production/deployment or Run 3.

The frozen shared automation readout source `b3c117dac76c79a303c39d21be7cbf41bd96ad8f` (tree `854ad412b982274d2f4ddf8c75b1479607282c43`) passed all 672 ZERO tests and all 14 corpus cases. Current-source full npm regression remains pending exact-head CI after publication.

## Hierarchy controls and acceptance evidence reconciliation — 26 September 2026

Hierarchy authoring now localizes owned controls in eight locales while preserving literal definitions and confirmation. Typed catalog/context validation precedes writable success. PUT is preceded by scoped opaque request metadata; uncertain and reloaded requests recover through the native metadata-only endpoint. Immutable record/definition hashes, current actor/tenant/dealer and terminal proof flags are verified before success. No changed draft is silently replayed. Current denial clears private content; storage failures preserve input and block unsafe writes. Existing node forecast/scenario/snapshot guards remain covered through the actual shared controller.

Frozen source `454cc2e` passes 1176 ZERO tests, 14 corpus cases and fourteen affected native scripts. New permanent coverage comprises 18 integrity, three shared-page and two actual-HTTP client cases, including a fresh VM after encrypted server restart. CI 237 independently verifies the preceding native hierarchy checkpoint on its exact published tree. Full CI for this new control tree remains pending. No real-browser/native-speaker/accessibility/provider acceptance follows from these contract fixtures.

The existing locked acceptance matrix now contains a dated, complete mapping of open gates to remaining code, missing evidence, external dependencies, conditional active-hour ranges and separate waiting time. Passed work and earlier failure evidence are preserved. No global percentage or test-count-derived completion forecast is introduced. The next concrete code defect is exact arithmetic and unknown probability coverage in the shared native Sales summary. Wider Run-2 verdict remains IN_PROGRESS_NOT_ACCEPTED.

## Exact native Sales summary — 26 September 2026

The Sales summary previously lost one cent at the safe integer boundary, represented missing probability as zero and returned unsafe summed cents. It now reuses exact decimal probability arithmetic, distinguishes known subtotals from complete totals, returns null with explicit coverage/overflow metadata where precision or completeness is unavailable, and preserves independent currencies. Current authorization applies before aggregation, including all rows beyond list pagination. Empty sources differ from disabled/unavailable sources. No native opportunity is modified and recorded won amounts are not claimed as accounting revenue.

Source `7326232`/tree `f6a3824` passes 1187 ZERO contracts, all 14 corpus cases and 18 affected native scripts. Eleven new permanent tests include actual HTTP, encrypted restart, source/capability isolation, source changes through the native ZERO tool and exact/unknown/overflow behavior. The existing 105-row test retains its exact known-subtotal proof and now also verifies incompleteness caused by earlier currency-less records. Its original failing assertion is preserved in checkpoint evidence. Full CI for this new source is pending.

Hierarchy controls have meanwhile passed full CI 238 on published `4c5be88`, with exact CI merge tree `167264c`, 1176 ZERO and 14 corpus cases. The existing acceptance matrix has been updated with these scoped results while retaining open work and conditional active-time/waiting-time ranges. Calendar native creation recovery is the next active gap; no broad Run-2 acceptance, external invitation, deployment or main merge is claimed.

## Native Calendar creation recovery — 26 September 2026

Source `8debd3936d18815f371c31e8785de38c9f94c6f8`, tree `56db617e3ecaa9b07d7b0cef226dbc55c16d101e`, passes 1205 ZERO tests, all 14 corpus cases and 16 affected native scripts. The original ZERO invocation exited zero with an empty log and remains unverified; the complete same-source TAP/corpus recheck is the actual proof. Eighteen new permanent cases cover native and ZERO lost-response recovery, encrypted restart, immutable original creation versus current revision, terminal absence/late-body retirement, current authority/deletion, receipt corruption, legacy safety and storage rollback. Full CI for this new Calendar tree is pending.

Sales summary now has full CI 239 PASS: published `a2d9959d918f03a11fad9d66e035697d3a0b25fc`, exact tree `0ee9b15fa712bbc182768d8f915986545792691f`, CI merge `217c907d08740383f7b5346f0976210948f8ca47`, 1187 ZERO and 14 corpus cases. Prior hierarchy and Sales checkpoints remain preserved.

The existing 125-gate matrix retains all 20 open component estimates, with code/evidence/external dependencies and separate active hours/waiting time. New local evidence does not close broad localization, security, business-quality or browser gates. No overall percentage is inferred from test counts. Calendar browser authoring is the next active checkpoint: three shared-page baseline regressions reproduce locale retention, blind replay after an uncertain create, and absent recovery after reload. Continue implementing these controls in the separate preserved worktree. Run 2 remains IN_PROGRESS_NOT_ACCEPTED; no deployment or main merge has occurred.

## Calendar event controls — 26 September 2026

Source `8ba289bf3c5c775d1f10d99579473e550dab2d48`, tree `8c58a1fef47ce3021e5f020e5deb823146b5f86f`, adds 71 explicit keys in eight locales and 23 permanent contracts (18 integrity, three shared-page, two actual-HTTP shared-page). Literal inputs/confirmation survive locale changes; three opaque request fields survive reload; native and ZERO creation recover without replay. Typed/scoped source and immutable result validation, current authority, terminal absence/new review, storage faults and shared navigation guards are enforced. Original creation and current event remain distinct. Browser and server reuse the established time-zone/recurrence calculation. Synthetic DOM/VM and HTTP contracts do not establish real-browser, accessibility, native-speaker or live-provider acceptance.

All 16 affected native scripts pass. The first full ZERO run passed 1227 and failed one existing hierarchy restart HTTP test with `fetch failed`. Both hierarchy tests subsequently pass on unchanged source. The complete same-source recheck passes 1228 ZERO and all 14 corpus cases. Evidence retains the initial failure and the nonconclusive simultaneous resource event; memory pressure is a possible, unproven cause. No timeout or assertion was weakened. Full CI for this event-controls tree is pending.

The preceding native Calendar checkpoint now has full CI 240 PASS on `72875ff759a862911657f00ad9f166d3d366418d`, exact tree `f48394dd102776b944115ce3d143f6c3138cc72b`, CI merge `d71f807ab6b83ddc70970b1b3b62474065f9e7b9`: 1205 ZERO plus 14 corpus cases. The existing 125-gate, 20-open-component matrix is updated with these scopes and retains conditional active-hour bands and separate waiting time. No global completion percentage is introduced.

Continue the separate external Calendar coverage worktree: its baseline proves three real gaps in locale retention, parent navigation/refresh input protection and rejection of observations for a different calendar. Source changes and three initially passing targeted controls are preserved there; it is not yet a frozen or fully verified checkpoint. Wider Run 2 remains IN_PROGRESS_NOT_ACCEPTED.

## External Calendar coverage controls — 26 September 2026

Source `3cac478f84ff8756426d06b4f611fe5b8d9137b3`, tree `6c49be2041284b770d97b1359a19d59b7cd102aa`, passes 1240 ZERO, all 14 corpus cases and 16 native scripts. Twelve new permanent cases cover eight-locale literal input/confirmation retention, parent/tab guards, typed current scope and calendar revision, unknown coverage, uncertain reply/passive status/new confirmation, management permission, provider-wait revocation, source deletion and actual HTTP/encrypted restart. All 29 owned keys have explicit eight-locale entries. Three original UI defects and the deleted-calendar source defect have failing baseline evidence. Live-provider, real-browser, accessibility and native-speaker acceptance remain unverified; the provider is explicitly an isolated fixture.

Calendar event controls now have full CI 241 PASS on published `6b4537983088d855472e875d177e3e86432c8a0a`, exact tree `6f4b8811d642d06dcdd88b69b098ed2922d47cd4`, CI merge `3b39a43a5c725ee192505be25e0d0e4637d49bd4`, with 1228 ZERO and 14 corpus cases. Full CI for the newer external-coverage source is pending. The existing 125-gate matrix retains 20 open work components with evidence, remaining code, external dependencies, active-hour ranges and separate waiting time. No global percentage is added.

The next preserved worktree is Calendar scheduling. Four baseline tests demonstrate deleted calendars/availability/events affecting offers and hidden per-calendar/calendar-selection limits being presented as complete totals. Corrections have started; slot/booking source correctness and broader scheduling UI/recovery are not yet accepted. Continue without rebuilding the completed Calendar, Sales, hierarchy or prior work. Run 2 remains IN_PROGRESS_NOT_ACCEPTED.

## External Calendar production page wiring checkpoint — 26 September 2026

Final inspection found that the production HTML did not load the external client, although the earlier fixtures did. The strengthened actual-HTTP test fails before the fix and now verifies dependency order in the real served HTML as well as the exact asset. This limitation is explicitly attached to the preserved `3cac478` proof. Source `8e217cad01d9123579805c6116bdb4ad191e3ee0`, tree `a571cab61957f174e627d7feacd6dba2eaa9ffca`, then passed all 1240 ZERO cases, 14 corpus cases and 16 affected native scripts on an unchanged tracked tree. Log verification found the module-access log empty after the runner recorded 332 bytes; the cause remains unverified and the original record is retained. That one command was rechecked on the same source into a separately captured, hash-verified log; all other log hashes match. Full CI for the exact next published tree is pending. CI 241 for the earlier Calendar event controls remains PASS.

The existing matrix still has all 125 requirements and 20 genuinely open component groups with remaining code, missing evidence, external dependencies, active-hour ranges and separate waiting time. The conservative text scan is 277 candidates in 62 scripts and is not a progress percentage. Broad language/browser/provider acceptance stays open.

Work continued in the preserved scheduling worktree: eight targeted native cases now pass for deleted calendars/availability/events, booking a freed slot, explicit zero duration/step and the distinction between an incomplete source total and a known result-page total. Their earlier failing evidence remains retained. This is ongoing work, not a frozen or accepted scheduling checkpoint; HTTP/page proof, safe booking recovery and owned eight-locale controls still remain. Run 2 remains IN_PROGRESS_NOT_ACCEPTED.

## Calendar scheduling source observations — 26 September 2026

Frozen source `63a1348d8812ad2cbdf030084443517e81e11ead`, tree `5634acb4d6ef9ae7c6e68d141546e14d5e9efc06`, passes 1252 ZERO tests, 14 corpus cases and 16 affected native scripts, with all log hashes checked. Twelve permanent cases cover source deletion, replacement booking, explicit zero rejection, unknown truncated totals, known paginated totals, current booking rights, stale availability, encrypted restart and actual shared-page limit messages in all eight locales. The UI's separate fifty-row display boundary is explicit. Earlier failures and the corrected Norwegian locale-code fixture error are preserved. Native booking recovery and the rest of the scheduling controls remain open, active work in a separate preserved worktree.

Full CI 242 now passes published `6137a830aa20be2023064e33adb5cadefbaaa901`, exact tree `9c29d38c655b443bc3787642b99015db3e9647d8`, merge `fc48c7f626c215e6dafc6cdb35be601ad57298aa`, with 1240 ZERO and 14 corpus cases. It ran from 07:07:37 to 07:19:02 UTC. Full CI for this newer scheduling checkpoint is pending. The existing matrix preserves all 125 gates, all earlier work, 20 open groups, justified active-hour ranges and distinct waiting time. No global test-based percentage is used; Run 2 remains IN_PROGRESS_NOT_ACCEPTED.

## Native Calendar booking recovery — 26 September 2026

Source `b15458ca87af69ef623fe4f348661c2ae847d4d2`, tree `d3b7ca85eb6081b333a794f3132454cefc866ff2`, passes all 1267 ZERO tests, 14 corpus cases and 16 affected native scripts with verified log hashes. Fifteen new permanent tests prove immutable original booking snapshots, separate later current events, current actor/source rights, bounded metadata, terminal absent-request closure, legacy unverifiability, capacity and persistence rollback. Actual HTTP destroys a committed reply, restarts encrypted storage, holds a delayed original request body and revokes an authenticated session during recovery. The existing native idempotency error contract was restored after an introduced mismatch. A fixture expectation was corrected to require 401 for the revoked session and 403 for a fresh viewer; no authority check was weakened. Earlier failures remain recorded.

Full CI 243 passed published `8dbe135f9f4bf5cdadc4d820f20a0afacc808deb`, exact tree `da17f4fafa1d52b6be1e97f711cf4b4cd8a08a09`, merge `6a86e55d788d4bde6ac72349381e734207fcfab2`, with 1252 ZERO/14 corpus. It ran 07:24:40–07:35:38 UTC. Full exact-tree CI for the newer booking source is pending. The scheduling-controls worktree is preserved and active: its typed client-state prototype passes five cases, while actual controls, eight-locale copy, HTTP/page integration and reload recovery remain unaccepted. The 125-gate matrix retains 20 open groups, evidence-based active-hour ranges and separate waiting time, without test-count percentages. Run 2 remains IN_PROGRESS_NOT_ACCEPTED.

## Scheduling controls, reviewed calendar identity and deleted reminders — 26 September 2026

Source `920afec94ab174aaf507c42d52240c4a095ee941`, tree `1013a97c8dca366c180ef9dcaffef82dde59a273`, passes all 1290 ZERO tests, 14 corpus cases and 16 affected native scripts with checked raw-log hashes and unchanged source throughout. The earlier scheduling source `984cb292dc1a9611ba4be186feaab814d12395f4` separately passed 1285 ZERO/14 corpus/16 native; it remains recorded alongside the subsequently discovered gaps. The 23 new cases since the booking checkpoint cover typed native observations, eight-locale owned controls, scoped metadata-only recovery, input/identity preservation, authorization/storage failure, actual served page/assets, lost reply plus encrypted restart, literal target-calendar name/revision and deleted-reminder batch behavior. Names and revisions are rechecked before booking. Deleted reminder rows cannot emit a notification or consume the bounded batch ahead of valid reminders. Three calendar-identity cases and two corrected original-source reminder cases fail before the fixes and pass after them; earlier invalid-owner fixture errors are explicitly not counted as bug evidence.

Full CI 244 passed published `12d02cf8c52bb0a74ae1f9108abf8e95dfffcd13`, exact tree `ad91ea06bd595c0a16e52e437fd1688c334024d7`, merge `03f0fd13b5c76b1b8f9d5cbc5136c94e34dc85e4`, with 1267 ZERO/14 corpus, from 2026-09-26T07:46:30Z to 2026-09-26T07:57:38Z. Full CI for this newer reviewed tree remains pending. The existing 125-gate matrix retains all 20 genuinely open groups, each with remaining code, missing evidence, dependencies and evidence-based active-hour/waiting ranges. The refreshed conservative inventory has 270 candidates in 64 scripts and remains nonexhaustive. Procurement clarifications continue in their own preserved worktree. No global percentage or test-count ETA is introduced. DOM/VM/HTTP do not establish real-browser, accessibility, native-speaker or live-provider acceptance. Run 2 remains IN_PROGRESS_NOT_ACCEPTED.

## Procurement clarification controls and live source deletion — 26 September 2026

Source `3db0f1fd6548641af61199959fe0e74ef92c66da`, tree `c261d972427603de6a2d1907d48c497c94bd942b`, passes 1303 ZERO tests, 14 corpus cases and 16 affected native scripts with verified raw logs and unchanged source. Thirteen new cases cover actual workspace entry and eight-locale ownership, literal source/input/control preservation, exact native/ZERO confirmation identity after lost replies, safe errors, stale searches, typed missing collaborators and live deletion boundaries. Actual HTTP preserves the same mounted confirmation across encrypted SERVER restart and checks real page assets/order; it does not establish browser reload recovery or immutable original/current Procurement action outcomes. Existing authorized retained-export semantics remain intact. The failed baselines, corrected undefined-key fixture comparison and empty initial ZERO-client capture remain explicit; complete frozen native/ZERO runs pass.

Full CI 245 passed published `575ed85391a38283be60fe9c497db20d00b7bf2f`, tree `09661d98bc50de26f5dc49748aa7d3acb4c4ffec`, merge `b346efa2919cd1846e79ed14a18e898d20e4df4c`, with 1290 ZERO/14 corpus (2026-09-26T08:12:26Z–2026-09-26T08:23:35Z). Full CI for this newer Procurement tree remains pending. All 125 gates and the 20 open component estimates remain, with active work and wait times separate; 243 scan candidates/64 scripts are nonexhaustive and not a progress measure. Continue the preserved Procurement-intelligence worktree for costs, alternatives and reported outcomes. Run 2 remains IN_PROGRESS_NOT_ACCEPTED.

## Procurement costs, reported outcomes and current-source boundaries — 26 September 2026

UI source `ba4beddfee78f4beedfc52bda42aea75864f49a1`/tree `86f49cf82fb335d919895f213690f6d8a3bf89a5` passes 1321 ZERO/14 corpus/16 native. The subsequent source `f90f2910a28411f22efe8fdcbdb168947d8e6bca`/tree `070d6cf8701a20c839ac84cb09230f7d05e85677` passes 1328 ZERO/14 corpus/18 native with unchanged source and verified logs. Eighteen new UI/HTTP cases prove owned eight-locale cost/outcome controls, literal input/source preservation, exact large-cent prefill and correction, negative margins, explicit unknown values, native/ZERO pending request identity, late/detached guards, real served assets and encrypted SERVER restart. Seven further native cases require non-deleted current sources and prevent old observations from reappearing after deletion of the latest one. Authorized historical export retains its prior semantics. Five failing deleted-source baselines remain visible alongside the earlier green UI run. None of these claims establishes durable browser-reload recovery, immutable original/current Procurement receipts, a dedicated mid-body source-deletion test, real-browser/accessibility/native-speaker or live-provider acceptance.

Full CI 246 passed `53b5b55fa0fe6459a40aa10a2881100f23e42c53`/tree `e009bf430ac243c0f3b1c4128637e309165a7e3e`, merge `9b15b9164a0eb9ea78445733b87a0c6f980f926c`, with 1303 ZERO/14 corpus (2026-09-26T08:33:51Z–2026-09-26T08:46:32Z). New-tree full CI remains pending. The 125-gate matrix retains 20 open groups, concrete remaining work and separate active-hour/waiting bands. The scan now records 216 candidates/64 scripts and stays nonexhaustive. Continue shared Procurement bid/allocation/approval controls and remaining generated/backend copy. No test-count percentage or total-date extrapolation is introduced. Run 2 remains IN_PROGRESS_NOT_ACCEPTED.

## Procurement proposals, decisions and current approval sources — 26 September 2026

Shared control source `cadf691aa418ecf6d0aeecb7fe755f472c7390c9` / tree `b0a7aab43b76ce2eb15a22877d51ff874f2e6d74` passes 1348 ZERO, 14 corpus and 19 native scripts on an unchanged source. Twenty new tests (nineteen actual-controller DOM cases and one real HTTP case) cover eight-locale owned copy, literal input, exact cent amounts, cumulative allocation basis, single-request retry identity, competing-action/draft/late-response guards and two encrypted SERVER restarts. A review-stage multi-record draft failure is preserved and fixed. Eight subsequent native cases in `8255b50ccc08dd06156885fe49a3d2e76918d3ee` / tree `c4e1b473edd83e8c9f17b63508362754e1c81385` pass the wider frozen 1356 ZERO/14 corpus/19 native run and reject deleted current approval sources. Historical approved quantities continue to consume capacity and authorized export remains unchanged. Seven native baseline failures remain visible; the earlier green UI run is not presented as proof of those later fixes.

Full CI 247 passed `569079a602c0ebc5b614eef442a12545232c88dc` / tree `f45162704406ba012f046ea252abcd2290309d80`, merge `a79e8f1d61f3e358216570e33dc3dc605541cd97`, with 1328 ZERO/14 corpus (2026-09-26T09:04:08Z–2026-09-26T09:15:02Z). New-tree full CI is pending. The existing 125-gate matrix retains twenty open groups and separate active-hour/waiting bands; no global percentage or test-derived ETA is introduced. The scan records 188 candidates/64 scripts and is nonexhaustive. Durable Procurement recovery, original/current receipts and non-execution retirement are remaining codework; real-browser/accessibility/native-speaker/live-provider proof remains separate. Continue directly with the recovery contract. Run 2 stays IN_PROGRESS_NOT_ACCEPTED.

## Native Procurement action recovery — 26 September 2026

Source `a124247c85310fbb16a5a13f63a0a30be048aa91` / tree `883692eac2edf0c8eacd0e6e345029fbee33c22c` passes a frozen 1377 ZERO/14 corpus/19 native scripts with verified complete logs. Twenty-one new cases (sixteen native/direct ZERO, five actual HTTP) cover atomic original action snapshots, distinct current results, exact replay, metadata-only recovery, durable non-execution, current actor/source checks, corrupted/missing evidence, capacity, legacy compatibility, encrypted HTTP restart, destroyed committed replies, final-body-byte races, storage rollback and mid-body membership withdrawal. Native fixture restart is not encryption evidence; an early label was corrected and remains honestly described in the log inventory. Browser controls are currently being connected in a separate preserved worktree and are not accepted by this native checkpoint. Other Procurement action families still require the durable protocol.

Full CI 248 passed `ef56112dbf5c0e4d187e0f7b08459a5038bfd8a6` / tree `a9e6ca2ab8377b97310d8b9a36fc5590e4de07b6`, merge `06e37823b8402d7767f758aa2c81ce9c7dabd11b`, with 1356 ZERO/14 corpus (2026-09-26T09:32:39Z–2026-09-26T09:45:29Z). CI for the new source remains pending. All 125 gates and twenty open work groups remain; active execution and unattended/external waiting are separate and no percentage or test-count ETA is introduced. Continue directly with browser recovery controls and remaining gates. Run 2 stays IN_PROGRESS_NOT_ACCEPTED.
