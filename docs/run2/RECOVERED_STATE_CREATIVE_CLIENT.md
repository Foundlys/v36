# Same Run 2: creative revision client recovery and presentation

Frozen source `7d0098b6d04e33aedd0d0b6f0437b421bcb29ce7`, tree `4cdf64326434b8ae90237d3441d2bae4aa0ced00`: all 845 ZERO tests, 14 corpus cases and eight affected native commands PASS. Nineteen new permanent tests exercise the real controller and shared workspace page, including native and ZERO HTTP lost completed replies, encrypted server restart and a fresh client instance. Full logs and hashes are recorded in the checkpoint manifest. Published commit `e019809d944138b25abd84c00f503132f8dcb5e2`, tree `1569d9455725eac41f501645557ce5665d51f4fe`, has complete full CI 223 PASS (run `36162248536`, job `108161391309`). CI merge `e87bf64e9a5980f791395288553e9302cee823b9` has the exact same tree; the complete log confirms 845 ZERO tests and all 14 corpus cases.

Only exact request metadata is stored in actor/tenant/dealer-scoped browser session storage. A changed input cannot become a retry of an uncertain write. Recovery identifies the original applied snapshot and any newer current record separately; an absent request is durably closed by the native service. Strict acknowledgement, context, history and snapshot proof checks reject contradictory or incomplete success. Form fields, reasons and literal source content survive all eight supported locale changes.

The shared page exposes scoped pending recovery even when the original record is outside the visible page. Dirty, busy and uncertain details cannot be silently discarded; closing a clean detail retires its view so reopening performs a current authorized read. Current access denial clears private content but keeps the metadata needed for later reconciliation. Delayed replies and old controls cannot restore a retired view. Storage failures, cleanup failures and read failures after a proven save do not become duplicate writes or invented success.

Preserved failed evidence includes the original 12 missing-behavior failures, the temporary missing locale keys, and two additional regressions: accepting an incomplete superseded snapshot and retaining private history on a clean close/reopen. Both product defects were fixed without weakening assertions. Native version/history/restore, review, ZERO transport, module access and shared workspace regressions pass.

Shared creative review commands, journeys and the other genuinely open production localization/state gates are the next continuation. Fixture DOM evidence does not establish browser layout or responsive acceptance. No wider matrix row is promoted by this bounded checkpoint; no main merge, deployment, real credentials or Run 3 action.
