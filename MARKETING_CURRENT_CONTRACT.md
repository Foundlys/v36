# Current Marketing continuation

Baseline PR #15: Finance `276a7a2c14bd7aa685c73ee7cc7128b9ac84fb75`, tree `9e888cffdfba67e4cdf549ddfd31e9a6775acf31`, CI 126 PASS.

Creative version/history captures only actually retained source snapshots. A legacy baseline is labelled as observed at upgrade; earlier history is not invented. Every native edit and approval records an immutable snapshot. Explicit edits/restores require current parent access, exact revision, separate reason/confirmation and stable action ID. Restoring a draft creates a new DRAFT revision. Changing an approved creative makes a separate draft and preserves the approved original. Approval, publication state and reviewer authority are never inherited. Current parent and derivation-source access govern records, history and export. Hash/duplicate/foreign snapshot checks, bounded history, atomic rollback, exact replay and encrypted HTTP restart are tested. Native and typed ZERO share the same services and preserve uncertain requests and unsaved input.

Marketing overview previously summed attributed revenue and full revenue together over all retained events and counted raw attribution rows without scoped evidence. It now uses a strict explicit half-open window, current canonical event permissions and bounded typed field projection. Currency groups are separate; no cross-currency monetary total is exposed. The default overview window is the last 30 days and is returned in its details. Native Measurement/Attribution controls accept explicit periods/source/campaign filters and support fingerprint-bound paginated source drilldown. Future periods are observed only through the current instant, with incomplete-period status.

The bounded recorded metric contract is explicit: `metric_semantics=DELTA` identifies increments; `PERIOD_TOTAL` additionally requires exact `metric_period_from` and `metric_period_to` matching the query. Multiple period totals or mixtures with deltas in one source/provider/campaign/currency scope are unavailable, not summed. Missing/malformed amounts and unsafe aggregate totals are unavailable. Currency is required. Raw ad events do not prove complete exposure counts.

Cross-source financial reconciliation requires recorded `financial_outcome_namespace`, `financial_outcome_id` and `financial_outcome_complete=true`. Within that explicitly linked outcome, `invoice_paid` supersedes `purchase_completed`, which supersedes `deal_won`. Equal terminal observations count once; conflicting terminal amounts or currencies are unresolved. Monetary field precedence is attributed revenue, then revenue, then value; zero is preserved. These are user/provider-recorded semantics, not independent proof of completeness, settlement or causality. Missing links are not inferred from customers or anonymous IDs. Unknown inputs cannot produce a trustworthy aggregate. Source-bound drilldown explains selected, superseded, duplicate and unresolved records. Lead IDs are namespaced by source; unknown completeness is never represented as full external coverage.

Typed ZERO retains source-free references and recomputes under current rights on replay. Actual member HTTP, encrypted restart, mid-body revocation, native services and production synthetic DOM tests cover the contract. Synthetic DOM is not browser acceptance. No provider publishing/activation, legal consent validation, production change or comparative superiority is claimed.

Journey execution/audience activation remain open, as do recorded Procurement gaps. Phase 24 is ACTIVE. The new regression receipt and source manifest are recorded after the run in MARKETING_CURRENT_EVIDENCE.json.
