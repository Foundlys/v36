'use strict';

const crypto=require('crypto');
const {executeWorkflow,validateWorkflow}=require('./workflow-execution');
const {scopedMutation}=require('./scoped-mutation');
const {queueOwnedEvent,flushOwnedEvents}=require('./module-event-outbox');

const PLATFORM_SCHEMA_VERSION=5;
const EVENT_VERSION=1;
const INGEST_VERSION=1;
const FACT_OWNER=Symbol('foundlyFactOwner');
const KNOWLEDGE_TYPES=new Set(['RAW_SOURCE','OBSERVATION','NORMALIZED_FACT','DERIVED_INSIGHT','USER_PREFERENCE','MODEL_HYPOTHESIS']);
const PRIVACY_CLASSES=new Set(['PUBLIC','INTERNAL','PSEUDONYMOUS','PERSONAL','SENSITIVE','FINANCIAL']);
const CONNECTOR_STATES=new Set(['UNCONFIGURED','CONFIGURED','AUTHORIZING','AUTHENTICATED','PROBING','SYNCING','CONNECTED','DEGRADED','ERROR','EXPIRED','DISCONNECTED']);
const FRESHNESS=new Set(['REALTIME','NEAR_REALTIME','BATCH','STALE','UNKNOWN']);
const LEGAL_FORMS=['EENMANSZAAK','VOF','MAATSCHAP','CV','BV','NV','COOPERATIE','STICHTING','VERENIGING','HOLDING','WERKMAATSCHAPPIJ','FISCALE_EENHEID'];
const TAX_DOMAINS=['BTW','INKOMSTENBELASTING','VENNOOTSCHAPSBELASTING','LOONHEFFINGEN','DIVIDENDBELASTING','ICP_EU_VAT','KOR','IMPORT_EXPORT_VAT','FISCAL_CORRECTIONS'];
const DATA_SOURCE_KINDS=new Set(['USER_INPUT','INTERNAL_SERVICE','EXTERNAL_PROVIDER','WEB_RESEARCH','DOCUMENT','OFFLINE_CLIENT','DERIVED_INTELLIGENCE']);
const SYNC_STATES=new Set(['LOCAL','PENDING','SYNCED','CONFLICT','ERROR','ARCHIVED']);
const AUTOMATION_TRIGGERS=new Set(['new_lead','stage_changed','no_response','appointment','invoice_overdue','payment','marketing_threshold','forecast_anomaly','tax_deadline','connector_state','custom_event','schedule']);
const AUTOMATION_ACTIONS=new Set(['create_task','assign','notify','approved_communication','update_field','move_pipeline','create_document','create_invoice_draft','run_analysis','webhook','zero_task','approval_request','financial_posting','payment','filing','critical_setting','delay']);
const HIGH_RISK_AUTOMATION_ACTIONS=new Set(['approved_communication','webhook','financial_posting','payment','filing','critical_setting']);
const META_STANDARD_EVENTS=Object.freeze({page_view:'PageView',view_content:'ViewContent',lead_created:'Lead',contact_created:'Contact',registration_completed:'CompleteRegistration',appointment_scheduled:'Schedule',purchase_completed:'Purchase',deal_won:'Purchase',invoice_paid:'Purchase'});
const GOOGLE_EVENT_NAMES=Object.freeze({page_view:'page_view',view_content:'view_item',lead_created:'generate_lead',contact_created:'generate_lead',registration_completed:'sign_up',appointment_scheduled:'schedule_appointment',purchase_completed:'purchase',deal_won:'purchase',invoice_paid:'purchase'});

const KPI_DEFINITIONS=Object.freeze({
  conversion_rate:{id:'conversion_rate',name:'Conversion rate',version:1,formula:'lead_created / session_started * 100',inputs:['lead_created','session_started'],dimensions:['campaign_id','source','medium'],time_grain:'DAY',currency_behavior:'NONE',null_behavior:'NULL_WHEN_DENOMINATOR_ZERO',freshness_requirement:'REALTIME',effective_from:'2026-01-01'},
  win_rate:{id:'win_rate',name:'Win rate',version:1,formula:'deal_won / (deal_won + deal_lost) * 100',inputs:['deal_won','deal_lost'],dimensions:['owner_id','pipeline_id','campaign_id'],time_grain:'DAY',currency_behavior:'NONE',null_behavior:'NULL_WHEN_DENOMINATOR_ZERO',freshness_requirement:'NEAR_REALTIME',effective_from:'2026-01-01'},
  cpl:{id:'cpl',name:'Cost per lead',version:1,formula:'marketing_spend_cents / lead_created',inputs:['marketing_spend_cents','lead_created'],dimensions:['campaign_id','provider'],time_grain:'DAY',currency_behavior:'MINOR_UNITS',null_behavior:'NULL_WHEN_DENOMINATOR_ZERO',freshness_requirement:'BATCH',effective_from:'2026-01-01'},
  cac:{id:'cac',name:'Customer acquisition cost',version:1,formula:'marketing_spend_cents / customer_acquired',inputs:['marketing_spend_cents','customer_acquired'],dimensions:['campaign_id','source'],time_grain:'DAY',currency_behavior:'MINOR_UNITS',null_behavior:'NULL_WHEN_DENOMINATOR_ZERO',freshness_requirement:'BATCH',effective_from:'2026-01-01'},
  roas:{id:'roas',name:'Return on ad spend',version:1,formula:'attributed_revenue_cents / marketing_spend_cents',inputs:['attributed_revenue_cents','marketing_spend_cents'],dimensions:['campaign_id','provider'],time_grain:'DAY',currency_behavior:'RATIO_SAME_CURRENCY',null_behavior:'NULL_WHEN_DENOMINATOR_ZERO',freshness_requirement:'BATCH',effective_from:'2026-01-01'},
  mer:{id:'mer',name:'Marketing efficiency ratio',version:1,formula:'revenue_cents / marketing_spend_cents',inputs:['revenue_cents','marketing_spend_cents'],dimensions:['source','medium'],time_grain:'DAY',currency_behavior:'RATIO_SAME_CURRENCY',null_behavior:'NULL_WHEN_DENOMINATOR_ZERO',freshness_requirement:'BATCH',effective_from:'2026-01-01'},
  pipeline_value:{id:'pipeline_value',name:'Pipeline value',version:1,formula:'sum(open_deal_value_cents)',inputs:['open_deal_value_cents'],dimensions:['pipeline_id','stage_id','owner_id'],time_grain:'INSTANT',currency_behavior:'MINOR_UNITS',null_behavior:'ZERO',freshness_requirement:'NEAR_REALTIME',effective_from:'2026-01-01'},
  weighted_pipeline:{id:'weighted_pipeline',name:'Weighted pipeline',version:1,formula:'sum(open_deal_value_cents * probability)',inputs:['open_deal_value_cents','deal_probability'],dimensions:['pipeline_id','owner_id'],time_grain:'INSTANT',currency_behavior:'MINOR_UNITS',null_behavior:'ZERO',freshness_requirement:'NEAR_REALTIME',effective_from:'2026-01-01'},
  gross_margin:{id:'gross_margin',name:'Gross margin',version:1,formula:'revenue_cents - cost_of_goods_cents',inputs:['revenue_cents','cost_of_goods_cents'],dimensions:['product_id','campaign_id'],time_grain:'MONTH',currency_behavior:'MINOR_UNITS',null_behavior:'ZERO',freshness_requirement:'BATCH',effective_from:'2026-01-01'},
  net_margin:{id:'net_margin',name:'Net margin',version:1,formula:'operating_result_cents / revenue_cents * 100',inputs:['operating_result_cents','revenue_cents'],dimensions:['legal_entity_id'],time_grain:'MONTH',currency_behavior:'PERCENT',null_behavior:'NULL_WHEN_DENOMINATOR_ZERO',freshness_requirement:'BATCH',effective_from:'2026-01-01'},
  sales_cycle:{id:'sales_cycle',name:'Sales cycle',version:1,formula:'average(sales_cycle_days)',inputs:['sales_cycle_days'],dimensions:['owner_id','pipeline_id'],time_grain:'MONTH',currency_behavior:'NONE',null_behavior:'NULL_WHEN_EMPTY',freshness_requirement:'BATCH',effective_from:'2026-01-01'},
  response_time:{id:'response_time',name:'Response time',version:1,formula:'average(response_time_seconds)',inputs:['response_time_seconds'],dimensions:['owner_id','channel'],time_grain:'DAY',currency_behavior:'NONE',null_behavior:'NULL_WHEN_EMPTY',freshness_requirement:'REALTIME',effective_from:'2026-01-01'},
  follow_up_rate:{id:'follow_up_rate',name:'Follow-up rate',version:1,formula:'follow_up_completed / follow_up_due * 100',inputs:['follow_up_completed','follow_up_due'],dimensions:['owner_id'],time_grain:'DAY',currency_behavior:'NONE',null_behavior:'NULL_WHEN_DENOMINATOR_ZERO',freshness_requirement:'NEAR_REALTIME',effective_from:'2026-01-01'},
  dso:{id:'dso',name:'Days sales outstanding',version:1,formula:'receivables_cents / credit_revenue_cents * period_days',inputs:['receivables_cents','credit_revenue_cents','period_days'],dimensions:['legal_entity_id'],time_grain:'MONTH',currency_behavior:'RATIO_SAME_CURRENCY',null_behavior:'NULL_WHEN_DENOMINATOR_ZERO',freshness_requirement:'BATCH',effective_from:'2026-01-01'}
});

const ANALYSIS_DASHBOARD=Object.freeze({id:'foundly-analysis-command-dashboard',name:'Foundly Analysis Command Dashboard',preset:'ANALYSIS',widgets:[['realtime','EVENT_FEED'],['conversion_rate','KPI'],['win_rate','KPI'],['roas','KPI'],['gross_margin','KPI'],['commercial_funnel','FUNNEL'],['attribution','TABLE'],['sales_trend','LINE'],['source_freshness','STATUS'],['automation_health','STATUS']]});

const TAX_SOURCES=Object.freeze({
  vat_rates:{authority:'Belastingdienst',url:'https://www.belastingdienst.nl/wps/wcm/connect/bldcontentnl/belastingdienst/zakelijk/btw/tarieven_en_vrijstellingen/tarieven_en_vrijstellingen',verified_at:'2026-09-04',description:'Algemeen 21%, verlaagd 9%, nul 0% en vrijstelling afhankelijk van prestatie.'},
  invoice_requirements:{authority:'Belastingdienst',url:'https://www.belastingdienst.nl/wps/wcm/connect/bldcontentnl/belastingdienst/zakelijk/btw/administratie_bijhouden/facturen_maken/factuureisen/factuureisen',verified_at:'2026-09-04'},
  retention:{authority:'Belastingdienst',url:'https://www.belastingdienst.nl/wps/wcm/connect/bldcontentnl/belastingdienst/zakelijk/ondernemen/administratie/hoelang-moet-u-gegevens-bewaren',verified_at:'2026-09-04',basis_years:7,real_estate_and_oss_years:10},
  corporate_tax_2026:{authority:'Belastingdienst',url:'https://www.belastingdienst.nl/wps/wcm/connect/bldcontentnl/belastingdienst/zakelijk/winst/vennootschapsbelasting/veranderingen-vennootschapsbelasting-2026/veranderingen-vennootschapsbelasting-2026',verified_at:'2026-09-04',description:'2026 VPB-tarieven: 19% tot en met EUR 200.000 belastbaar bedrag en 25,8% daarboven; overige fiscale correcties blijven reviewplichtig.'}
});

const BUILTIN_TAX_RULES=Object.freeze([
  {rule_id:'nl-btw-standard-2025',jurisdiction:'NL',tax_type:'BTW',legal_forms:LEGAL_FORMS,effective_from:'2025-01-01',effective_until:'2025-12-31T23:59:59.999Z',version:'2025.1',source:TAX_SOURCES.vat_rates,parameters:{rate:21,classification:'STANDARD'},verification_status:'VERIFIED',last_verified_date:'2026-09-04'},
  {rule_id:'nl-btw-low-2025',jurisdiction:'NL',tax_type:'BTW',legal_forms:LEGAL_FORMS,effective_from:'2025-01-01',effective_until:'2025-12-31T23:59:59.999Z',version:'2025.1',source:TAX_SOURCES.vat_rates,parameters:{rate:9,classification:'REDUCED'},verification_status:'VERIFIED',last_verified_date:'2026-09-04'},
  {rule_id:'nl-btw-zero-2025',jurisdiction:'NL',tax_type:'BTW',legal_forms:LEGAL_FORMS,effective_from:'2025-01-01',effective_until:'2025-12-31T23:59:59.999Z',version:'2025.1',source:TAX_SOURCES.vat_rates,parameters:{rate:0,classification:'ZERO'},verification_status:'VERIFIED',last_verified_date:'2026-09-04'},
  {rule_id:'nl-btw-standard-2026',jurisdiction:'NL',tax_type:'BTW',legal_forms:LEGAL_FORMS,effective_from:'2026-01-01',effective_until:'2026-12-31T23:59:59.999Z',version:'2026.1',source:TAX_SOURCES.vat_rates,parameters:{rate:21,classification:'STANDARD'},verification_status:'VERIFIED',last_verified_date:'2026-09-04'},
  {rule_id:'nl-btw-low-2026',jurisdiction:'NL',tax_type:'BTW',legal_forms:LEGAL_FORMS,effective_from:'2026-01-01',effective_until:'2026-12-31T23:59:59.999Z',version:'2026.1',source:TAX_SOURCES.vat_rates,parameters:{rate:9,classification:'REDUCED'},verification_status:'VERIFIED',last_verified_date:'2026-09-04'},
  {rule_id:'nl-btw-zero-2026',jurisdiction:'NL',tax_type:'BTW',legal_forms:LEGAL_FORMS,effective_from:'2026-01-01',effective_until:'2026-12-31T23:59:59.999Z',version:'2026.1',source:TAX_SOURCES.vat_rates,parameters:{rate:0,classification:'ZERO'},verification_status:'VERIFIED',last_verified_date:'2026-09-04'}
].map(rule=>Object.freeze({...rule,rule_version:rule.version,official_source:rule.source.authority,source_reference:rule.source.url,last_verified_at:rule.last_verified_date})));

const CAPABILITY_PACKS=Object.freeze({
  AUTOMOTIVE:{modules:['crm','analysis','finance','inventory','procurement','sales','marketing','automation','data'],pipelines:['VEHICLE_SALES','PROCUREMENT'],kpis:['conversion_rate','win_rate','pipeline_value','weighted_pipeline','gross_margin','sales_cycle'],connectors:['rdw','mobile_de','marktplaats','meta','google_ads'],roles:['ADMIN','MANAGER','SALES','ACCOUNTANT'],workspaces:['/automotive'],zero_tools:['crm_customer_360','crm_inventory_customer_matches','analysis_kpi','finance_report','knowledge_search','automotive_search','automotive_comparables','automotive_economics','automotive_candidate_analysis','automotive_today']},
  PROFESSIONAL_SERVICES:{modules:['crm','analysis','finance','projects','calendar','marketing','automation','data'],pipelines:['CONSULTATIVE_SALES','DELIVERY'],kpis:['conversion_rate','win_rate','pipeline_value','weighted_pipeline','gross_margin','response_time','dso'],connectors:['google_calendar','email','exact_online'],roles:['ADMIN','MANAGER','SALES','ACCOUNTANT','CONSULTANT'],zero_tools:['crm_customer_360','analysis_kpi','finance_report','knowledge_search']},
  ECOMMERCE:{modules:['crm','analysis','finance','catalog','orders','marketing','automation','data'],pipelines:['ECOMMERCE_CUSTOMER_LIFECYCLE'],kpis:['conversion_rate','cac','roas','mer','gross_margin'],connectors:['shopify','meta','meta_pixel','meta_capi','ga4','google_ads','mollie'],roles:['ADMIN','MANAGER','MARKETING','ACCOUNTANT','SUPPORT'],zero_tools:['crm_customer_360','analysis_kpi','finance_report','knowledge_search']},
  GENERAL_B2B:{modules:['crm','analysis','finance','marketing','automation','data'],pipelines:['B2B_SALES'],kpis:['conversion_rate','win_rate','pipeline_value','weighted_pipeline','gross_margin','dso'],connectors:['email','google_calendar'],roles:['ADMIN','MANAGER','SALES','ACCOUNTANT'],zero_tools:['crm_customer_360','analysis_kpi','finance_report','knowledge_search']},
  REAL_ESTATE:{modules:['crm','analysis','finance','properties','marketing','automation','data'],pipelines:['PROPERTY_SALES'],kpis:['conversion_rate','win_rate','pipeline_value','response_time'],connectors:['meta','ga4','google_ads'],roles:['ADMIN','MANAGER','SALES','ACCOUNTANT'],zero_tools:['crm_customer_360','analysis_kpi','finance_report','knowledge_search']}
});

const ROLE_PERMISSIONS=Object.freeze({
  ADMIN:['*'],
  MANAGER:['platform:read','events:write','events:read','analysis:read','knowledge:write','knowledge:read','learning:write','connectors:manage','automation:manage','provision:manage','export:run'],
  ANALYST:['platform:read','events:read','analysis:read','knowledge:read'],
  MARKETING:['platform:read','events:write','events:read','analysis:read','knowledge:read'],
  SALES:['platform:read','events:write','events:read','analysis:read','knowledge:read'],
  ACCOUNTANT:['platform:read','events:write','events:read','analysis:read','knowledge:read'],
  VIEWER:['platform:read','events:read','analysis:read','knowledge:read']
});

function platformError(status,code,message,details){const error=new Error(message);error.statusCode=status;error.code=code;if(details!==undefined)error.details=details;return error}
function clone(value){return value===undefined?undefined:JSON.parse(JSON.stringify(value))}
function digest(value){return crypto.createHash('sha256').update(String(value)).digest('hex')}
function safeId(value){const id=String(value||'').trim();return /^[A-Za-z0-9][A-Za-z0-9_.:-]{0,199}$/.test(id)?id:null}
function text(value,max=4000){return String(value??'').replace(/[\u0000-\u001f\u007f]/g,' ').trim().slice(0,max)}
function iso(value){const date=new Date(value);return Number.isNaN(date.getTime())?null:date.toISOString()}
function finite(value,fallback=0){const number=Number(value);return Number.isFinite(number)?number:fallback}
function contextShape(value={}){const tenant_id=safeId(value.tenant_id),dealer_id=safeId(value.dealer_id||'default');if(!tenant_id||!dealer_id)throw platformError(400,'platform_context_invalid','Geldige tenant- en businesscontext is verplicht');return {tenant_id,dealer_id}}
function principalShape(value={}){const roles=(Array.isArray(value.roles)?value.roles:[value.role||'VIEWER']).map(item=>String(item).toUpperCase()).slice(0,10),permissions=new Set([...(Array.isArray(value.permissions)?value.permissions:[]),...roles.flatMap(role=>ROLE_PERMISSIONS[role]||[])]);return {id:safeId(value.id)||'anonymous',roles,permissions,team_ids:(Array.isArray(value.team_ids)?value.team_ids:[]).map(safeId).filter(Boolean)}}
function eventReadable(event,principal){
  if(!principal||principal.permissions?.has('*'))return true;
  const access=event.permissions||{},roles=Array.isArray(access.roles),users=Array.isArray(access.user_ids);
  return !roles&&!users||roles&&access.roles.some(role=>principal.roles.includes(role))||users&&access.user_ids.includes(principal.id);
}
function requirePermission(principal,permission){if(!principal.permissions.has('*')&&!principal.permissions.has(permission))throw platformError(403,'platform_forbidden','Deze platformactie is niet toegestaan voor de actieve rol')}
function sanitize(value,key='',depth=0){
  if(depth>8)throw platformError(400,'platform_payload_too_deep','Platformpayload is te diep genest');
  if(value===null||value===undefined||typeof value==='boolean')return value??null;
  if(typeof value==='number')return Number.isFinite(value)?value:null;
  if(typeof value==='string')return text(value,12000);
  if(Array.isArray(value))return value.slice(0,500).map(item=>sanitize(item,key,depth+1));
  if(typeof value==='object'){
    const out={};for(const [raw,item] of Object.entries(value).slice(0,500)){if(!/^[a-z][a-z0-9_]{0,79}$/i.test(raw)||['__proto__','prototype','constructor'].includes(raw))continue;if(/(?:password|secret|token|authorization|cookie|private_key|api_key)$/i.test(raw)&&item)throw platformError(400,'platform_secret_rejected','Secrets horen niet in platformrecords');out[raw]=sanitize(item,raw,depth+1)}return out;
  }
  return null;
}
function average(values){const rows=values.filter(Number.isFinite);return rows.length?rows.reduce((a,b)=>a+b,0)/rows.length:null}
function day(value){const at=iso(value);return at?at.slice(0,10):null}
function dateRange(rows,from,to){const start=from?Date.parse(from):null,end=to?Date.parse(to):null;return rows.filter(row=>(start===null||Date.parse(row.occurred_at)>=start)&&(end===null||Date.parse(row.occurred_at)<=end))}
function normalizeEmail(value){let email=String(value||'').trim().toLowerCase().replace(/\s+/g,'');const parts=email.split('@');if(parts.length===2&&/^(gmail|googlemail)\.com$/.test(parts[1]))parts[0]=parts[0].replace(/\./g,'').replace(/\+.*/,''),email=parts.join('@');return email}
function normalizePhone(value){const raw=String(value||'').trim(),digits=raw.replace(/\D/g,'');const phone=raw.startsWith('+')?`+${digits}`:`+${digits}`;if(!/^\+[1-9]\d{6,14}$/.test(phone))throw platformError(400,'measurement_phone_invalid','Telefoonnummer moet naar E.164 normaliseerbaar zijn');return phone}
function sha256(value){return digest(value)}
function freshness(lastAt,expectedSeconds,nowMs=Date.now()){if(!lastAt)return {classification:'UNKNOWN',last_received_at:null,freshness_seconds:null};const seconds=Math.max(0,Math.round((nowMs-Date.parse(lastAt))/1000));let classification='REALTIME';if(seconds>Math.max(60,expectedSeconds))classification='NEAR_REALTIME';if(seconds>Math.max(900,expectedSeconds*4))classification='BATCH';if(seconds>Math.max(86400,expectedSeconds*24))classification='STALE';return {classification,last_received_at:lastAt,freshness_seconds:seconds}}

class FoundlyPlatformCore{
  constructor(adapter={}){if(typeof adapter.bucket!=='function')throw new TypeError('Platform adapter.bucket is required');this.adapter={persist:()=>{},emit:()=>{},enqueue:()=>null,id:()=>crypto.randomUUID(),now:()=>new Date(),...adapter};this.factCache=new Map()}
  scope(context,principalInput){return {ctx:contextShape(context),principal:principalShape(principalInput)}}
  bucket(ctx,name){return this.adapter.bucket(ctx,`platform:${name}`)}
  now(){return this.adapter.now().toISOString()}
  commit(){this.adapter.persist()}
  audit(ctx,principal,action,entity,recordId,meta={}){const row={id:this.adapter.id(),tenant_id:ctx.tenant_id,dealer_id:ctx.dealer_id,actor_id:principal.id,actor_roles:principal.roles,action,entity,record_id:recordId||null,meta:sanitize(meta),created_at:this.now(),immutable:true,retention_lock:true};this.bucket(ctx,'audit').push(row);return row}
  migrate(context,principalInput){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'provision:manage');const rows=this.bucket(ctx,'migrations'),applied=[];let current=Number(rows.at(-1)?.version||0);const plans={1:'canonical tenant platform buckets',2:'event consent and immutable dedup index',3:'KPI rollups and knowledge lifecycle',4:'connector checkpoints and durable outbox',5:'canonical data records, object manifests, retention, connector execution and automation outcomes'};while(current<PLATFORM_SCHEMA_VERSION){current++;const row={id:`platform-v${current}`,version:current,description:plans[current],applied_at:this.now(),applied_by:principal.id,destructive:false,backfill_aware:true,rollback:'restore encrypted snapshot'};rows.push(row);applied.push(row)}if(applied.length){this.audit(ctx,principal,'MIGRATE','platform_schema',`v${current}`,{versions:applied.map(row=>row.version)});this.commit()}return {ok:true,current_version:current,target_version:PLATFORM_SCHEMA_VERSION,applied:clone(applied),destructive:false}}
  consentAllows(event){const consent=event.consent_context||{},purpose=String(consent.purpose||'').toLowerCase(),status=String(consent.status||'').toUpperCase(),basis=String(consent.legal_basis||'').toLowerCase();if(!purpose)return false;if(event.privacy_classification==='PUBLIC'||event.privacy_classification==='INTERNAL')return Boolean(basis||status==='GRANTED');if(purpose.includes('marketing'))return status==='GRANTED';return status==='GRANTED'||['contract','legal_obligation','legitimate_interest'].includes(basis)}
  normalizeEvent(ctx,input,options={}){
    if(!input||typeof input!=='object'||Array.isArray(input))throw platformError(400,'event_payload_invalid','Eventobject is verplicht');
    if(input.tenant_id&&input.tenant_id!==ctx.tenant_id||input.dealer_id&&input.dealer_id!==ctx.dealer_id)throw platformError(403,'event_tenant_mismatch','Event behoort tot een andere tenant');
    if(input.event_version!==undefined&&input.event_version!==EVENT_VERSION)throw platformError(422,'event_version_unsupported','Eventversie wordt niet ondersteund');
    if(input.schema_version!==undefined&&input.schema_version!==1&&input.schema_version!=='foundly-event/1.0.0')throw platformError(422,'event_schema_unsupported','Eventschema wordt niet ondersteund');
    input={...input,event_name:input.event_name||input.event_type,properties:input.properties||input.payload,privacy_classification:input.privacy_classification||input.classification};
    const source=text(input.source||'foundly',80),eventName=text(input.event_name||input.type,80).toLowerCase();if(!/^[a-z][a-z0-9_.:-]{1,79}$/.test(eventName))throw platformError(400,'event_name_invalid','Canonical event_name is ongeldig');const occurred=iso(input.occurred_at||this.now());if(!occurred)throw platformError(400,'event_time_invalid','occurred_at is ongeldig');const privacy=String(input.privacy_classification||'INTERNAL').toUpperCase();if(!PRIVACY_CLASSES.has(privacy))throw platformError(400,'event_privacy_invalid','Privacyclassificatie is ongeldig');const consent=sanitize(input.consent_context||{});const event={event_id:safeId(input.event_id)||this.adapter.id(),event_name:eventName,event_type:eventName,event_version:EVENT_VERSION,schema_version:'foundly-event/1.0.0',actor_id:safeId(input.actor_id),source_module:text(input.source_module||source,80),subject:sanitize(input.subject||{type:input.entity_type||null,id:input.entity_id||null}),permissions:sanitize(input.permissions||{}),idempotency_key:text(input.idempotency_key,200)||null,tenant_id:ctx.tenant_id,dealer_id:ctx.dealer_id,occurred_at:occurred,received_at:this.now(),source,source_kind:text(input.source_kind||'internal_service',80),provider:text(input.provider||'',80)||null,provider_event_id:safeId(input.provider_event_id),session_id:safeId(input.session_id),anonymous_id:safeId(input.anonymous_id),user_id:safeId(input.user_id),customer_id:safeId(input.customer_id),lead_id:safeId(input.lead_id),entity_type:text(input.entity_type,80)||null,entity_id:safeId(input.entity_id),campaign_id:safeId(input.campaign_id),utm_source:text(input.utm_source||input.source_name,160)||null,utm_medium:text(input.utm_medium,160)||null,utm_content:text(input.utm_content,300)||null,utm_term:text(input.utm_term,300)||null,click_ids:sanitize(input.click_ids||{}),correlation_id:safeId(input.correlation_id),causation_id:safeId(input.causation_id),properties:sanitize(input.properties||{}),consent_context:consent,privacy_classification:privacy,provenance:{source_id:source,source_kind:text(input.provenance?.source_kind||input.source_kind||'internal_service',80),external_id:text(input.provenance?.external_id||input.provider_event_id,200)||null,observed_at:occurred,ingested_at:this.now(),confidence:input.provenance?.confidence??null,evidence:sanitize(input.provenance?.evidence||[])},provider_verified:Boolean(input.provider_verified),ingest_version:INGEST_VERSION,immutable:true,retention_lock:Boolean(['FINANCIAL','SENSITIVE'].includes(privacy))};if(!this.consentAllows(event))throw platformError(422,'event_consent_denied','Event mist geldige consent- of doelcontext',{purpose:consent.purpose||null,privacy_classification:privacy});if(options.requireEventId&&!safeId(input.event_id))throw platformError(400,'event_id_required','Een canonical event_id is verplicht voor deze transport');return event;
  }
  ingestEvent(context,principalInput,input,options={}){
    const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'events:write');const event=this.normalizeEvent(ctx,input,options),dedup=this.bucket(ctx,'event_dedup'),keys=[`event:${event.event_id}`,event.provider&&event.provider_event_id?`provider:${event.provider}:${event.provider_event_id}`:null,options.idempotencyKey?`idempotency:${text(options.idempotencyKey,200)}`:null].filter(Boolean),existing=dedup.find(row=>keys.includes(row.key));if(existing){const stored=this.bucket(ctx,'raw_events').find(row=>row.event_id===existing.event_id);return {event:clone(stored),deduplicated:true,dedup_key:existing.key}}
    const raw=this.bucket(ctx,'raw_events');if(raw.length>=200000)throw platformError(507,'event_capacity_reached','Raw-eventcapaciteit bereikt; archiveer volgens retentionbeleid');raw.push(event);this.updateFactCache(ctx,event);for(const key of keys)dedup.push({key,event_id:event.event_id,created_at:event.received_at});this.incrementRollup(ctx,event);this.recordTouchFromEvent(ctx,event);this.audit(ctx,principal,'INGEST','event',event.event_id,{event_name:event.event_name,source:event.source,privacy_classification:event.privacy_classification});this.bucket(ctx,'event_notifications').push({event_id:event.event_id,status:'PENDING',attempts:0,created_at:this.now()});this.commit();this.flushEventNotifications(ctx,principalInput);return {event:clone(event),deduplicated:false};
  }
  flushEventNotifications(context,principalInput){
    const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'events:write');let attempted=0;
    for(const notification of this.bucket(ctx,'event_notifications').filter(row=>row.status==='PENDING').slice(0,50)){
      const event=this.bucket(ctx,'raw_events').find(row=>row.event_id===notification.event_id);if(!event)continue;
      notification.attempts++;attempted++;
      try{this.adapter.emit(ctx,clone(event));notification.status='DELIVERED';notification.delivered_at=this.now();notification.error=null;}catch(error){notification.error=text(error.code||'subscriber_unavailable',120);}
    }
    if(attempted)this.commit();return {attempted,pending:this.bucket(ctx,'event_notifications').filter(row=>row.status==='PENDING').length};
  }
  eventsForProjection(context,principalInput){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'events:read');return clone(this.bucket(ctx,'raw_events').filter(row=>eventReadable(row,principal)));}
  canonicalRecordsForProjection(context,principalInput){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'platform:read');return clone(this.bucket(ctx,'canonical_records').filter(row=>eventReadable(row,principal)));}
  canReadEvent(context,principalInput,event){const {principal}=this.scope(context,principalInput);requirePermission(principal,'events:read');return eventReadable(event,principal);}
  incrementRollup(ctx,event){const date=day(event.occurred_at),campaign=event.campaign_id||'',source=event.utm_source||event.source||'',key=[date,event.event_name,campaign,source,...(Object.keys(event.permissions||{}).length?[digest(JSON.stringify(event.permissions))]:[])].join('|'),rows=this.bucket(ctx,'event_rollups');let row=rows.find(item=>item.key===key);if(!row){row={key,date,permissions:clone(event.permissions||{}),event_name:event.event_name,campaign_id:event.campaign_id||null,source,events:0,value_cents:0,revenue_cents:0,gross_margin_cents:0,last_received_at:null,source_event_ids:[]};rows.push(row)}row.events++;row.value_cents+=Math.round(finite(event.properties.value_cents));row.revenue_cents+=Math.round(finite(event.properties.revenue_cents||(/purchase|invoice_paid|deal_won/.test(event.event_name)?event.properties.value_cents:0)));row.gross_margin_cents+=Math.round(finite(event.properties.gross_margin_cents));row.last_received_at=event.received_at;if(row.source_event_ids.length<500)row.source_event_ids.push(event.event_id)}
  recordTouchFromEvent(ctx,event){if(!event.session_id&&!event.lead_id&&!event.customer_id)return;if(!event.campaign_id&&!event.utm_source&&!event.utm_medium&&!Object.keys(event.click_ids||{}).length)return;const subjectId=event.customer_id||event.lead_id||event.session_id,rows=this.bucket(ctx,'attribution_touches');if(rows.some(row=>row.event_id===event.event_id))return;rows.push({touch_id:this.adapter.id(),permissions:clone(event.permissions||{}),tenant_id:ctx.tenant_id,dealer_id:ctx.dealer_id,subject_id:subjectId,session_id:event.session_id,lead_id:event.lead_id,customer_id:event.customer_id,event_id:event.event_id,occurred_at:event.occurred_at,campaign_id:event.campaign_id,source:event.utm_source||event.source,medium:event.utm_medium,content:event.utm_content,term:event.utm_term,click_ids:clone(event.click_ids),landing_page:text(event.properties.landing_page,1000)||null,immutable:true})}
  events(context,principalInput,query={}){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'events:read');const limit=Math.max(1,Math.min(500,Number(query.limit)||100)),cursor=Math.max(0,Number(query.cursor)||0),from=query.from?iso(query.from):null,to=query.to?iso(query.to):null;let rows=dateRange(this.bucket(ctx,'raw_events'),from,to).filter(row=>eventReadable(row,principal));for(const field of ['event_name','campaign_id','source','provider','entity_type','entity_id','correlation_id'])if(query[field])rows=rows.filter(row=>String(row[field]||'')===String(query[field]));rows=rows.sort((a,b)=>Date.parse(b.occurred_at)-Date.parse(a.occurred_at));return {items:clone(rows.slice(cursor,cursor+limit)),total:rows.length,next_cursor:cursor+limit<rows.length?cursor+limit:null,immutable:true}}
  normalizeFactQuery(options={}){const filters=options.filters&&typeof options.filters==='object'?options.filters:{},normalized={from:options.from?iso(options.from):null,to:options.to?iso(options.to):null,filters:{},period_days:finite(options.period_days??filters.period_days,30)};for(const field of ['campaign_id','source','provider','medium','owner_id','pipeline_id','stage_id','product_id','legal_entity_id','channel']){const value=options[field]??filters[field];if(value!==undefined&&value!==null&&String(value)!=='')normalized.filters[field]=String(value)}return normalized}
  factFilterValue(event,field){if(field==='source')return [event.source,event.utm_source].filter(Boolean);if(field==='medium')return [event.utm_medium,event.properties?.medium].filter(Boolean);return [event[field],event.properties?.[field]].filter(value=>value!==undefined&&value!==null)}
  eventMatchesFactQuery(event,query){if(query.access&&!eventReadable(event,query.access))return false;const occurred=Date.parse(event.occurred_at);if(query.from&&occurred<Date.parse(query.from))return false;if(query.to&&occurred>Date.parse(query.to))return false;for(const [field,wanted] of Object.entries(query.filters))if(!this.factFilterValue(event,field).some(value=>String(value)===wanted))return false;return true}
  emptyFacts(periodDays=30){const supporting={};for(const id of Object.keys(KPI_DEFINITIONS))supporting[id]={count:0,event_ids:[],sources:new Set(),last_received_at:null};const facts={events:[],session_started:0,lead_created:0,deal_won:0,deal_lost:0,customer_acquired:0,marketing_spend_cents:0,attributed_revenue_cents:0,revenue_cents:0,cost_of_goods_cents:0,operating_result_cents:0,open_deal_value_cents:0,weighted_pipeline_cents:0,sales_cycle_days:[],response_time_seconds:[],follow_up_completed:0,follow_up_due:0,receivables_cents:0,credit_revenue_cents:0,period_days:finite(periodDays,30),supporting,_customer_acquired_explicit:0,_attributed_revenue_total:0,_financial_revenue_total:0,_financial_value_total:0};Object.defineProperty(facts,FACT_OWNER,{value:this});return facts}
  addEventToFacts(facts,event){const properties=event.properties||{},name=event.event_name,signals=new Set([name,...Object.keys(properties)]);facts.events.push(event);if(name==='session_started')facts.session_started++;if(name==='lead_created')facts.lead_created++;if(name==='deal_won')facts.deal_won++;if(name==='deal_lost')facts.deal_lost++;if(name==='customer_acquired')facts._customer_acquired_explicit++;facts.customer_acquired=facts._customer_acquired_explicit||facts.deal_won;if(['marketing_spend','campaign_snapshot'].includes(name)){facts.marketing_spend_cents+=finite(properties.spend_cents);signals.add('marketing_spend_cents')}if(['deal_won','invoice_paid','purchase_completed'].includes(name)){facts._attributed_revenue_total+=finite(properties.attributed_revenue_cents);facts._financial_revenue_total+=finite(properties.revenue_cents);facts._financial_value_total+=finite(properties.value_cents);facts.attributed_revenue_cents=facts._attributed_revenue_total||facts._financial_revenue_total||facts._financial_value_total;facts.revenue_cents=facts._financial_revenue_total||facts._financial_value_total;facts.cost_of_goods_cents+=finite(properties.cost_of_goods_cents);signals.add('attributed_revenue_cents');signals.add('revenue_cents');signals.add('customer_acquired')}if(name==='period_result')facts.operating_result_cents+=finite(properties.operating_result_cents);if(['deal_created','deal_changed'].includes(name)){facts.open_deal_value_cents+=finite(properties.open_value_cents);facts.weighted_pipeline_cents+=finite(properties.weighted_value_cents);signals.add('open_deal_value_cents');signals.add('deal_probability')}if(name==='deal_won'&&Number.isFinite(finite(properties.sales_cycle_days,NaN)))facts.sales_cycle_days.push(finite(properties.sales_cycle_days));if(name==='lead_contacted'&&Number.isFinite(finite(properties.response_time_seconds,NaN)))facts.response_time_seconds.push(finite(properties.response_time_seconds));if(name==='follow_up_completed')facts.follow_up_completed++;if(name==='follow_up_due')facts.follow_up_due++;if(name==='finance_snapshot'){facts.receivables_cents+=finite(properties.receivables_cents);facts.credit_revenue_cents+=finite(properties.credit_revenue_cents)}for(const [id,definition] of Object.entries(KPI_DEFINITIONS)){if(!definition.inputs.some(input=>signals.has(input)))continue;const support=facts.supporting[id];support.count++;if(support.event_ids.length<1000)support.event_ids.push(event.event_id);support.sources.add(event.provider||event.source);if(!support.last_received_at||event.received_at>support.last_received_at)support.last_received_at=event.received_at}return facts}
  facts(ctx,options={},principal=null){const query=this.normalizeFactQuery(options);if(principal&&!principal.permissions.has('*'))query.access={id:principal.id,roles:[...principal.roles].sort()};const key=JSON.stringify([ctx.tenant_id,ctx.dealer_id,query]),cached=this.factCache.get(key);if(cached){this.factCache.delete(key);this.factCache.set(key,cached);return cached.facts}const facts=this.emptyFacts(query.period_days);for(const event of this.bucket(ctx,'raw_events'))if(this.eventMatchesFactQuery(event,query))this.addEventToFacts(facts,event);this.factCache.set(key,{ctx:{tenant_id:ctx.tenant_id,dealer_id:ctx.dealer_id},query,facts});while(this.factCache.size>16)this.factCache.delete(this.factCache.keys().next().value);return facts}
  updateFactCache(ctx,event){for(const cached of this.factCache.values()){if(cached.ctx.tenant_id!==ctx.tenant_id||cached.ctx.dealer_id!==ctx.dealer_id||!this.eventMatchesFactQuery(event,cached.query))continue;this.addEventToFacts(cached.facts,event)}}
  calculateKpi(context,principalInput,kpiId,options={}){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'analysis:read');const definition=KPI_DEFINITIONS[kpiId];if(!definition)throw platformError(404,'kpi_unknown','Onbekende KPI');const internalFacts=options.__facts?.[FACT_OWNER]===this?options.__facts:null,facts=internalFacts||this.facts(ctx,options,principal),ratio=(a,b,multiplier=1)=>b?Number((a/b*multiplier).toFixed(4)):null;let value=null,unit='COUNT';switch(kpiId){case'conversion_rate':value=ratio(facts.lead_created,facts.session_started,100);unit='PERCENT';break;case'win_rate':value=ratio(facts.deal_won,facts.deal_won+facts.deal_lost,100);unit='PERCENT';break;case'cpl':value=ratio(facts.marketing_spend_cents,facts.lead_created);unit='CENTS';break;case'cac':value=ratio(facts.marketing_spend_cents,facts.customer_acquired);unit='CENTS';break;case'roas':value=ratio(facts.attributed_revenue_cents,facts.marketing_spend_cents);unit='RATIO';break;case'mer':value=ratio(facts.revenue_cents,facts.marketing_spend_cents);unit='RATIO';break;case'pipeline_value':value=facts.open_deal_value_cents;unit='CENTS';break;case'weighted_pipeline':value=facts.weighted_pipeline_cents;unit='CENTS';break;case'gross_margin':value=facts.revenue_cents-facts.cost_of_goods_cents;unit='CENTS';break;case'net_margin':value=ratio(facts.operating_result_cents,facts.revenue_cents,100);unit='PERCENT';break;case'sales_cycle':value=average(facts.sales_cycle_days);unit='DAYS';break;case'response_time':value=average(facts.response_time_seconds);unit='SECONDS';break;case'follow_up_rate':value=ratio(facts.follow_up_completed,facts.follow_up_due,100);unit='PERCENT';break;case'dso':value=ratio(facts.receivables_cents,facts.credit_revenue_cents,facts.period_days);unit='DAYS';break}const support=facts.supporting[kpiId],fresh=freshness(support.last_received_at,definition.freshness_requirement==='REALTIME'?60:definition.freshness_requirement==='NEAR_REALTIME'?900:86400,this.adapter.now().getTime()),available=support.count>0&&value!==null,query=this.normalizeFactQuery(options);return {kpi:{...definition},value:available?value:null,unit,available,unavailable_reason:available?null:'NO_VERIFIED_SOURCE_DATA',calculated_at:this.now(),range:{from:query.from,to:query.to},filters:sanitize(query.filters),freshness:fresh,drilldown:{event_ids:[...support.event_ids],source_count:support.count,sources:[...support.sources]},source:'foundly_canonical_events',query_execution:'INCREMENTAL_LRU_FACT_CACHE',no_fake_data:true}}
  realtime(context,principalInput,options={}){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'analysis:read');const since=new Date(this.adapter.now().getTime()-Math.max(60,Math.min(86400,Number(options.seconds)||900))*1000).toISOString(),start=Date.parse(since),raw=this.bucket(ctx,'raw_events'),events=[];for(let index=raw.length-1;index>=0;index--){const event=raw[index];if(Date.parse(event.received_at)<start)break;if(Date.parse(event.occurred_at)>=start&&eventReadable(event,principal))events.push(event)}events.reverse();const facts=this.emptyFacts(30),byName={};for(const event of events){this.addEventToFacts(facts,event);byName[event.event_name]=(byName[event.event_name]||0)+1}const last=events.reduce((latest,row)=>!latest||row.received_at>latest?row.received_at:latest,null),metrics={};for(const id of ['conversion_rate','win_rate','pipeline_value','gross_margin'])metrics[id]=this.calculateKpi(context,principalInput,id,{from:since,__facts:facts});return {ok:true,window:{from:since,to:this.now()},events:events.length,by_event:byName,latest:clone(events.slice(-50).reverse()),metrics,freshness:freshness(last,60,this.adapter.now().getTime()),subscription:'SSE',query_execution:'REVERSE_RECEIVED_AT_WINDOW_SCAN',source:'foundly_canonical_events',no_fake_data:true}}
  historical(context,principalInput,options={}){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'analysis:read');const from=options.from?iso(options.from):null,to=options.to?iso(options.to):null,limit=Math.max(1,Math.min(500,Number(options.limit)||200)),cursor=Math.max(0,Number(options.cursor)||0),rows=this.bucket(ctx,'event_rollups').filter(row=>eventReadable(row,principal)&&(!from||row.date>=from.slice(0,10))&&(!to||row.date<=to.slice(0,10))&&(!options.campaign_id||row.campaign_id===options.campaign_id)).sort((a,b)=>String(b.date).localeCompare(String(a.date))||String(a.key).localeCompare(String(b.key))),page=rows.slice(cursor,cursor+limit);return {ok:true,range:{from,to},rollups:clone(page),total:rows.length,next_cursor:cursor+limit<rows.length?cursor+limit:null,pagination:{limit,cursor,server_side:true},freshness:freshness(rows.reduce((latest,row)=>!latest||row.last_received_at>latest?row.last_received_at:latest,null),86400,this.adapter.now().getTime()),source:'incremental_event_rollups',no_full_history_recompute:true,no_fake_data:true}}
  attribution(context,principalInput,subjectId,model='LAST_NON_DIRECT'){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'analysis:read');const touches=this.bucket(ctx,'attribution_touches').filter(row=>eventReadable(row,principal)&&(row.subject_id===subjectId||row.lead_id===subjectId||row.customer_id===subjectId)).sort((a,b)=>Date.parse(a.occurred_at)-Date.parse(b.occurred_at));if(!touches.length)return {model:String(model).toUpperCase(),touches:[],credits:[],available:false};const selected=String(model).toUpperCase();let credits=[];if(selected==='FIRST_TOUCH')credits=[{touch_id:touches[0].touch_id,credit:1}];else if(selected==='LAST_TOUCH')credits=[{touch_id:touches.at(-1).touch_id,credit:1}];else if(selected==='LINEAR')credits=touches.map(row=>({touch_id:row.touch_id,credit:Number((1/touches.length).toFixed(8))}));else{const chosen=[...touches].reverse().find(row=>String(row.medium||'').toLowerCase()!=='direct')||touches.at(-1);credits=[{touch_id:chosen.touch_id,credit:1}]}return {model:selected,touches:clone(touches),credits,available:true,original_history_preserved:true}}
  commercialFunnel(context,principalInput,options={}){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'analysis:read');const stages=['ad_impression','ad_click','session_started','form_submitted','lead_created','lead_qualified','appointment_scheduled','quote_created','deal_won','invoice_created','invoice_paid'],shared=options.__facts?.[FACT_OWNER]===this?options.__facts:null,events=(shared||this.facts(ctx,options,principal)).events,counts=stages.map(name=>({stage:name,count:events.filter(row=>row.event_name===name).length,event_ids:events.filter(row=>row.event_name===name).map(row=>row.event_id).slice(0,1000)})),revenue=events.filter(row=>['deal_won','invoice_paid'].includes(row.event_name)).reduce((sum,row)=>sum+finite(row.properties.revenue_cents||row.properties.value_cents),0),margin=events.filter(row=>['deal_won','invoice_paid'].includes(row.event_name)).reduce((sum,row)=>sum+finite(row.properties.gross_margin_cents),0);return {stages:counts,events:events.length,revenue_cents:revenue,gross_margin_cents:margin,campaign_id:options.campaign_id||options.filters?.campaign_id||null,source:'foundly_canonical_events',query_execution:'SHARED_FACT_SNAPSHOT',queryable_relationships:true,observed_at:this.now(),no_fake_data:true}}
  campaignOutcome(context,principalInput,campaignIdInput,options={}){
    const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'analysis:read');const campaignId=safeId(campaignIdInput);if(!campaignId)throw platformError(400,'campaign_id_invalid','Een geldige campaign_id is verplicht');
    const events=dateRange(this.bucket(ctx,'raw_events'),options.from?iso(options.from):null,options.to?iso(options.to):null).filter(row=>row.campaign_id===campaignId&&eventReadable(row,principal)),unique=(names,fields)=>new Set(events.filter(row=>names.includes(row.event_name)).map(row=>fields.map(field=>row[field]).find(Boolean)||row.event_id)).size;
    const financial=new Map(),priority={deal_won:1,purchase_completed:2,invoice_paid:3};for(const event of events.filter(row=>priority[row.event_name])){const subject=event.correlation_id||event.customer_id||event.lead_id||event.properties?.order_id||event.entity_id||event.event_id,current=financial.get(subject);if(!current||priority[event.event_name]>priority[current.event_name])financial.set(subject,event)}
    const outcomes=[...financial.values()],spend=events.filter(row=>['marketing_spend','campaign_snapshot'].includes(row.event_name)).reduce((sum,row)=>sum+finite(row.properties?.spend_cents),0),revenue=outcomes.reduce((sum,row)=>sum+finite(row.properties?.attributed_revenue_cents||row.properties?.revenue_cents||row.properties?.value_cents),0),margin=outcomes.reduce((sum,row)=>sum+finite(row.properties?.gross_margin_cents),0),touches=this.bucket(ctx,'attribution_touches').filter(row=>row.campaign_id===campaignId&&eventReadable(row,principal)),last=events.reduce((latest,row)=>!latest||row.received_at>latest?row.received_at:latest,null),sources=[];
    for(const event of events){const source=event.provider||event.source||'unknown',existing=sources.find(row=>row.source===source);if(existing){existing.events++;existing.provider_verified=existing.provider_verified&&Boolean(event.provider_verified);if(event.received_at>existing.last_received_at)existing.last_received_at=event.received_at}else sources.push({source,events:1,provider_verified:Boolean(event.provider_verified),last_received_at:event.received_at})}
    return {ok:true,available:events.length>0,unavailable_reason:events.length?null:'NO_VERIFIED_SOURCE_DATA',campaign_id:campaignId,range:{from:options.from?iso(options.from):null,to:options.to?iso(options.to):null},performance:{spend_cents:spend,leads:unique(['lead_created'],['lead_id','entity_id']),qualified_leads:unique(['lead_qualified'],['lead_id','entity_id']),deals:unique(['deal_created','deal_changed','deal_won','deal_lost'],['entity_id','correlation_id']),won_deals:unique(['deal_won'],['entity_id','correlation_id']),revenue_cents:revenue,gross_margin_cents:margin,roas:spend?Number((revenue/spend).toFixed(4)):null},attribution:{model:'LAST_NON_DIRECT',touch_count:touches.length,subject_count:new Set(touches.map(row=>row.subject_id)).size,touch_ids:touches.map(row=>row.touch_id).slice(0,1000),conversion_event_ids:outcomes.map(row=>row.event_id),original_history_preserved:true},supporting_records:events.slice(-1000).map(row=>({event_id:row.event_id,event_name:row.event_name,occurred_at:row.occurred_at,received_at:row.received_at,source:row.source,provider:row.provider||null,provider_verified:Boolean(row.provider_verified),entity_type:row.entity_type||null,entity_id:row.entity_id||null,correlation_id:row.correlation_id||null})),provenance:{source:'foundly_canonical_events',sources,calculation:'terminal financial event per correlation/customer/lead/order/entity; invoice_paid supersedes deal_won',double_count_protection:true},freshness:freshness(last,900,this.adapter.now().getTime()),observed_at:this.now(),tenant_filtered:true,no_fake_data:true};
  }
  dashboard(context,principalInput,options={}){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'analysis:read');const sharedFacts=this.facts(ctx,options,principal),realtime=this.realtime(context,principalInput,{seconds:options.seconds||900}),historical=this.historical(context,principalInput,options),kpis={};for(const id of ['conversion_rate','win_rate','cpl','cac','roas','mer','pipeline_value','weighted_pipeline','gross_margin','net_margin','sales_cycle','response_time','follow_up_rate','dso'])kpis[id]=this.calculateKpi(context,principalInput,id,{...options,__facts:sharedFacts});return {...clone(ANALYSIS_DASHBOARD),realtime,historical,kpis,funnel:this.commercialFunnel(context,principalInput,{...options,__facts:sharedFacts}),performance:{facts:'INCREMENTALLY_UPDATED_LRU_CACHE',kpi_snapshot_shared:true,historical:'SERVER_PAGINATED_INCREMENTAL_ROLLUPS',realtime:'REVERSE_RECEIVED_AT_WINDOW_SCAN',browser_full_dataset:false},observed_at:this.now(),no_fake_data:true}}
  metaPlan(context,principalInput,eventInput,config={},identity={}){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'events:write');const event=this.normalizeEvent(ctx,eventInput,{requireEventId:true});if(String(event.consent_context.status).toUpperCase()!=='GRANTED'||!String(event.consent_context.purpose||'').toLowerCase().includes('marketing'))throw platformError(422,'measurement_marketing_consent_required','Meta transport vereist expliciete marketingtoestemming');const eventAgeSeconds=Math.round((this.adapter.now().getTime()-Date.parse(event.occurred_at))/1000);if(eventAgeSeconds>604800||eventAgeSeconds < -300)throw platformError(422,'measurement_event_time_invalid','Meta serverevent valt buiten het toegestane tijdvenster');const name=META_STANDARD_EVENTS[event.event_name]||(/^custom\.[a-z0-9_]{1,40}$/.test(event.event_name)?event.event_name.slice(7):null);if(!name)throw platformError(422,'measurement_mapping_missing','Geen goedgekeurde Meta-eventmapping');const userData={};if(identity.email)userData.em=[sha256(normalizeEmail(identity.email))];if(identity.phone)userData.ph=[sha256(normalizePhone(identity.phone))];if(identity.first_name)userData.fn=[sha256(text(identity.first_name).toLowerCase())];if(identity.last_name)userData.ln=[sha256(text(identity.last_name).toLowerCase())];const externalId=identity.external_id||event.customer_id||event.lead_id;if(externalId)userData.external_id=[sha256(text(externalId,200).toLowerCase())];if(identity.fbc)userData.fbc=text(identity.fbc,300);if(identity.fbp)userData.fbp=text(identity.fbp,300);if(identity.client_user_agent)userData.client_user_agent=text(identity.client_user_agent,1000);const actionSource=text(config.action_source||'website',40),sourceUrl=text(event.properties.event_source_url||event.properties.landing_page,1000)||undefined;const base={event_name:name,event_time:Math.floor(Date.parse(event.occurred_at)/1000),event_id:event.event_id,action_source:actionSource,event_source_url:sourceUrl,user_data:Object.keys(userData).length?userData:undefined,custom_data:{currency:text(event.properties.currency||'EUR',8),value:finite(event.properties.value_cents)/100,content_ids:Array.isArray(event.properties.content_ids)?event.properties.content_ids.slice(0,100):undefined}};const configured=Boolean(config.pixel_id),serverConfigured=Boolean(config.dataset_id||config.pixel_id)&&Boolean(config.access_token_present),serverSendable=serverConfigured&&Boolean(base.user_data)&&(actionSource!=='website'||Boolean(sourceUrl));return {canonical_event_id:event.event_id,browser:{status:configured?'CONFIGURED_UNVERIFIED':'NOT_CONFIGURED',transport:'Meta Pixel',pixel_id:configured?text(config.pixel_id,80):null,event:name,parameters:{eventID:event.event_id,...base.custom_data}},server:{status:serverConfigured?'CONFIGURED_UNVERIFIED':'NOT_CONFIGURED',sendable:serverSendable,validation_issues:[...(!base.user_data?['MATCHING_DATA_REQUIRED']:[]),...(actionSource==='website'&&!sourceUrl?['EVENT_SOURCE_URL_REQUIRED_FOR_WEBSITE']:[])],transport:'Meta Conversions API',endpoint_template:'https://graph.facebook.com/{graph_version}/{dataset_id}/events',secret_location:'SERVER_ONLY',payload:{data:[base],test_event_code:config.test_event_code_present?'SERVER_SIDE_VALUE_PRESENT':undefined},retry:{max_attempts:5,backoff:'exponential_with_jitter'},observability:true},dedup:{shared_event_id:event.event_id,browser_and_server_same_identity:true},provider_contract:{verified_at:'2026-09-04',official_sources:['https://developers.facebook.com/documentation/ads-commerce/conversions-api/parameters/server-event.md/','https://developers.facebook.com/documentation/ads-commerce/conversions-api/deduplicate-pixel-and-server-events.md/']},connected:false,pii_logged:false}}
  ga4Plan(context,principalInput,eventInput,config={}){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'events:write');const event=this.normalizeEvent(ctx,eventInput,{requireEventId:true}),name=GOOGLE_EVENT_NAMES[event.event_name]||(/^custom\.[a-z0-9_]{1,40}$/.test(event.event_name)?event.event_name.slice(7):event.event_name.replace(/[.:-]/g,'_').slice(0,40));if(!/^[a-z][a-z0-9_]{0,39}$/.test(name))throw platformError(422,'measurement_mapping_missing','GA4-eventnaam is ongeldig');const clientId=text(event.properties.ga_client_id||event.anonymous_id,120),userId=event.user_id||event.customer_id;if(!clientId&&!userId)throw platformError(422,'measurement_identity_required','GA4 Measurement Protocol vereist client_id of user_id');const body={client_id:clientId||undefined,user_id:userId||undefined,timestamp_micros:String(Math.trunc(Date.parse(event.occurred_at)*1000)),consent:{ad_user_data:String(event.consent_context.status).toUpperCase()==='GRANTED'?'GRANTED':'DENIED',ad_personalization:String(event.consent_context.marketing_status||event.consent_context.status).toUpperCase()==='GRANTED'?'GRANTED':'DENIED'},events:[{name,params:{session_id:event.session_id&&/^\d+$/.test(event.session_id)?Number(event.session_id):undefined,engagement_time_msec:finite(event.properties.engagement_time_msec),transaction_id:event.event_id,value:finite(event.properties.value_cents)/100,currency:text(event.properties.currency||'EUR',8),campaign_id:event.campaign_id||undefined,source:event.utm_source||undefined,medium:event.utm_medium||undefined}}]};const configured=Boolean(config.measurement_id)&&Boolean(config.api_secret_present);return {canonical_event_id:event.event_id,status:configured?'CONFIGURED_UNVERIFIED':'NOT_CONFIGURED',endpoint:'https://region1.google-analytics.com/mp/collect',query:{measurement_id:configured?text(config.measurement_id,80):null,api_secret:'SERVER_ONLY'},payload:body,validation_endpoint:'https://www.google-analytics.com/debug/mp/collect',validation_behavior:'ENFORCE_RECOMMENDATIONS',do_not_blindly_double_send:true,provider_contract:{verified_at:'2026-09-04',official_source:'https://developers.google.com/analytics/devguides/collection/protocol/ga4/reference'},connected:false}}
  enhancedConversionPlan(context,principalInput,eventInput,identity={},config={}){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'events:write');const event=this.normalizeEvent(ctx,eventInput,{requireEventId:true});if(String(event.consent_context.status).toUpperCase()!=='GRANTED'||!String(event.consent_context.purpose||'').toLowerCase().includes('marketing'))throw platformError(422,'measurement_marketing_consent_required','Enhanced conversions vereisen expliciete toestemming en eerste-partijdata');const identifiers=[];if(identity.email)identifiers.push({hashedEmail:sha256(normalizeEmail(identity.email))});if(identity.phone)identifiers.push({hashedPhoneNumber:sha256(normalizePhone(identity.phone))});if(identity.first_name||identity.last_name){if(!identity.first_name||!identity.last_name||!identity.country_code||!identity.postal_code)throw platformError(422,'measurement_address_identity_incomplete','Naam-identifiers vereisen voornaam, achternaam, landcode en postcode');identifiers.push({addressInfo:{hashedFirstName:sha256(text(identity.first_name).toLowerCase()),hashedLastName:sha256(text(identity.last_name).toLowerCase()),countryCode:text(identity.country_code,2).toUpperCase(),postalCode:text(identity.postal_code,20)}})}if(!identifiers.length)throw platformError(422,'measurement_identity_required','Minimaal één first-party identifier is vereist');const conversion={conversionAction:config.conversion_action?text(config.conversion_action,300):null,adjustmentType:'ENHANCEMENT',orderId:text(event.properties.order_id||event.event_id,200),userIdentifiers:identifiers.slice(0,5),userAgent:text(identity.user_agent,1000)||undefined};if(event.click_ids.gclid)conversion.gclidDateTimePair={gclid:text(event.click_ids.gclid,200),conversionDateTime:event.occurred_at};const configured=Boolean(config.customer_id&&config.conversion_action&&config.access_configured&&config.developer_token_present);return {canonical_event_id:event.event_id,status:configured?'CONFIGURED_UNVERIFIED':'NOT_CONFIGURED',transport:'Google Ads enhanced conversions for web',customer_id:config.customer_id?text(config.customer_id,40):null,endpoint_template:'https://googleads.googleapis.com/{api_version}/customers/{customer_id}:uploadConversionAdjustments',payload:{conversionAdjustments:[conversion],partialFailure:true},unsupported_click_ids:[...(event.click_ids.gbraid?['gbraid']:[]),...(event.click_ids.wbraid?['wbraid']:[])],pii_logged:false,normalization:'official_google_sha256_contract',provider_contract:{verified_at:'2026-09-04',official_source:'https://developers.google.com/google-ads/api/docs/conversions/upload-online'},connected:false}}
  queueDelivery(context,principalInput,provider,eventInput,config={},identity={}){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'events:write');const kind=String(provider).toLowerCase(),plan=kind==='meta'?this.metaPlan(context,principalInput,eventInput,config,identity):kind==='ga4'?this.ga4Plan(context,principalInput,eventInput,config):kind==='google_ads'?this.enhancedConversionPlan(context,principalInput,eventInput,identity,config):null;if(!plan)throw platformError(404,'measurement_provider_unknown','Onbekende measurement-provider');const status=plan.status||plan.server?.status;if(status==='NOT_CONFIGURED')return {queued:false,status:'NOT_CONFIGURED',plan};if(kind==='meta'&&!plan.server.sendable)throw platformError(422,'measurement_meta_payload_incomplete','Meta CAPI-payload mist verplichte matching- of brondata',{issues:plan.server.validation_issues});const rows=this.bucket(ctx,'provider_deliveries'),existing=rows.find(row=>row.provider===kind&&row.canonical_event_id===plan.canonical_event_id&&['QUEUED','SENDING','ACCEPTED','VERIFIED'].includes(row.status));if(existing)return {queued:existing.status==='QUEUED',status:existing.status,delivery:clone(existing),deduplicated:true};const delivery={id:this.adapter.id(),provider:kind,canonical_event_id:plan.canonical_event_id,status:'QUEUED',attempts:0,max_attempts:5,next_attempt_at:this.now(),payload:plan,created_at:this.now(),secret_material:false};rows.push(delivery);this.adapter.enqueue(ctx,{type:'platform_provider_delivery',delivery_id:delivery.id,provider:kind});this.audit(ctx,principal,'QUEUE','provider_delivery',delivery.id,{provider:kind,event_id:plan.canonical_event_id});this.commit();return {queued:true,status:'CONFIGURED_UNVERIFIED',delivery:clone(delivery),deduplicated:false}}
  providerReceipt(context,principalInput,input){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'events:write');const provider=text(input.provider,80).toLowerCase(),eventId=safeId(input.canonical_event_id),status=String(input.status||'').toUpperCase();if(!provider||!eventId||!['VALIDATED','REJECTED','TRANSPORT_ACCEPTED','PARTIAL_FAILURE','FAILED','VERIFIED'].includes(status))throw platformError(400,'measurement_receipt_invalid','Providerreceipt mist provider, canonical event ID of geldige status');const rows=this.bucket(ctx,'provider_receipts'),row={id:this.adapter.id(),tenant_id:ctx.tenant_id,dealer_id:ctx.dealer_id,provider,canonical_event_id:eventId,status,http_status:Number.isInteger(Number(input.http_status))?Number(input.http_status):null,provider_reference:text(input.provider_reference,300)||null,response:sanitize(input.response||{}),provider_verified:Boolean(input.provider_verified),processing_verified:Boolean(input.processing_verified),received_at:this.now(),immutable:true,secret_material:false};rows.push(row);const delivery=this.bucket(ctx,'provider_deliveries').find(item=>item.provider===provider&&item.canonical_event_id===eventId&&['QUEUED','SENDING'].includes(item.status));if(delivery){delivery.status=status==='TRANSPORT_ACCEPTED'?'ACCEPTED':status;delivery.completed_at=row.received_at;delivery.receipt_id=row.id}this.audit(ctx,principal,'RECEIPT','provider_delivery',row.id,{provider,event_id:eventId,status,http_status:row.http_status});this.commit();return clone(row)}
  taxRules(context,principalInput,{date,legal_form,tax_type='BTW',classification}={}){const {principal}=this.scope(context,principalInput);requirePermission(principal,'platform:read');const at=iso(date||this.now()),form=String(legal_form||'').toUpperCase(),type=String(tax_type).toUpperCase();if(!LEGAL_FORMS.includes(form))throw platformError(400,'tax_legal_form_invalid','Ondersteunde Nederlandse rechtsvorm is verplicht');const rules=BUILTIN_TAX_RULES.filter(rule=>rule.tax_type===type&&rule.legal_forms.includes(form)&&rule.effective_from<=at&&(!rule.effective_until||rule.effective_until>=at)&&(!classification||rule.parameters.classification===String(classification).toUpperCase()));if(!rules.length)throw platformError(422,'tax_rule_unverified','Geen geverifieerde fiscale regel voor datum, rechtsvorm en domein; stale regels worden niet stil hergebruikt',{date:at,legal_form:form,tax_type:type});return {rules:clone(rules),selected_at:at,historical_selection:true,stale_reuse:false}}
  calculateVat(context,principalInput,input){const classification=String(input.classification||'STANDARD').toUpperCase(),selection=this.taxRules(context,principalInput,{date:input.date,legal_form:input.legal_form,tax_type:'BTW',classification}),rule=selection.rules.sort((a,b)=>b.effective_from.localeCompare(a.effective_from))[0],net=Number(input.net_cents);if(!Number.isInteger(net)||net<0)throw platformError(400,'tax_amount_invalid','net_cents moet een positief geheel aantal eurocenten zijn');return {jurisdiction:'NL',tax_type:'BTW',legal_form:String(input.legal_form).toUpperCase(),net_cents:net,rate:rule.parameters.rate,vat_cents:Math.round(net*rule.parameters.rate/100),gross_cents:net+Math.round(net*rule.parameters.rate/100),rule,calculated_at:this.now(),review_required:classification==='ZERO',filing_performed:false}}
  validateDutchInvoice(context,principalInput,input={}){
    const {principal}=this.scope(context,principalInput);requirePermission(principal,'platform:read');const invoice=sanitize(input.invoice||input),profile=String(input.profile||invoice.profile||'STANDARD').toUpperCase(),issues=[],warnings=[];
    if(profile!=='STANDARD')warnings.push({code:'SPECIAL_OR_SIMPLIFIED_REGIME_REVIEW_REQUIRED',field:'profile'});
    const required=['supplier_name','supplier_address','customer_name','customer_address','supplier_vat_id','invoice_date','invoice_number','supply_date'];
    if(invoice.supplier_kvk_registered!==false)required.push('supplier_kvk_number');
    for(const field of required)if(!text(invoice[field],500))issues.push({code:'REQUIRED_FIELD_MISSING',field});
    if(invoice.supplier_vat_id&&!/^NL[0-9]{9}B[0-9]{2}$/i.test(String(invoice.supplier_vat_id).replace(/\s/g,'')))issues.push({code:'SUPPLIER_VAT_ID_INVALID',field:'supplier_vat_id'});
    if(invoice.supplier_kvk_number&&!/^[0-9]{8}$/.test(String(invoice.supplier_kvk_number).replace(/\s/g,'')))issues.push({code:'SUPPLIER_KVK_INVALID',field:'supplier_kvk_number'});
    for(const field of ['invoice_date','supply_date'])if(invoice[field]&&!iso(invoice[field]))issues.push({code:'DATE_INVALID',field});
    const existing=(Array.isArray(input.existing_invoice_numbers)?input.existing_invoice_numbers:[]).map(String);if(invoice.invoice_number&&existing.includes(String(invoice.invoice_number)))issues.push({code:'INVOICE_NUMBER_DUPLICATE',field:'invoice_number'});
    let sequenceVerified=false;const numberMatch=String(invoice.invoice_number||'').match(/^(.*?)(\d+)$/),previousMatch=String(input.previous_invoice_number||'').match(/^(.*?)(\d+)$/);if(numberMatch&&previousMatch&&numberMatch[1]===previousMatch[1]){sequenceVerified=Number(numberMatch[2])===Number(previousMatch[2])+1;if(!sequenceVerified)issues.push({code:'INVOICE_NUMBER_NOT_SEQUENTIAL',field:'invoice_number'})}else warnings.push({code:'SEQUENCE_NOT_PROVEN',field:'invoice_number'});
    const lines=Array.isArray(invoice.lines)?invoice.lines:[];if(!lines.length)issues.push({code:'INVOICE_LINES_MISSING',field:'lines'});let netTotal=0,vatTotal=0;
    lines.forEach((line,index)=>{const prefix=`lines.${index}`,quantity=Number(line.quantity),unit=Number(line.unit_price_cents),net=Number.isInteger(Number(line.net_cents))?Number(line.net_cents):(Number.isFinite(quantity)&&Number.isInteger(unit)?Math.round(quantity*unit):NaN),rate=Number(line.vat_rate),vat=Number(line.vat_cents);if(!text(line.description,500))issues.push({code:'DESCRIPTION_MISSING',field:`${prefix}.description`});if(!Number.isFinite(quantity)||quantity<=0)issues.push({code:'QUANTITY_INVALID',field:`${prefix}.quantity`});if(!Number.isInteger(net))issues.push({code:'NET_AMOUNT_INVALID',field:`${prefix}.net_cents`});if(![0,9,21].includes(rate))warnings.push({code:'VAT_RATE_REQUIRES_RULE_REVIEW',field:`${prefix}.vat_rate`});if(!Number.isInteger(vat))issues.push({code:'VAT_AMOUNT_INVALID',field:`${prefix}.vat_cents`});else if(Number.isInteger(net)&&Math.abs(vat-Math.round(net*rate/100))>1)issues.push({code:'VAT_ARITHMETIC_MISMATCH',field:`${prefix}.vat_cents`});if(Number.isInteger(net))netTotal+=net;if(Number.isInteger(vat))vatTotal+=vat});
    if(Number.isInteger(Number(invoice.net_cents))&&Number(invoice.net_cents)!==netTotal)issues.push({code:'NET_TOTAL_MISMATCH',field:'net_cents'});if(Number.isInteger(Number(invoice.vat_cents))&&Number(invoice.vat_cents)!==vatTotal)issues.push({code:'VAT_TOTAL_MISMATCH',field:'vat_cents'});
    return {ok:issues.length===0,valid:issues.length===0,profile,issues,warnings,totals:{net_cents:netTotal,vat_cents:vatTotal,gross_cents:netTotal+vatTotal},sequence_verified:sequenceVerified,review_required:profile!=='STANDARD'||warnings.some(item=>item.code==='VAT_RATE_REQUIRES_RULE_REVIEW'),rule_contract:{jurisdiction:'NL',tax_type:'BTW_INVOICE_VALIDATION',rule_version:'2026.1',effective_from:'2026-01-01',effective_until:'2026-12-31T23:59:59.999Z',official_source:TAX_SOURCES.invoice_requirements.authority,source_reference:TAX_SOURCES.invoice_requirements.url,verification_status:'VERIFIED',last_verified_at:TAX_SOURCES.invoice_requirements.verified_at},filing_performed:false};
  }
  retentionPolicy(context,principalInput,input={}){const {principal}=this.scope(context,principalInput);requirePermission(principal,'platform:read');const category=String(input.category||'BASIS_DATA').toUpperCase(),years=['REAL_ESTATE','OSS'].includes(category)?10:7,start=iso(input.retention_start||input.effective_until||input.created_at||this.now());if(!start)throw platformError(400,'retention_start_invalid','Geldige startdatum voor de bewaartermijn is verplicht');const lockedUntil=new Date(start);lockedUntil.setUTCFullYear(lockedUntil.getUTCFullYear()+years);return {jurisdiction:'NL',category,retention_years:years,retention_start:start,locked_until:lockedUntil.toISOString(),retention_lock:true,user_delete_behavior:'LOGICAL_ARCHIVE_ONLY',legal_archive_required:true,eventual_purge_eligible_after:lockedUntil.toISOString(),source:TAX_SOURCES.retention}}
  archiveLegalRecord(context,principalInput,input={}){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'provision:manage');const recordId=safeId(input.record_id),entityType=text(input.entity_type,80);if(!recordId||!entityType||!text(input.reason,500))throw platformError(422,'retention_archive_invalid','entity_type, record_id en reden zijn verplicht');const policy=this.retentionPolicy(context,principalInput,input),rows=this.bucket(ctx,'retention_records'),existing=rows.find(row=>row.entity_type===entityType&&row.record_id===recordId);if(existing)return {...clone(existing),idempotent:true};const row={id:this.adapter.id(),tenant_id:ctx.tenant_id,dealer_id:ctx.dealer_id,entity_type:entityType,record_id:recordId,status:'LOGICALLY_ARCHIVED',reason:text(input.reason,500),archived_at:this.now(),archived_by:principal.id,...policy,physical_delete_performed:false,immutable:true};rows.push(row);this.audit(ctx,principal,'ARCHIVE',entityType,recordId,{locked_until:row.locked_until,physical_delete_performed:false});this.commit();return clone(row)}
  taxCapabilities(context,principalInput){const {principal}=this.scope(context,principalInput);requirePermission(principal,'platform:read');return {jurisdiction:'NL',legal_forms:LEGAL_FORMS,domains:TAX_DOMAINS.map(domain=>({domain,status:domain==='BTW'?'VERIFIED_IMPLEMENTED':'ARCHITECTURE_ONLY_UNVERIFIED',execution_allowed:domain==='BTW'})),sources:TAX_SOURCES,rule_contract:{required:['jurisdiction','tax_type','legal_forms','effective_from','effective_until','rule_version','official_source','source_reference','parameters','verification_status','last_verified_at'],historical_versions_immutable:true,stale_rule_reuse:false},invoice_validation:{status:'VERIFIED_IMPLEMENTED',profiles:['STANDARD','SPECIAL_REVIEW_REQUIRED'],source:TAX_SOURCES.invoice_requirements},retention:{basis_years:7,real_estate_and_oss_years:10,ordinary_delete_blocked:true,archive_then_eventual_policy_purge:true},filing:{status:'NOT_IMPLEMENTED_NO_PROVIDER',automatic_filing:false},model_memory_authoritative:false}}
  upsertCanonicalRecord(context,principalInput,input={},options={}){
    const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'events:write');const source=text(input.source,120),sourceKind=String(input.source_kind||'USER_INPUT').toUpperCase(),recordType=text(input.record_type||input.entity_type,80),externalId=text(input.external_id,300)||null,provider=text(input.provider,100)||null;
    if(!source||!DATA_SOURCE_KINDS.has(sourceKind)||!recordType)throw platformError(422,'data_record_invalid','source, source_kind en record_type zijn verplicht');const rows=this.bucket(ctx,'canonical_records'),internalId=safeId(input.internal_id),existing=internalId?rows.find(row=>row.record_type===recordType&&row.internal_id===internalId):(externalId?rows.find(row=>row.record_type===recordType&&row.source===source&&row.external_id===externalId):null),expected=options.expected_version===undefined?null:Number(options.expected_version);
    if(existing&&!eventReadable(existing,principal))throw platformError(404,'data_record_not_found','Record niet gevonden');
    if(existing&&expected!==null&&existing.version!==expected)throw platformError(409,'data_revision_conflict','Canonical record is gewijzigd sinds de lokale basisversie',{expected_version:expected,current_version:existing.version,conflict_strategy:'MANUAL_OR_DOMAIN_RULE'});
    const now=this.now(),record=existing||{tenant_id:ctx.tenant_id,dealer_id:ctx.dealer_id,internal_id:internalId||this.adapter.id(),record_type:recordType,created_at:iso(input.created_at)||now,created_by:principal.id,version:0};record.source=source;record.source_kind=sourceKind;record.provider=provider;record.external_id=externalId;record.provider_verified=Boolean(input.provider_verified);record.source_updated_at=input.source_updated_at?iso(input.source_updated_at):null;record.ingested_at=now;record.updated_at=now;record.sync_state=String(input.sync_state||'SYNCED').toUpperCase();if(!SYNC_STATES.has(record.sync_state))throw platformError(400,'data_sync_state_invalid','Ongeldige sync_state');record.version+=1;record.provenance=sanitize(input.provenance||{source_id:source,source_kind:sourceKind,provider_verified:record.provider_verified});record.permissions=sanitize(input.permissions||{roles:principal.roles});record.confidence=Number.isFinite(Number(input.confidence))?Math.max(0,Math.min(1,Number(input.confidence))):null;record.data=sanitize(input.data||input.payload||{});record.retention=sanitize(input.retention||{});record.content_digest=digest(JSON.stringify(record.data));if(!existing)rows.push(record);this.audit(ctx,principal,existing?'UPDATE':'CREATE','canonical_record',record.internal_id,{record_type:recordType,source,version:record.version});this.commit();return {...clone(record),created:!existing};
  }
  searchCanonicalRecords(context,principalInput,query={}){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'platform:read');const q=text(query.q,500).toLowerCase(),limit=Math.max(1,Math.min(200,Number(query.limit)||50));let rows=this.bucket(ctx,'canonical_records').filter(row=>{const allowed=(!Array.isArray(row.permissions?.roles)||row.permissions.roles.some(role=>principal.roles.includes(role))||principal.permissions.has('*'))&&eventReadable(row,principal);if(!allowed)return false;if(query.record_type&&row.record_type!==query.record_type)return false;if(query.source&&row.source!==query.source)return false;if(query.sync_state&&row.sync_state!==String(query.sync_state).toUpperCase())return false;return !q||[row.external_id,JSON.stringify(row.data),JSON.stringify(row.provenance)].some(value=>String(value||'').toLowerCase().includes(q))});rows=rows.sort((a,b)=>Date.parse(b.updated_at)-Date.parse(a.updated_at));return {items:clone(rows.slice(0,limit)),total:rows.length,tenant_filtered:true,permission_filtered:true,indexes:['record_type','source_external_id','sync_state','updated_at'],search_modes:['STRUCTURED','FULL_TEXT'],semantic_index:{available:false,reason:'Geen geverifieerde vectorindex geconfigureerd'},no_fake_data:true}}
  registerObject(context,principalInput,input={}){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'events:write');const sha=text(input.sha256,64).toLowerCase(),reference=text(input.object_reference,1000),size=Number(input.size_bytes);if(!/^[a-f0-9]{64}$/.test(sha)||!reference||!Number.isInteger(size)||size<0)throw platformError(422,'data_object_manifest_invalid','sha256, object_reference en size_bytes zijn verplicht');const rows=this.bucket(ctx,'object_manifests'),existing=rows.find(row=>row.sha256===sha);if(existing)return {...clone(existing),deduplicated:true};const retentionInput=input.retention||{category:input.category||'BASIS_DATA',created_at:input.created_at||this.now()},category=String(retentionInput.category||'BASIS_DATA').toUpperCase(),years=['REAL_ESTATE','OSS'].includes(category)?10:7,start=iso(retentionInput.retention_start||retentionInput.effective_until||retentionInput.created_at||this.now());if(!start)throw platformError(400,'retention_start_invalid','Geldige startdatum voor de bewaartermijn is verplicht');const lockedUntil=new Date(start);lockedUntil.setUTCFullYear(lockedUntil.getUTCFullYear()+years);const retention={jurisdiction:'NL',category,retention_years:years,retention_start:start,locked_until:lockedUntil.toISOString(),retention_lock:true,user_delete_behavior:'LOGICAL_ARCHIVE_ONLY',legal_archive_required:true,eventual_purge_eligible_after:lockedUntil.toISOString(),source:TAX_SOURCES.retention},row={id:this.adapter.id(),tenant_id:ctx.tenant_id,dealer_id:ctx.dealer_id,sha256:sha,object_reference:reference,content_type:text(input.content_type,120)||'application/octet-stream',size_bytes:size,source:text(input.source,120)||'upload',source_kind:String(input.source_kind||'DOCUMENT').toUpperCase(),provider_verified:Boolean(input.provider_verified),provenance:sanitize(input.provenance||{}),permissions:sanitize(input.permissions||{roles:principal.roles}),created_at:this.now(),created_by:principal.id,retention,immutable_manifest:true,object_bytes_stored:Boolean(input.object_bytes_stored)};rows.push(row);this.audit(ctx,principal,'REGISTER','object_manifest',row.id,{sha256:sha,size_bytes:size,object_bytes_stored:row.object_bytes_stored});this.commit();return clone(row)}
  applyOutbox(context,principalInput,outboxId,options={}){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'events:write');const row=this.bucket(ctx,'outbox').find(item=>item.id===outboxId);if(!row)throw platformError(404,'outbox_missing','Outbox-item niet gevonden');if(row.status==='APPLIED')return {...clone(row),replayed:true};if(row.status==='CONFLICT'&&!options.resolve_conflict)throw platformError(409,'outbox_conflict_unresolved','Outbox-conflict vereist expliciete domeinresolutie');try{const result=this.upsertCanonicalRecord(context,principalInput,{record_type:row.entity_type,internal_id:row.entity_id,source:'offline_client',source_kind:'OFFLINE_CLIENT',sync_state:'SYNCED',data:row.payload,provenance:{outbox_id:row.id,idempotency_key:row.idempotency_key,base_revision:row.base_revision}},{expected_version:options.resolve_conflict?undefined:row.base_revision});row.status='APPLIED';row.applied_at=this.now();row.applied_by=principal.id;row.result_record_id=result.internal_id;row.result_version=result.version;const event=this.ingestEvent(context,principalInput,{event_id:`outbox:${row.id}`,event_name:'offline_change_applied',occurred_at:this.now(),source:'foundly_offline_sync',source_kind:'internal_service',entity_type:row.entity_type,entity_id:result.internal_id,properties:{outbox_id:row.id,record_version:result.version},consent_context:{purpose:'business_operations',legal_basis:'contract'},privacy_classification:'INTERNAL'},{idempotencyKey:`outbox-apply:${row.id}`});this.audit(ctx,principal,'APPLY','outbox',row.id,{record_id:result.internal_id,version:result.version});this.commit();return {outbox:clone(row),record:result,event_id:event.event.event_id,replayed:false}}catch(error){if(error.code==='data_revision_conflict'){row.status='CONFLICT';row.last_error=error.code;row.conflict=sanitize(error.details);row.updated_at=this.now();this.commit()}throw error}}
  dataPlatformStatus(context,principalInput){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'platform:read');const persistence=typeof this.adapter.persistenceStatus==='function'?this.adapter.persistenceStatus(ctx):{configured:false,durable:false};return {ok:true,roles:{operational_database:{implementation:'encrypted_transactional_core_adapter',production_ready:Boolean(persistence.durable),limitation:persistence.durable?null:'Geen duurzame production mount of externe operationele database bewezen'},immutable_event_store:{implementation:'append_only_tenant_event_bucket',events:this.bucket(ctx,'raw_events').length},object_storage:{implementation:'content_addressed_manifests',manifests:this.bucket(ctx,'object_manifests').length,bytes_external_to_manifest:true},full_text_search:{implementation:'tenant_and_permission_filtered_scan',records:this.bucket(ctx,'canonical_records').length,index_contract:true},knowledge_store:{implementation:'typed_versioned_records',records:this.bucket(ctx,'knowledge').length},semantic_index:{implementation:null,status:'NOT_CONFIGURED_NOT_CLAIMED'},cache:{implementation:'incremental_event_rollups'}},persistence,retention:{policies:['BASIS_DATA_7_YEARS','REAL_ESTATE_10_YEARS','OSS_10_YEARS'],logical_archive:true,ordinary_physical_delete:false},offline:{durable_outbox_items:this.bucket(ctx,'outbox').length,conflict_strategy:'OPTIMISTIC_REVISION_THEN_DOMAIN_REVIEW',high_risk_last_write_wins:false},no_ephemeral_primary_claim:true,no_fake_data:true}}
  recoveryStatus(context,principalInput){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'platform:read');const persistence=typeof this.adapter.persistenceStatus==='function'?this.adapter.persistenceStatus(ctx):{configured:false,durable:false};return {ok:true,persistence,application_snapshot:{encrypted_at_rest:Boolean(persistence.encrypted_at_rest),atomic_write:'TEMP_FILE_FSYNC_RENAME_DIRECTORY_FSYNC',restart_restore_supported:true,integrity:'authenticated_encryption'},schema_migrations:{platform_version:PLATFORM_SCHEMA_VERSION,versioned:true,backfill_aware:true,destructive:false,rollback_strategy:'restore validated encrypted snapshot'},production_backup:{verified:false,status:'EXTERNAL_BACKUP_NOT_VERIFIED',required:'Railway volume/database snapshot policy plus restore drill'},readiness_gate:{safe:Boolean(persistence.durable),fails_when_critical_persistence_unsafe:true},tenant_id:ctx.tenant_id,dealer_id:ctx.dealer_id,no_secret_material:true,observed_at:this.now()}}
  createKnowledge(context,principalInput,input){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'knowledge:write');const type=String(input.type||'').toUpperCase(),subject=text(input.subject,300),predicate=text(input.predicate,160),sources=Array.isArray(input.sources)?sanitize(input.sources):[],privacy=String(input.privacy_classification||'INTERNAL').toUpperCase();if(!KNOWLEDGE_TYPES.has(type)||!subject||!predicate||!PRIVACY_CLASSES.has(privacy))throw platformError(400,'knowledge_record_invalid','Type, subject, predicate en privacyclassificatie zijn verplicht');if(['NORMALIZED_FACT','DERIVED_INSIGHT'].includes(type)&&!sources.length)throw platformError(422,'knowledge_evidence_required','Feit of inzicht vereist bronprovenance');const confidence=Number(input.confidence),now=this.now(),row={knowledge_id:this.adapter.id(),tenant_id:ctx.tenant_id,dealer_id:ctx.dealer_id,subject,type,predicate,value:sanitize(input.value),text:text(input.text,12000),sources,evidence:sanitize(input.evidence||[]),confidence:Number.isFinite(confidence)?Math.max(0,Math.min(1,confidence)):null,effective_from:iso(input.effective_from||now),effective_until:input.effective_until?iso(input.effective_until):null,created_at:now,created_by:principal.id,last_verified_at:input.last_verified_at?iso(input.last_verified_at):null,expires_at:input.expires_at?iso(input.expires_at):null,supersedes:safeId(input.supersedes),privacy_classification:privacy,permissions:sanitize(input.permissions||{roles:principal.roles}),status:type==='MODEL_HYPOTHESIS'?'HYPOTHESIS':'ACTIVE',immutable_source:true};if(row.supersedes){const previous=this.bucket(ctx,'knowledge').find(item=>item.knowledge_id===row.supersedes);if(!previous||!eventReadable(previous,principal))throw platformError(404,'knowledge_superseded_missing','Te vervangen kennisrecord bestaat niet');if(!previous.effective_until)previous.effective_until=now;previous.status='SUPERSEDED';previous.superseded_by=row.knowledge_id}this.bucket(ctx,'knowledge').push(row);this.audit(ctx,principal,'CREATE','knowledge',row.knowledge_id,{type,predicate,supersedes:row.supersedes});this.commit();return clone(row)}
  searchKnowledge(context,principalInput,query={}){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'knowledge:read');const q=text(query.q,500).toLowerCase(),at=iso(query.at||this.now()),current=query.current!==false;let rows=this.bucket(ctx,'knowledge').filter(row=>{const allowed=(!Array.isArray(row.permissions?.roles)||row.permissions.roles.some(role=>principal.roles.includes(role))||principal.permissions.has('*'))&&eventReadable(row,principal);if(!allowed)return false;if(query.type&&row.type!==String(query.type).toUpperCase())return false;if(query.source&&!row.sources.some(source=>String(source.source_id||source.provider||source).toLowerCase()===String(query.source).toLowerCase()))return false;if(current&&(row.effective_from&&row.effective_from>at||row.effective_until&&row.effective_until<=at||row.expires_at&&row.expires_at<=at))return false;return !q||[row.subject,row.predicate,row.text,JSON.stringify(row.value)].some(value=>String(value||'').toLowerCase().includes(q))});rows=rows.map(row=>({...clone(row),freshness:row.expires_at&&row.expires_at<=at?'STALE':row.last_verified_at?freshness(row.last_verified_at,86400,Date.parse(at)).classification:'UNKNOWN'}));if(query.freshness)rows=rows.filter(row=>row.freshness===String(query.freshness).toUpperCase());rows=rows.sort((a,b)=>(b.confidence??-1)-(a.confidence??-1)||Date.parse(b.created_at)-Date.parse(a.created_at));return {items:rows.slice(0,Math.max(1,Math.min(100,Number(query.limit)||20))),total:rows.length,at,current,tenant_filtered:true,permission_filtered:true,model_context_safe:true,retrieval:{structured:true,full_text:true,semantic:false,time_filter:true,source_filter:true,freshness_filter:true,tenant_filter_before_context:true,permission_filter_before_context:true},no_training_claim:true}}
  deriveInsight(context,principalInput,input){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'learning:write');const evidence=Array.isArray(input.evidence_set)?sanitize(input.evidence_set):[],sample=Number(input.sample_size),confidence=Number(input.confidence);if(!text(input.type,120)||!text(input.model_version,120)||!evidence.length||!Number.isInteger(sample)||sample<1||!Number.isFinite(confidence)||confidence<0||confidence>1)throw platformError(422,'learning_insight_invalid','Inzicht vereist type, modelversie, bewijsset, steekproef en confidence');const row={insight_id:this.adapter.id(),tenant_id:ctx.tenant_id,dealer_id:ctx.dealer_id,type:text(input.type,120),model_version:text(input.model_version,120),rule_version:text(input.rule_version,120)||null,evidence_set:evidence,sample_size:sample,confidence,statement:text(input.statement,4000),structured_value:sanitize(input.structured_value),created_at:this.now(),created_by:principal.id,last_evaluated_at:null,valid_from:iso(input.valid_from||this.now()),valid_until:input.valid_until?iso(input.valid_until):null,outcome_metrics:{evaluations:0,mae:null,accuracy:null,precision:null,recall:null,calibration:null,drift:null},status:'DERIVED_NOT_RETRAINED'};this.bucket(ctx,'insights').push(row);this.audit(ctx,principal,'DERIVE','insight',row.insight_id,{model_version:row.model_version,sample_size:sample});this.commit();return clone(row)}
  recordFeedback(context,principalInput,input){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'learning:write');const insight=this.bucket(ctx,'insights').find(row=>row.insight_id===input.insight_id);if(!insight)throw platformError(404,'learning_insight_missing','Inzicht niet gevonden');const prediction=Number(input.prediction),actual=Number(input.actual);if(!Number.isFinite(prediction)||!Number.isFinite(actual))throw platformError(400,'learning_feedback_invalid','Prediction en actual moeten numeriek zijn');const predictedClass=input.predicted_class===undefined?null:Boolean(input.predicted_class),actualClass=input.actual_class===undefined?null:Boolean(input.actual_class),correct=input.correct===undefined?(predictedClass===null||actualClass===null?null:predictedClass===actualClass):Boolean(input.correct),probability=Number(input.probability);const row={feedback_id:this.adapter.id(),tenant_id:ctx.tenant_id,dealer_id:ctx.dealer_id,insight_id:insight.insight_id,prediction,timestamp:iso(input.timestamp||this.now()),model_version:insight.model_version,actual,error:actual-prediction,absolute_error:Math.abs(actual-prediction),predicted_class:predictedClass,actual_class:actualClass,correct,probability:Number.isFinite(probability)?Math.max(0,Math.min(1,probability)):null,drift:Number.isFinite(Number(input.drift))?Number(input.drift):null,created_at:this.now(),created_by:principal.id};this.bucket(ctx,'feedback').push(row);const all=this.bucket(ctx,'feedback').filter(item=>item.insight_id===insight.insight_id),classified=all.filter(item=>item.predicted_class!==null&&item.actual_class!==null),tp=classified.filter(item=>item.predicted_class&&item.actual_class).length,fp=classified.filter(item=>item.predicted_class&&!item.actual_class).length,fn=classified.filter(item=>!item.predicted_class&&item.actual_class).length,correctRows=all.filter(item=>item.correct!==null),calibrated=all.filter(item=>item.probability!==null&&item.actual_class!==null);insight.last_evaluated_at=this.now();insight.outcome_metrics={...insight.outcome_metrics,evaluations:all.length,mae:Number((all.reduce((sum,item)=>sum+item.absolute_error,0)/all.length).toFixed(6)),accuracy:correctRows.length?Number((correctRows.filter(item=>item.correct).length/correctRows.length).toFixed(6)):null,precision:tp+fp?Number((tp/(tp+fp)).toFixed(6)):null,recall:tp+fn?Number((tp/(tp+fn)).toFixed(6)):null,calibration:calibrated.length?Number((calibrated.reduce((sum,item)=>sum+Math.abs(item.probability-(item.actual_class?1:0)),0)/calibrated.length).toFixed(6)):null,drift:row.drift??insight.outcome_metrics.drift};insight.status='EVALUATED_NO_AUTOMATIC_RETRAINING';this.audit(ctx,principal,'EVALUATE','insight',insight.insight_id,{feedback_id:row.feedback_id,evaluations:all.length});this.commit();return {feedback:clone(row),insight:clone(insight),base_model_retrained:false,learning_claim_supported:all.length>0}}
  learningStatus(context,principalInput){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'analysis:read');const insights=this.bucket(ctx,'insights'),feedback=this.bucket(ctx,'feedback');return {items:clone(insights),total:insights.length,feedback_records:feedback.length,evaluated:insights.filter(row=>Number(row.outcome_metrics?.evaluations)>0).length,unevaluated:insights.filter(row=>!Number(row.outcome_metrics?.evaluations)).length,base_model_retrained:false,learning_claim:'OUTCOME_EVIDENCE_REQUIRED',tenant_filtered:true}}
  declareConnector(context,principalInput,input){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'connectors:manage');const provider=text(input.provider,80),contract={provider,capabilities:sanitize(input.capabilities||[]),auth_type:text(input.auth_type,80),config_schema:sanitize(input.config_schema||{}),scopes:sanitize(input.scopes||[]),probe:sanitize(input.probe||{}),sync_directions:sanitize(input.sync_directions||[]),supported_entities:sanitize(input.supported_entities||[]),webhooks:sanitize(input.webhooks||[]),rate_limits:sanitize(input.rate_limits||{}),retry:sanitize(input.retry||{max_attempts:5,backoff:'exponential_with_jitter'}),cursor:sanitize(input.cursor||{type:'opaque'}),freshness:sanitize(input.freshness||{expected_seconds:3600}),mapping:sanitize(input.mapping||{}),version:Number(input.version||1)};if(!provider||!contract.auth_type||!contract.capabilities.length||!contract.supported_entities.length)throw platformError(422,'connector_contract_invalid','Connector vereist provider, auth type, capabilities en entities');const rows=this.bucket(ctx,'connector_contracts'),existing=rows.find(row=>row.provider===provider&&row.version===contract.version);if(existing)return clone(existing);const row={id:this.adapter.id(),tenant_id:ctx.tenant_id,dealer_id:ctx.dealer_id,...contract,created_at:this.now(),created_by:principal.id,secret_fields_server_only:true};rows.push(row);this.audit(ctx,principal,'DECLARE','connector',row.id,{provider,version:row.version});this.commit();return clone(row)}
  connectorTransition(context,principalInput,provider,next,evidence={}){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'connectors:manage');const state=String(next).toUpperCase();if(!CONNECTOR_STATES.has(state))throw platformError(400,'connector_state_invalid','Connectorstatus is ongeldig');const rows=this.bucket(ctx,'connector_states'),current=rows.find(row=>row.provider===provider)||{id:this.adapter.id(),provider,state:'UNCONFIGURED',history:[]},history={from:current.state,to:state,at:this.now(),actor_id:principal.id,evidence:sanitize(evidence)};if(state==='CONNECTED'&&!(evidence.authenticated&&evidence.probe_ok&&evidence.initial_sync_ok))throw platformError(422,'connector_connected_unproven','CONNECTED vereist authenticatie, geslaagde probe en initiële sync');current.state=state;current.updated_at=history.at;current.history.push(history);current.authenticated=Boolean(evidence.authenticated);current.probe_ok=Boolean(evidence.probe_ok);current.initial_sync_ok=Boolean(evidence.initial_sync_ok);current.last_probe_at=evidence.last_probe_at?iso(evidence.last_probe_at):current.last_probe_at||null;current.last_sync_at=evidence.last_sync_at?iso(evidence.last_sync_at):current.last_sync_at||null;current.error=text(evidence.error,500)||null;if(!rows.includes(current))rows.push(current);this.audit(ctx,principal,'TRANSITION','connector',current.id,{provider,...history});this.commit();return clone(current)}
  connectorCheckpoint(context,principalInput,input){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'connectors:manage');const provider=text(input.provider,80),direction=String(input.direction||'INBOUND').toUpperCase(),mode=String(input.mode||'INCREMENTAL').toUpperCase();if(!provider||!['INBOUND','OUTBOUND','BIDIRECTIONAL'].includes(direction)||!['FULL_INITIAL','INCREMENTAL','WEBHOOK','MANUAL_RESYNC','RECONCILIATION'].includes(mode))throw platformError(400,'connector_checkpoint_invalid','Connectorcheckpoint is ongeldig');const rows=this.bucket(ctx,'connector_checkpoints'),existing=rows.find(row=>row.provider===provider&&row.direction===direction),row=existing||{id:this.adapter.id(),provider,direction,created_at:this.now()};row.mode=mode;row.cursor=text(input.cursor,1000)||null;row.watermark=iso(input.watermark)||null;row.last_sync_at=this.now();row.records_applied=Math.max(0,Number(input.records_applied)||0);row.dead_letter_count=Math.max(0,Number(input.dead_letter_count)||0);row.idempotency_scope=text(input.idempotency_scope,200)||`${provider}:${direction}`;if(!existing)rows.push(row);this.commit();return clone(row)}
  connectorStatus(context,principalInput,provider=null){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'platform:read');const contracts=this.bucket(ctx,'connector_contracts'),states=this.bucket(ctx,'connector_states'),checkpoints=this.bucket(ctx,'connector_checkpoints'),attempts=this.bucket(ctx,'connector_attempts'),webhooks=this.bucket(ctx,'connector_webhooks'),providers=[...new Set([...contracts.map(row=>row.provider),...states.map(row=>row.provider)])].filter(value=>!provider||value===provider),items=providers.map(id=>{const contract=contracts.filter(row=>row.provider===id).sort((a,b)=>b.version-a.version)[0]||null,state=states.find(row=>row.provider===id)||{state:'UNCONFIGURED'},lastAttempt=attempts.filter(row=>row.provider===id).sort((a,b)=>Date.parse(b.updated_at)-Date.parse(a.updated_at))[0]||null;return {provider:id,state:state.state,connected:state.state==='CONNECTED'&&Boolean(state.authenticated&&state.probe_ok&&state.initial_sync_ok),authenticated:Boolean(state.authenticated),probe_ok:Boolean(state.probe_ok),initial_sync_ok:Boolean(state.initial_sync_ok),last_probe_at:state.last_probe_at||null,last_sync_at:state.last_sync_at||null,error:state.error||null,contract:contract?{version:contract.version,capabilities:contract.capabilities,auth_type:contract.auth_type,sync_directions:contract.sync_directions,supported_entities:contract.supported_entities,secret_fields_server_only:true}:null,checkpoints:clone(checkpoints.filter(row=>row.provider===id)),last_attempt:lastAttempt?clone(lastAttempt):null,webhook_receipts:webhooks.filter(row=>row.provider===id).length,status_truthful:true}});return {items,total:items.length,connected:items.filter(row=>row.connected).length,configured:items.filter(row=>row.state!=='UNCONFIGURED').length,connected_contract:'AUTHENTICATED + PROBE_OK + INITIAL_SYNC_OK',tenant_filtered:true}}
  recordConnectorAttempt(context,principalInput,input={}){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'connectors:manage');const provider=text(input.provider,80),operation=String(input.operation||'SYNC').toUpperCase(),idempotencyKey=text(input.idempotency_key,200),success=Boolean(input.success),maxAttempts=Math.max(1,Math.min(20,Number(input.max_attempts)||5));if(!provider||!['AUTH','PROBE','SYNC','WEBHOOK','RECONCILIATION'].includes(operation)||!/^[A-Za-z0-9_.:-]{8,200}$/.test(idempotencyKey))throw platformError(422,'connector_attempt_invalid','provider, operation en idempotency_key zijn verplicht');const rows=this.bucket(ctx,'connector_attempts'),existing=rows.find(row=>row.provider===provider&&row.operation===operation&&row.idempotency_key===idempotencyKey);if(existing&&existing.status==='SUCCEEDED')return {...clone(existing),replayed:true};const row=existing||{id:this.adapter.id(),tenant_id:ctx.tenant_id,dealer_id:ctx.dealer_id,provider,operation,idempotency_key:idempotencyKey,attempts:0,created_at:this.now()};row.attempts++;row.max_attempts=maxAttempts;row.updated_at=this.now();row.error=success?null:text(input.error,500)||'provider_operation_failed';row.result=success?sanitize(input.result||{}):null;row.status=success?'SUCCEEDED':row.attempts>=maxAttempts?'DEAD_LETTER':'RETRY_SCHEDULED';row.next_attempt_at=row.status==='RETRY_SCHEDULED'?new Date(this.adapter.now().getTime()+Math.min(3600000,30000*(2**(row.attempts-1)))).toISOString():null;row.backoff='EXPONENTIAL_CAPPED';if(!existing)rows.push(row);this.audit(ctx,principal,success?'SUCCEED':'FAIL','connector_attempt',row.id,{provider,operation,status:row.status,attempts:row.attempts});this.commit();return clone(row)}
  recordConnectorWebhook(context,principalInput,input={}){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'connectors:manage');const provider=text(input.provider,80),providerEventId=text(input.provider_event_id,300),signatureVerified=Boolean(input.signature_verified);if(!provider||!providerEventId||!signatureVerified)throw platformError(401,'connector_webhook_unverified','Ondertekende provider-webhook met event-ID is verplicht');const rows=this.bucket(ctx,'connector_webhooks'),existing=rows.find(row=>row.provider===provider&&row.provider_event_id===providerEventId);if(existing)return {...clone(existing),deduplicated:true};const row={id:this.adapter.id(),tenant_id:ctx.tenant_id,dealer_id:ctx.dealer_id,provider,provider_event_id:providerEventId,signature_verified:true,payload_digest:digest(JSON.stringify(input.payload||{})),received_at:this.now(),status:'ACCEPTED',idempotent:true,raw_payload_stored:false,secret_material:false};rows.push(row);this.audit(ctx,principal,'INGEST','connector_webhook',row.id,{provider,provider_event_id:providerEventId});this.commit();return clone(row)}
  enqueueOutbox(context,principalInput,input){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'events:write');const risk=String(input.risk||'LOW').toUpperCase();if(['HIGH','FINANCIAL','LEGAL'].includes(risk))throw platformError(422,'outbox_high_risk_conflict_policy_required','High-risk/offline mutation vereist online bevestiging en domeinspecifieke conflictresolutie');const idempotencyKey=text(input.idempotency_key,200);if(!/^[A-Za-z0-9_.:-]{8,200}$/.test(idempotencyKey))throw platformError(400,'outbox_idempotency_invalid','Geldige idempotency_key is verplicht');const rows=this.bucket(ctx,'outbox'),existing=rows.find(row=>row.idempotency_key===idempotencyKey);if(existing)return {...clone(existing),deduplicated:true};const row={id:this.adapter.id(),tenant_id:ctx.tenant_id,dealer_id:ctx.dealer_id,idempotency_key:idempotencyKey,entity_type:text(input.entity_type,80),entity_id:safeId(input.entity_id),operation:String(input.operation||'UPSERT').toUpperCase(),base_revision:Number(input.base_revision||0),payload:sanitize(input.payload||{}),risk,status:'PENDING',attempts:0,created_at:this.now(),created_by:principal.id,conflict_strategy:'OPTIMISTIC_REVISION'};rows.push(row);this.commit();return clone(row)}
  defineAutomation(context,principalInput,input){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'automation:manage');const name=text(input.name,200),version=Number(input.version||1),trigger=sanitize(typeof input.trigger==='object'?input.trigger:{type:input.trigger}),triggerType=String(trigger.type||'').toLowerCase(),actions=Array.isArray(input.actions)?sanitize(input.actions):[];if(!name||!Number.isInteger(version)||version<1||!AUTOMATION_TRIGGERS.has(triggerType)||!actions.length||actions.some(action=>!AUTOMATION_ACTIONS.has(String(action.type||'').toLowerCase())))throw platformError(422,'automation_invalid','Naam, ondersteunde trigger, positieve versie en ondersteunde acties zijn verplicht');validateWorkflow(actions);const highRisk=actions.some(action=>HIGH_RISK_AUTOMATION_ACTIONS.has(String(action.type).toLowerCase())),enabled=input.enabled!==false,approvalRequired=highRisk||Boolean(input.approval_required),definition={name,version,trigger,actions,enabled,approval_required:approvalRequired},signature=digest(JSON.stringify(definition)),rows=this.bucket(ctx,'automations'),existing=rows.find(row=>row.name===name&&row.version===version);if(existing){const previous={name:existing.name,version:existing.version,trigger:existing.trigger,actions:existing.actions,enabled:existing.enabled!==false,approval_required:Boolean(existing.approval_required)};if(digest(JSON.stringify(previous))!==signature)throw platformError(409,'automation_version_immutable','Bestaande workflowversie is immutable');return {...clone(existing),idempotent:true}}const row={id:this.adapter.id(),tenant_id:ctx.tenant_id,dealer_id:ctx.dealer_id,name,version,signature,trigger,actions,enabled:input.enabled!==false,risk:highRisk?'HIGH':'MEDIUM',approval_required:highRisk||Boolean(input.approval_required),created_at:this.now(),created_by:principal.id,immutable_version:true};this.moduleMutation(ctx,['automations'],()=>{rows.push(row);this.audit(ctx,principal,'DEFINE','automation',row.id,{version:row.version,risk:row.risk});queueOwnedEvent(this,ctx,principal,'automation','workflow',row,'created');});flushOwnedEvents(this,ctx,principal);return clone(row)}
  automationRecords(context,principalInput,entity,query={}){
    const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'platform:read');
    if(!['tasks','documents'].includes(entity))throw platformError(404,'automation_entity_invalid','Onbekend automationonderdeel');
    const privileged=principal.permissions.has('*')||principal.permissions.has('automation:manage');
    const rows=this.bucket(ctx,`automation_${entity}`).filter(row=>privileged||row.owner_id===principal.id),limit=Math.max(1,Math.min(250,Number(query.limit)||100)),offset=Math.max(0,Number(query.offset)||0);
    return {items:clone(rows.slice(offset,offset+limit)),total:rows.length,next_offset:offset+limit<rows.length?offset+limit:null,tenant_filtered:true};
  }
  exportAutomation(context,principalInput){
    const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'export:run');
    const collections=Object.fromEntries(['automations','automation_runs','automation_tasks','automation_documents'].map(scope=>[scope,clone(this.bucket(ctx,scope))]));
    this.audit(ctx,principal,'EXPORT','automation',null,{collections:Object.keys(collections)});this.commit();return {module_id:'automation',tenant_id:ctx.tenant_id,collections,audited:true};
  }
  flushModuleEvents(context,principalInput){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'events:write');return flushOwnedEvents(this,ctx,principal);}
  moduleMutation(ctx,scopes,mutate){return scopedMutation({bucket:(ctx,name)=>this.bucket(ctx,name),persist:()=>this.commit()},ctx,[...scopes,'audit','module_event_outbox'],mutate);}
  createAutomationRecord(context,principalInput,entity,input,options={}){
    const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'automation:manage');
    if(!['tasks','documents'].includes(entity))throw platformError(404,'automation_entity_invalid','Onbekend automationonderdeel');
    const title=text(input.title,240);if(!title)throw platformError(422,'automation_title_required','Titel is verplicht');
    const fields={title,content:text(input.content,12000),owner_id:safeId(input.owner_id)||principal.id,related_entity:text(input.related_entity,80)||null,related_id:safeId(input.related_id),status:entity==='tasks'?'OPEN':'DRAFT'};
    const rows=this.bucket(ctx,`automation_${entity}`),key=text(options.idempotencyKey,200),fingerprint=digest(JSON.stringify(fields)),prior=key&&rows.find(row=>row.idempotency_key===key&&row.created_by===principal.id);
    if(prior){if(prior.fingerprint!==fingerprint)throw platformError(409,'automation_record_conflict','Idempotency key heeft andere inhoud');return clone(prior)}
    if(rows.length>=25000)throw platformError(507,'automation_record_capacity','Recordlimiet bereikt');
    const row={id:this.adapter.id(),tenant_id:ctx.tenant_id,dealer_id:ctx.dealer_id,...fields,idempotency_key:key||null,fingerprint,created_at:this.now(),updated_at:this.now(),created_by:principal.id,revision:1,source_module:'automation',schema_version:1,external_write:false};
    this.moduleMutation(ctx,[`automation_${entity}`],()=>{rows.push(row);this.audit(ctx,principal,'CREATE',`automation:${entity}`,row.id,{status:row.status});queueOwnedEvent(this,ctx,principal,'automation',entity,row,'created');});flushOwnedEvents(this,ctx,principal);return clone(row);
  }
  runAutomation(context,principalInput,automationId,event,options={}){
    const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'automation:manage');
    const workflow=this.bucket(ctx,'automations').find(row=>row.id===automationId&&row.enabled);
    if(!workflow)throw platformError(404,'automation_missing','Automation niet gevonden');
    return executeWorkflow(this,ctx,principal,workflow,event,options,{sanitize,highRisk:HIGH_RISK_AUTOMATION_ACTIONS});
  }
  tickAutomations(context,principalInput){
    const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'automation:manage');const now=this.adapter.now().getTime(),results=[];
    // Only explicitly opted-in workflow versions subscribe. Historical workflows
    // remain manual until their owner creates a version with automatic=true.
    const workflows=this.bucket(ctx,'automations').filter(row=>row.enabled&&row.trigger.automatic===true);
    const aliases={new_lead:'lead_created',stage_changed:'crm_record_updated',appointment:'appointment_scheduled',payment:'invoice_paid',connector_state:'connector_state_changed'};
    for(const flow of workflows){
      if(results.length>=10)break;
      const pending=this.bucket(ctx,'automation_runs').filter(row=>row.automation_id===flow.id&&row.status==='WAITING_TIME'&&Date.parse(row.next_wakeup_at)<=now&&row.actor_id===principal.id);
      for(const row of pending.slice(0,10-results.length))results.push(this.runAutomation(ctx,principalInput,flow.id,row.trigger,{inputs:row.inputs}));
      if(flow.trigger.type==='schedule'){
        const at=Date.parse(flow.trigger.at||'');if(Number.isFinite(at)&&at<=now&&results.length<10){const event={event_id:`schedule:${flow.id}:${at}`,event_version:1,type:'schedule',occurred_at:new Date(at).toISOString(),source:'configured_schedule'};if(!this.bucket(ctx,'automation_runs').some(row=>row.automation_id===flow.id&&row.event_id===event.event_id))results.push(this.runAutomation(ctx,principalInput,flow.id,event));}
      }else{
        const eventName=flow.trigger.event_name||aliases[flow.trigger.type];if(!eventName)continue;
        const events=this.bucket(ctx,'raw_events').filter(event=>event.event_name===eventName&&Date.parse(event.received_at)>=Date.parse(flow.created_at)&&eventReadable(event,principal)&&!this.bucket(ctx,'automation_runs').some(row=>row.automation_id===flow.id&&row.event_id===event.event_id));
        for(const event of events.slice(0,10-results.length))results.push(this.runAutomation(ctx,principalInput,flow.id,event));
      }
    }
    return {processed:results.length,runs:results.map(row=>({run_id:row.run_id,status:row.status})),explicit_opt_in:true};
  }
  automationStatus(context,principalInput){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'platform:read');const visible=row=>principal.permissions.has('*')||principal.permissions.has('automation:manage')||row.created_by===principal.id||row.actor_id===principal.id,workflows=this.bucket(ctx,'automations').filter(visible),runs=this.bucket(ctx,'automation_runs').filter(visible);return {workflows:clone(workflows),runs:clone(runs.slice(-100).reverse()),workflow_count:workflows.length,run_count:runs.length,awaiting_approval:runs.filter(row=>row.status==='AWAITING_APPROVAL').length,failed:runs.filter(row=>row.status==='ERROR').length,blocked:runs.filter(row=>row.status==='BLOCKED').length,execution_adapter:typeof this.adapter.executeAutomationAction==='function'?'AVAILABLE':'NOT_CONFIGURED',tenant_filtered:true}}
  resolvePack(input){const industry=String(input.industry||'').toLowerCase(),model=String(input.business_model||'').toLowerCase();if(/auto|dealer|mobility|vehicle/.test(industry))return'AUTOMOTIVE';if(/e.?commerce|retail|shop|webwinkel/.test(industry)||/e.?commerce/.test(model))return'ECOMMERCE';if(/consult|service|agency|advies|professional/.test(industry))return'PROFESSIONAL_SERVICES';if(/real.?estate|vastgoed|makelaar/.test(industry))return'REAL_ESTATE';return'GENERAL_B2B'}
  provision(context,principalInput,input,options={}){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'provision:manage');for(const field of ['business_name','country','market','industry','segment','business_model'])if(!text(input[field],200))throw platformError(422,'provision_profile_invalid',`${field} is verplicht`);const packId=this.resolvePack(input),pack=CAPABILITY_PACKS[packId],profile={business_name:text(input.business_name,200),country:text(input.country,80).toUpperCase(),market:text(input.market,120),industry:text(input.industry,120),segment:text(input.segment,120),business_model:text(input.business_model,120),locations:sanitize(input.locations||[]),channels:sanitize(input.channels||[]),existing_software:sanitize(input.existing_software||[]),pack:packId,modules:pack.modules,capabilities:[...new Set([...pack.modules,'events','knowledge','learning','connectors','provisioning'])],engines:['crm','analysis','finance','event_gateway','knowledge','learning','automation'],neural_regions:pack.modules.filter(value=>!['analysis','finance'].includes(value)),crm_configuration:{pipeline_templates:pack.pipelines,locale:input.country==='NL'?'nl-NL':'en-US',currency:input.currency||'EUR'},pipelines:pack.pipelines,kpis:pack.kpis,dashboard_presets:['FOUNDLY_CRM_COMMAND_CENTER','FOUNDLY_ANALYSIS_COMMAND_DASHBOARD','FOUNDLY_FINANCE_COMMAND_CENTER'],workspaces:pack.workspaces||[],connectors:pack.connectors,workflows:['lead_follow_up','connector_degraded','invoice_overdue'],roles:pack.roles,permissions:'ROLE_BASED_SERVER_ENFORCED',zero_tools:pack.zero_tools,schema_version:PLATFORM_SCHEMA_VERSION,one_core_codebase:true,customer_code_fork:false};const signature=digest(JSON.stringify(profile)),profiles=this.bucket(ctx,'tenant_profiles'),existing=profiles.find(row=>row.signature===signature);if(existing)return {ok:true,profile:clone(existing),created:false,idempotent:true,diff:{added:[],removed:[],changed:[]},business_records_created:0,no_demo_data:true};const prior=profiles.at(-1),row={id:this.adapter.id(),tenant_id:ctx.tenant_id,dealer_id:ctx.dealer_id,...profile,signature,revision:Number(prior?.revision||0)+1,created_at:this.now(),created_by:principal.id,status:'PROVISIONED_CONFIGURATION'};profiles.push(row);this.audit(ctx,principal,'PROVISION','tenant_profile',row.id,{pack:packId,revision:row.revision});this.commit();const diff=prior?{added:row.modules.filter(value=>!prior.modules.includes(value)),removed:prior.modules.filter(value=>!row.modules.includes(value)),changed:['profile_revision']}:{added:row.modules,removed:[],changed:[]};return {ok:true,profile:clone(row),created:true,idempotent:false,diff,business_records_created:0,no_demo_data:true}}
  export(context,principalInput,scope,format='JSON'){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'export:run');const allowed={events:'raw_events',knowledge:'knowledge',insights:'insights',audit:'audit'},bucket=allowed[scope];if(!bucket)throw platformError(404,'platform_export_scope_invalid','Niet-ondersteunde export');const rows=clone(this.bucket(ctx,bucket).filter(row=>(!Array.isArray(row.permissions?.roles)||row.permissions.roles.some(role=>principal.roles.includes(role))||principal.permissions.has('*'))&&eventReadable(row,principal))),kind=String(format).toUpperCase();this.audit(ctx,principal,'EXPORT',scope,null,{format:kind,count:rows.length});this.commit();if(kind==='CSV'){const keys=[...new Set(rows.flatMap(Object.keys))].filter(key=>!/(?:text|value|properties|evidence)/.test(key)),escape=value=>`"${String(value??'').replace(/"/g,'""')}"`;return {format:'CSV',content:[keys.map(escape).join(','),...rows.map(row=>keys.map(key=>escape(typeof row[key]==='object'?JSON.stringify(row[key]):row[key])).join(','))].join('\n'),count:rows.length,tenant_id:ctx.tenant_id,audited:true}}return {format:'JSON',records:rows,count:rows.length,tenant_id:ctx.tenant_id,audited:true}}
  observability(context,principalInput){const {ctx,principal}=this.scope(context,principalInput);requirePermission(principal,'platform:read');const queue=this.adapter.queueDepth?this.adapter.queueDepth(ctx):null,connectors=this.bucket(ctx,'connector_states'),events=this.bucket(ctx,'raw_events'),rollups=this.bucket(ctx,'event_rollups'),lastEvent=events.at(-1),lastRollup=rollups.reduce((latest,row)=>!latest||row.last_received_at>latest?row.last_received_at:latest,null),now=this.adapter.now().getTime(),runtime=typeof this.adapter.runtimeMetrics==='function'?sanitize(this.adapter.runtimeMetrics(ctx)):null;return {schema_version:PLATFORM_SCHEMA_VERSION,queue_depth:queue,events:events.length,event_lag:freshness(lastEvent?.received_at,60,now),analytics_lag:freshness(lastRollup,60,now),connectors:connectors.map(row=>({provider:row.provider,state:row.state,last_probe_at:row.last_probe_at,last_sync_at:row.last_sync_at,error:row.error||null})),automation_failures:this.bucket(ctx,'automation_runs').filter(row=>row.status==='ERROR').length,provider_deliveries:{queued:this.bucket(ctx,'provider_deliveries').filter(row=>row.status==='QUEUED').length,failed:this.bucket(ctx,'provider_deliveries').filter(row=>row.status==='ERROR').length},zero_tool_failures:this.bucket(ctx,'zero_tool_failures').length,http:runtime,pii_logged:false}}
  schema(){return {version:PLATFORM_SCHEMA_VERSION,event_version:EVENT_VERSION,ingest_version:INGEST_VERSION,kpis:clone(KPI_DEFINITIONS),analysis_dashboard:clone(ANALYSIS_DASHBOARD),knowledge_types:[...KNOWLEDGE_TYPES],privacy_classes:[...PRIVACY_CLASSES],connector_states:[...CONNECTOR_STATES],freshness:[...FRESHNESS],data_source_kinds:[...DATA_SOURCE_KINDS],sync_states:[...SYNC_STATES],automation:{triggers:[...AUTOMATION_TRIGGERS],actions:[...AUTOMATION_ACTIONS],high_risk_actions:[...HIGH_RISK_AUTOMATION_ACTIONS]},capability_packs:Object.keys(CAPABILITY_PACKS),tax:{legal_forms:LEGAL_FORMS,domains:TAX_DOMAINS,sources:TAX_SOURCES,verified_rule_count:BUILTIN_TAX_RULES.length,production_ready_domains:['BTW'],architecture_only_domains:TAX_DOMAINS.filter(domain=>domain!=='BTW')},provider_contracts:{meta:{verified_at:'2026-09-04',status:'ARCHITECTURE_VERIFIED_PROVIDER_ACCESS_REQUIRED'},ga4:{verified_at:'2026-09-04',status:'ARCHITECTURE_VERIFIED_PROVIDER_ACCESS_REQUIRED'},google_ads:{verified_at:'2026-09-04',status:'ARCHITECTURE_VERIFIED_PROVIDER_ACCESS_REQUIRED'}},data_platform:{operational_store:'encrypted_core_adapter',raw_event_store:'append_only_tenant_bucket',document_store:'content_addressed_object_manifest',full_text_search:'tenant_permission_filtered',semantic_index:'OPTIONAL_NOT_CLAIMED',knowledge_store:'typed_lifecycle_records',cache:'incremental_rollups',durability:'RUNTIME_DEPENDENT_REPORTED_SEPARATELY'},no_fake_data:true}}
}

module.exports={FoundlyPlatformCore,PLATFORM_SCHEMA_VERSION,EVENT_VERSION,INGEST_VERSION,KPI_DEFINITIONS,ANALYSIS_DASHBOARD,TAX_SOURCES,BUILTIN_TAX_RULES,CAPABILITY_PACKS,ROLE_PERMISSIONS,LEGAL_FORMS,TAX_DOMAINS,DATA_SOURCE_KINDS,SYNC_STATES,AUTOMATION_TRIGGERS,AUTOMATION_ACTIONS,META_STANDARD_EVENTS,GOOGLE_EVENT_NAMES,platformError,normalizeEmail,normalizePhone,sha256};
