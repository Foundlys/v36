'use strict';
const {FoundlyCrmCore}=require('../crm-core');
function fixture(){let stores=new Map(),sequence=0,persisted=0,disk='[]',failPersist=false;const events=[],ctx={tenant_id:'dashboard_tenant',dealer_id:'dashboard_dealer'},actor={id:'dashboard_actor',roles:['SALES'],team_ids:['team_a']},other={id:'dashboard_other',roles:['SALES'],team_ids:['team_a']},adapter={bucket(ctx,scope){const key=ctx.tenant_id+':'+ctx.dealer_id+':'+scope;if(!stores.has(key))stores.set(key,[]);return stores.get(key);},persist(){if(failPersist)throw Error('simulated persistence failure');persisted++;disk=JSON.stringify([...stores]);},emit(ctx,event){events.push(event);},id:()=> 'dashboard_id_'+(++sequence),now:()=>new Date('2026-09-24T12:00:00Z')},core=new FoundlyCrmCore(adapter),save=(name,input={},options={})=>core.saveDashboard(ctx,actor,{name,widgets:[{id:'dashboard_leads',type:'KPI',metric:'leads',x:0,y:0,w:4,h:2}],share_mode:'PRIVATE',...input},options),snapshot=()=>JSON.stringify([...stores].filter(([,rows])=>rows.length));return {core,ctx,actor,other,adapter,events,save,snapshot,count:()=>persisted,fail(value){failPersist=value;},restart(){stores=new Map(JSON.parse(disk));}};}
module.exports={fixture};
