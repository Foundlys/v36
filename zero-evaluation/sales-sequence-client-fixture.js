'use strict';
const fs=require('node:fs'),path=require('node:path'),vm=require('node:vm'),crypto=require('node:crypto'),{browserFixture}=require('./dom-fixture'),{WorkspaceView}=require('./workspace-page-fixture'),{fixture:nativeFixture,clone}=require('./sales-sequence-fixture'),service=require('../sales-sequences'),execution=require('../sales-sequence-execution'),outcomes=require('../sales-outcomes'),zero=require('../sales-zero');
function fixture(options={}){const f=browserFixture(options.locale||'en-GB'),native=options.native||nativeFixture(),actor=options.actor||native.actor,storage=options.storage||new Map(),realm=options.realm||{...native.ctx,actor_id:actor.id},runId=options.catalog||options.global?null:options.runId||native.enroll(),calls=[],gates=[];let active=true,denied=false,loss=false,before=false,alter=null,storageFailure=false,cleanupFailure=false;
 Object.assign(f.context,{crypto,TextEncoder,URL,URLSearchParams,sessionStorage:{getItem:k=>{if(storageFailure)throw Error('PRIVATE storage failure');return storage.get(k)??null;},setItem:(k,v)=>{if(storageFailure)throw Error('PRIVATE storage failure');storage.set(k,String(v));},removeItem:k=>{if(storageFailure||cleanupFailure)throw Error('PRIVATE cleanup failure');storage.delete(k);}}});f.context.document.createElement=tag=>new WorkspaceView(tag);f.context.document.dispatchEvent=event=>{for(const handler of f.context.document.handlers[event.type]||[])handler(event);return true;};
 const request=async(route,opts={})=>{const input=JSON.parse(opts.body||'{}'),method=opts.method||'GET',key=opts.headers?.['idempotency-key'],operation=route==='/api/zero/turn'?input.client_context.sales_action.operation:null,recovery=route.includes('sequence-requests')||operation==='SEQUENCE_RECOVER',mutation=['POST','PUT'].includes(method)&&!recovery&&(operation?zero.OPERATIONS[operation].mode==='write':!route.endsWith('/preview'));calls.push({route,input:clone(input),method,key,operation,recovery,mutation});if(options.transport)return options.transport(route,opts);if(denied)throw Object.assign(Error('PRIVATE sequence denial'),{status:403});if(before&&mutation){before=false;throw Error('PRIVATE pre-dispatch failure');}let data;
 try{if(operation)data=zero.execute(native.core,native.communication,native.ctx,actor,input.client_context.sales_action,{message:input.message,conversation_id:input.conversation_id,turn_id:input.turn_id});else if(recovery)data=service.recover(native.core,native.communication,native.ctx,actor,input);else{const url=new URL(route,'http://fixture'),parts=url.pathname.split('/').filter(Boolean);if(parts[2]==='sequences')data=parts[3]?parts[4]?service[parts[4]](native.core,native.communication,native.ctx,actor,parts[3],input,{idempotency_key:key}):service.define(native.core,native.ctx,actor,parts[3],input,{idempotency_key:key}):service.catalog(native.core,native.ctx,actor,Object.fromEntries(url.searchParams));else if(parts[2]==='sequence_runs'&&parts[3])data=parts[4]==='outcome'?method==='GET'?outcomes.preview(native.core,native.communication,native.ctx,actor,parts[3]):outcomes.record(native.core,native.communication,native.ctx,actor,parts[3],input,{idempotency_key:key}):method==='GET'?execution.read(native.core,native.communication,native.ctx,actor,parts[3]):parts[4]==='advance'?execution.advance(native.core,native.communication,native.ctx,actor,parts[3],input,{idempotency_key:key}):parts[4]==='task-complete'?execution.completeTask(native.core,native.communication,native.ctx,actor,parts[3],input,{idempotency_key:key}):execution.control(native.core,native.communication,native.ctx,actor,parts[3],parts[4].toUpperCase(),input,{idempotency_key:key});else data=parts[3]?{record:native.core.get(native.ctx,actor,parts[2],parts[3])}:native.core.list(native.ctx,actor,parts[2],Object.fromEntries(url.searchParams));}}catch(error){error.status=error.statusCode;throw error;}
 const gate=gates.find(g=>!g.used&&g.method===method&&route.includes(g.part));if(gate){gate.used=true;gate.started();await gate.promise;}if(loss&&mutation){loss=false;throw Error('PRIVATE lost sequence response');}return clone(alter?alter(clone(data),route,method):data);};
 for(const name of ['sales-sequence-client-state.js','sales-sequences-client.js','sales-zero-client.js']){const file=path.join(__dirname,'..',name);if(fs.existsSync(file))vm.runInContext(fs.readFileSync(file,'utf8'),f.context);}const conversation=crypto.randomUUID(),box=f.context.FoundlySalesSequences.create({document:f.context.document,request,requestContext:realm,runId,isActive:()=>active,zeroRequest:async(action,turn)=>{const data=await request('/api/zero/turn',{method:'POST',body:JSON.stringify({message:'Chosen Sales action',conversation_id:conversation,turn_id:turn,preferred_module:'sales',client_context:{sales_action:action}})});return data.sales_data;}});f.nodes.sequenceBox=box;
 const labels={reason:'Reden',confirm:'Ik bevestig deze afzonderlijke actie',title:'Reeksnaam',key:'Reekscode',step_title:'Stapnaam',transport:'Sales-bediening'},buttons={save:'Actie vastleggen',define:'Nieuwe reeksversie',open_run:'Opvolging bekijken',advance:'Volgende stap voorbereiden',refresh:'Reeksen verversen'};
 const field=name=>box.all().find(n=>n.getAttribute?.('data-sequence-field')===name)||box.all().find(n=>n.tag==='label'&&n.childNodes.find(c=>c.nodeType===3)?.textContent===labels[name])?.children.find(n=>['input','select','textarea'].includes(n.tag)),control=name=>box.all().find(n=>n.getAttribute?.('data-sequence-action')===name)||box.all().find(n=>n.tag==='button'&&n.textContent===buttons[name]);
 return Object.assign(f,{native,actor,realm,storage,runId,box,calls,field,control,i:f.context.FoundlyI18n,notice:()=>box.all().find(n=>n.tag==='output'),writes:()=>calls.filter(c=>c.mutation),async ready(){await box.ready;if(!control('save')&&runId&&!f.context.FoundlySalesSequenceState){await control('open_run').fire('click');await control('advance').fire('click');}if(options.zero){const input=field('transport');input.value='ZERO';await input.fire('change');}},async fill(){field('reason').value='Literal private sequence reason';await field('reason').fire('input');field('confirm').checked=true;},async submit(){const b=control('save');if(b?.getAttribute?.('data-sequence-action'))await b.fire('click');else await box.all().find(n=>n.tag==='form').fire('submit');},async save(){await this.fill();await this.submit();},retire:()=>active=false,deny:()=>denied=true,lose:()=>loss=true,loseBefore:()=>before=true,alter:fn=>alter=fn,failStorage:()=>storageFailure=true,failCleanup:()=>cleanupFailure=true,hold(part,method='GET'){let release,started;const promise=new Promise(r=>release=r);release.started=new Promise(r=>started=r);gates.push({part,method,promise,started,used:false});return release;}});
}
module.exports={fixture};
