# Customer reference scene — current correction

The user's attached black Foundly reference, explicitly approved on 2026-09-12, supersedes the prior historical-approval evidence gate. Historical renderer recovery is complete and is not repeated. The previous production customer visualization was rejected with HARD FAIL.

This candidate renders the constellation with native WebGL2: actual sphere meshes/material normals, a common perspective/world transform, depth buffering, curved triangle ribbons, particle points, moving trails, orbit geometry and a floating-point bloom pipeline where supported. The reference image is never embedded as background, texture, animation or video. Canvas-generated label/icon textures are billboard surfaces at their real scene depths; the DOM anchors supply keyboard access and revalidated navigation, not the visual graph. Renderer failure leaves the authorized sidebar and ZERO available and does not advertise a working 360 view.

The canonical 1536×864 reference maps the nine cluster positions as follows: upper-left orange Marketing; upper-centre cyan Mail; upper-right yellow Analytics; left blue Verkoop; right violet Finance; lower-right red Inkoop; bottom-right green CRM; bottom-left gold Agenda; lower-left pink Social Media. These are exactly the nine permitted product identities. Main anchor coordinates are back-projected to deliberately different Z values. Rotation never reflows those world points. The reference's obsolete product identities and sample business figures are not copied into customer content.

Existing capability resolver, workspace availability, entitlements, tenant/dealer checks, source connection evidence and navigation targets remain authoritative. Only the existing buildGraph result reaches scene labels, structural paths, sidebar or search. Social platforms remain absent unless individually connected and available. Unavailable products do not leave labelled decorative substitutes. ZERO remains the shared intelligence layer. The only server diff registers the new JS asset within the existing authenticated static map.

A deterministic 180-second motion path combines a continuous complete yaw revolution with positive/negative pitch and slight roll. A geometry-derived camera distance keeps active node surfaces framed. The canonical all-products front camera remains at the reference distance. The same elapsed-time clock drives spatial highlights and trails. Pause freezes clock and all orientation axes; manual inspection changes the paused pose, and resume adds subsequent path increments without resetting it. Focus/click pauses. Hidden-tab and reduced-motion behavior remain supported.

The fixed reference UI includes top-left brand, authorized product navigation, customer-function search, existing authenticated activity feed, global navigation control, live capability counts, ZERO command entry, slogan and bottom-right 360/pause state. No reference profile initials or revenue/lead/deal figures are fabricated. The activity feed uses the existing /api/events read boundary; no new business API is introduced.

## Evidence and limits

- Existing 2,560 real-resolver authorization profiles and actual isolated authenticated HTTP tests are retained.
- New scene tests cover canonical product anchors, immutable world coordinates, exact structural endpoints, full rotation, above/below movement, changing depth order, portrait/landscape framing, bounded frame-to-frame camera movement and exact pause/manual/resume.
- The production scene's actual GL command stream is additionally replayed through native EGL/OpenGL ES on the local software renderer. Shader compile/link and draw calls execute with GL error checks. Canvas labels in this offline harness use a registered Arial-compatible Nimbus Sans font. The harness produces 73 sequential scene frames over one full application motion cycle. This is real scene rendering, not browser rendering, production evidence or target-device FPS evidence.
- Supported cloud-browser navigation to the new isolated local preview returned `net::ERR_BLOCKED_BY_CLIENT`. No alternate browser, proxy or security bypass is used.
- Native scene inspection found and corrected flat outer orbit geometry, label colour-space handling, missing local font fallback, canonical capability collisions and framing overflow. It exposes actual spatial depth and occlusion. It does not establish pixel-level parity of the complete HTML UI with the reference.
- The completed-cycle front comparison also exposed underpowered blue core lighting: edge beacons were partly buried by the sphere depth surface. Their positions now sit outside that surface, with stronger blue edge/crown illumination matching the reference's visible lighting relationship. Targeted tests and native GL rendering were repeated for this source change; the earlier candidate evidence remains separately preserved.
- **Complete browser/reference fidelity and real-time browser motion/performance remain UNVERIFIED / BLOCKED_EXTERNAL. No visual PASS or completed-dashboard verdict is made.**
- Merge/deployment authorization exists, but release remains subject to the user's visual acceptance requirement. Do not convert green geometry tests or CI into a visual PASS or silently waive the remaining gate.
